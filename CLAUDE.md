# Working rules

- Never run `git commit` or `git push` unless the user explicitly says to commit/push in that message. Making changes, staging, and building/testing is fine without asking — committing is not.
