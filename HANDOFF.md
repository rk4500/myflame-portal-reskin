# Portal Reskin — Handoff

## Goal
Custom frontend for college portal (`my.flame.edu.in`), own account, personal use. Two fronts: web (Tampermonkey/Violentmonkey userscript — done, working) + Android app (`com.myflame`, confirmed React Native + WebView wrapper — **native injection working end-to-end, autostart, no PC needed at launch**; Kiwi Browser + Tampermonkey remains available as a no-patch fallback if ever needed).

**Final working APK**: `MyFLAME-reskin-working.apk` in the project root. Also pushed to the phone's `/sdcard/Download/MyFLAME-reskin.apk` for direct install from the file manager. Regenerate via the build sequence documented below if `hook.js` or `portal-reskin.user.js` change.

## Platform: Siteforce (Salesforce Experience Cloud)

Confirmed via HAR capture: **all** traffic is a single RPC endpoint, no separate REST/Connect API in use.

- Endpoint: `POST /s/sfsites/aura?r=N&aura.ApexAction.execute=1` (r= is just a request counter, ignore).
- Request `message` param (URL-encoded JSON): `{"actions":[{"id":"...","descriptor":"apex://Controller/ACTION$method","params":{...}}]}`
- Response JSON: `{"actions":[{"id":"...","state":"SUCCESS","returnValue":{...}}]}` — match by `id`.
- Auth context: `aura.context` (fwuid, app config) + `aura.token` (CSRF-ish, session-bound) — both scraped from page's initial HTML bootstrap script (`window.Aura.appBootstrap` / `Aura.Bootstrap.bootstrap(...)`). Token expires — re-scrape on `INVALID_TOKEN`/reload.
- Checked for `/services/data/...` (standard Salesforce REST/Connect API) — 404/not present on this domain. Portal doesn't expose it. **Don't bother with OAuth/REST route, stick to aura.**

## Mobile app

**Confirmed 2026-09-01** via Chrome remote debugging (`chrome://inspect`, USB debugging): `com.myflame` is a **React Native app wrapping a `react-native-webview`** pointed at `https://my.flame.edu.in/s/` — NOT Salesforce Mobile Publisher, not a plain native WebView wrapper as originally guessed. Evidence: `libhermes.so`, `libreactnative.so`, `libfbjni.so`, `librnscreens.so` in the APK; logcat shows `RNCWebViewManager`/`RNCWebViewClient` (the standard `react-native-webview` library's Android classes); `assets/index.android.bundle` is precompiled **Hermes bytecode** (magic `C6 1F BC 03`), not readable/patchable plain JS. App is a split-APK install (base + `arm64_v8a`/`en`/`xxhdpi` config splits) — the arch split alone carries the whole React Native native runtime, so any repackage must merge all splits, not just `base.apk`.

Since it's ultimately still a real `android.webkit.WebView` under the hood (RN just creates one via its own Java classes), the same Frida-gadget-hooks-WebView approach outlined below applies regardless of the RN wrapper — RN doesn't block it, it just means the hookable class name (`RNCWebViewClient` or similar) has to be discovered at runtime rather than assumed, and console output/behavior needs testing on the actual app rather than assumed from a generic "webview wrapper" mental model.

**iOS**: sideload resigned IPA via AltStore/Sideloadly + personal Apple ID, no jailbreak needed for a simple config/JS patch. Real runtime hooking (Frida-equivalent) without jailbreak is not really viable — skip if it comes to that, not worth the effort for this project. Not attempted yet.

### Android APK patch attempt (2026-09-01) — tooling built, injection unresolved

Goal: inject `portal-reskin.user.js` into the app's WebView automatically on every launch, no computer needed at runtime (Frida Gadget in **autostart "script" mode**, not a live `frida -U` attach session).

**Environment set up** (all in `.patch-tools/`, gitignore-worthy, not committed):
- `jdk-openjdk` + `android-apktool` (AUR) installed via pacman/yay (needed the user's sudo password, run manually).
- `.patch-tools/venv/` — Python venv with `objection` + `frida-tools` (`pip install objection frida-tools`), avoids needing sudo for the Python side.
- `.patch-tools/build-tools/` — extracted `build-tools_r34-linux.zip` (direct-download, no `sdkmanager`/license flow needed) purely for the classic `aapt`, `apksigner`, `zipalign` binaries — **`objection patchapk` needs real `aapt` specifically** (not `aapt2`) to run `aapt dump badging` and find the launchable Activity; this is separate from and unconditional on apktool's own `--skip-resources` flag, which only skips apktool's *own* resource decompilation step. apktool 3.0.2 (pacman/AUR) bundles its own `aapt2` internally and doesn't need a system `aapt` at all — only objection's separate check does.
- `.patch-tools/APKEditor.jar` (REAndroid/APKEditor, direct GitHub release download) — merges the 4 split APKs into one monolithic APK (`apkeditor m -i <dir> -o merged.apk`) before patching, since `react-native-webview`'s native libs live in the arch split, not base.
- `.patch-tools/hook.js` — the Frida script (generated by a small inline Python snippet from `portal-reskin.user.js`, embedding it as a JSON-escaped string constant; **do not hand-edit** `hook.js` directly, regenerate it from the generator snippet in this session's history if the userscript changes).
- `.patch-tools/gadget-config.json` — `{"interaction": {"type": "script", "path": "libfrida-gadget.script.so"}}`, per `objection patchapk --help`'s documented convention for autostart mode.

**Patch command that worked** (after the bugs below were fixed):
```
export PATH="$PWD/build-tools/android-14:$PWD/venv/bin:$PATH"
export JAVA_TOOL_OPTIONS="-Xmx6g"
objection patchapk -s flame-merged.apk -a arm64-v8a -c gadget-config.json -l hook.js -j 1
```

**Bugs found and fixed along the way** (all confirmed against real behavior, not guessed):
1. **`-a arm64` vs `-a arm64-v8a`** — passing the short `arm64` form makes objection name the native-lib folder literally `lib/arm64/` instead of Android's real ABI folder name `lib/arm64-v8a/`. The linker only searches real ABI names, so the gadget `.so` silently isn't found (`UnsatisfiedLinkError: dlopen failed: library "libfrida-gadget.so" not found`) even though the file is genuinely present in the APK. Fix: always pass the *exact* string `adb shell getprop ro.product.cpu.abi` returns.
2. **`OutOfMemoryError` during apktool's smali rebuild** — default JVM heap too small for this app's large multidex/RN smali tree. One rebuild thread died silently mid-build while the overall process still reported "success" and continued to zipalign/sign a **corrupted APK**. Fix: `JAVA_TOOL_OPTIONS=-Xmx6g` + `-j 1` (single-threaded rebuild, avoids parallel peak-memory pressure). This one is dangerous precisely because objection doesn't hard-fail on it — always check for `OutOfMemoryError` in the "may have failed, read the output" sections objection prints, don't just check the exit code.
3. **`Java.use('android.webkit.WebView')` crashes Frida's Java bridge** (`access violation accessing 0x0`, native crash inside `/frida/bridges/java.js`) **if called too early** — confirmed via live `frida -U -l script.js -n <process>` testing. Root cause: the real `android.webkit.WebView` implementation is loaded from a separate WebView-provider classloader (Chromium/Trichrome package) that may not exist in the process's classloader list yet at script-start; Frida's classloader search picks up an incomplete stub. Fix confirmed by direct test: wrap the `Java.use('android.webkit.WebView')` call in a try/catch + `setTimeout(retry, 1000)` loop — succeeds cleanly once the app has had a moment to actually create a WebView. **This fix is in the current `hook.js`.**
4. **Frida spawn mode (`frida -U -f <pkg>`) doesn't work on this non-rooted device** — `Failed to spawn: need Gadget to attach on jailed Android`. Expected: spawn-and-inject needs root (frida-server) or an already-embedded gadget; on a jailed device you can only **attach** (`-n <process-name>`) to something already running, which inherently races against however early the app calls `setWebViewClient` (usually before an external attach can happen). This is fine for the *autostart* production config (the gadget script runs from `System.loadLibrary` time, effectively before the app does anything), but it means **live `frida -U -n` attach can't be used to test the "catch the very first setWebViewClient call" behavior** — only the autostart config's own real timing can.

**Where it stands, unresolved**: with the ABI-folder fix + OOM fix + classloader-retry fix all applied, the gadget `.so` loads cleanly (confirmed via `adb logcat`, no `UnsatisfiedLinkError`), the app runs normally, real navigation reaches `https://my.flame.edu.in/s/` (confirmed via `ReactNativeJS` log lines showing the SSO `frontdoor.jsp` redirect completing) — **but the reskin never visually appears** (screenshotted the stock Siteforce UI, unmodified). No `[flame-inject]` log lines appear either, but that's inconclusive on its own: a gadget in `"type": "script"` autostart mode has no attached Frida client to receive `console.log` messages, and this Frida version doesn't appear to fall back to Android logcat for that mode (confirmed indirectly: switching to the same gadget in plain listen-mode + live `frida -U -n` attach *does* show `console.log` output fine, in the terminal, not logcat — so logcat silence in autostart mode isn't proof of failure by itself).

**Diagnosis was in progress, not concluded**, when this was parked: the plan was to reinstall the plain listen-mode diagnostic build, live-attach with a minimal probe script (`Java.choose('android.webkit.WebView', {...})` to find the real instance directly and call `evaluateJavascript` by hand), and confirm whether `evaluateJavascript` itself works via Frida's Java bridge on this specific device/app/WebView-sandboxing setup at all, before assuming the fault is in the hook's `setWebViewClient` override logic specifically. That probe was written (`.patch-tools/probe.js`) but not run to completion — session moved to a simpler fallback instead. **Resume from here next time**, tools are all still set up in `.patch-tools/`.

**Other things worth checking next time, not yet tried**:
- Does `RNCWebViewClient` (or whatever the actual runtime class name is) override `onPageFinished` itself, or does it delegate to the base `WebViewClient`? Our hook hooks whatever class `setWebViewClient` is actually called with (dynamic, not hardcoded), so this shouldn't matter, but hasn't been directly confirmed by inspecting the live instance's class name.
- Whether Android 16 (SDK 36, this device — very new OS, released same window as this session) has *other* Frida/ART incompatibilities beyond the one already found and fixed. Frida 17.17.0 (latest at time of testing) generally supports Android 16, but edge cases on freshly-released OS versions are common.
- Whether the WebView content process being sandboxed (`com.google.android.webview:sandboxed_process0`, confirmed in logcat) matters for where `onPageFinished`/`evaluateJavascript` actually need to be called from — in theory `WebViewClient` callbacks are always delivered back to the embedding app's main process via IPC regardless of renderer sandboxing, but this wasn't independently verified for this specific case.

### Fallback in use for now: Kiwi Browser + Tampermonkey (not a patch, no APK work)

Since the native patch stalled, recommended and in-use interim solution: **Kiwi Browser** (Chromium-based, Play Store) supports desktop Chrome extensions including Tampermonkey — install it from the Chrome Web Store inside Kiwi, paste in the same `portal-reskin.user.js` unmodified, full parity with desktop. Kiwi also supports Chromium's standard "Add to Home Screen → open as standalone window" PWA install, which gets rid of the address bar/tabs UI on the home-screen icon — closest thing to "installed as an app" without actually patching the native app.

Checked and ruled out for this specific ask (no-chrome "installed app" look + userscript support in one browser):
- **Brave for Android**: no browser-extension support at all on mobile (unlike Brave desktop) — can't run Tampermonkey.
- **Firefox for Android**: does support Violentmonkey (officially on its curated mobile add-on list) but its "Add to Home Screen" doesn't produce a chromeless standalone window like Chromium's PWA install does — always opens in a normal tab with browser UI.

**Current device state**: the phone currently has our resigned/self-signed patch build of `com.myflame` installed (from the aborted attempt above) — it works exactly like the stock app (since our injection never actually fired) but won't receive Play Store updates while it's this build. Reinstall the real one from Play Store whenever convenient; not urgent since it's functionally identical to stock right now.

## Frontend plan: Tampermonkey userscript
- `@match https://my.flame.edu.in/*`, `@grant none` (runs in page's own JS context → inherits session cookies automatically, no auth plumbing needed).
- Strategy chosen: **full replace** — hide original Siteforce DOM (`.siteforceLayoutDiv` etc via injected `<style>`), render fully custom UI driven by our own `callAura()` calls using the mapped descriptors.
- Gotcha: Aura re-renders parts of DOM on nav (pushState SPA, no full reload) — need `MutationObserver` on `document.body` to keep re-hiding original chrome as it reappears.
- `callAura(descriptor, params)` helper not yet written — needs the context/token scraping logic wired in.

## Tooling built
`parse_aura_har.py` (scratchpad — **move this into the new project folder**):
- Input: exported HAR file.
- Parses every aura `ApexAction.execute` POST, matches request `actions[]` to response `actions[]` by id, dedupes by descriptor+params signature, groups by page (via HAR page refs).
- Output: `<prefix>.json` (full data) + `<prefix>.md` (readable, per-descriptor sections with params + returnValue samples).
- Usage: `python3 parse_aura_har.py portal.har booking_map`

## Progress / state (2026-09-01)
- Parser run → `aura_map.json` / `aura_map.md` (24 calls, all on `/s/calendar`, single descriptor `aura://ApexActionController/ACTION$execute` differentiated by `classname`+`method`).
- Descriptors mapped:
  - `StudentPortalCalendarCtrl.getAllScheduledEvents({userId})` — class schedule
  - `PortalEventController.getAllEvents()` — campus events
  - `CustomBookingController.getResources()` (cacheable) — facility/resource list, **double-JSON-encoded** (returnValue is a JSON string, parse again)
  - `CustomBookingController.getResourceAvailability({resourceId, bookingDate})` — slots, also double-JSON-encoded
  - `CustomBookingController.getReservations({userId})` — user's bookings (covers my-bookings)
  - Bonus: `PortalAllFeedsCtrl.getAllEvents` (news feed), `PortalCreateServiceAndSupportTicketCntrl.getCases`, `NavigationMenuItemsController.getNavigationMenuItems` (nav tree), `ResourceController.getCategoriesWithResources`.
- **Booking SUBMIT + CANCEL now mapped** (2026-09-01, second HAR capture `my.flame.edu.in_Archive book and cancel.har`, merged into `aura_map.json`/`.md` — 29 distinct calls total now):
  - `CustomBookingController.createReservation({dateSelected, startTime, endTime, resource, bookingPurpose, coAttendee, userId})` → `returnValue` is a human-readable string: `"Booking request submitted successfully with Booking Id R-714794. Check your email for updates. Thank you."` — parse the `R-\d+` id out of it if needed programmatically.
  - `CustomBookingController.cancelReservation({userId, bookingId})` → same pattern: `"Booking with R-714794 has been cancelled successfully."`
  - `startTime`/`endTime` format matches `getResourceAvailability`'s slot format exactly (e.g. `"6:00 AM"`) — pass the slot values straight through.
  - `bookingPurpose`/`coAttendee` were empty strings in the captured sample — optional fields, unconfirmed if server-required or truly optional (treat as optional, send `""` if not provided).
- **`userId` gotcha**: the Apex methods above want a Salesforce **User** Id (`005…` prefix, e.g. `0055i00000CwfKqAAJ`). `PortalAllFeedsCtrl.getCurrentUserContactId` looks like the obvious fetch-it-yourself call but returns a **Contact** Id (`003…`) instead — not interchangeable. Working fallback: `AiAssistantFlameCommunityWrapper.getAssistantContext()` (cacheable, no params) returns a `threadContext` string containing `"The Salesforce user record Id is 005…"` — regex it out. Implemented as `resolveUserId()` in the userscript.
- Verified real aura request shape directly against the HAR (not just the parser's `.md` summary): param names are exactly `message`, `aura.context`, `aura.pageURI`, `aura.token`; `callingDescriptor` is literally `"UNKNOWN"`; no-arg methods **omit the `params` key entirely** (not `params: {}}` — sending `{}` risks an Apex deserialization error); response body is plain JSON, no `while(1);`/`/*ERROR*/` prefix to strip.
- `callAura()` written — **does not scrape `window.Aura.appBootstrap`** (handoff's original plan) — instead hooks `fetch`/`XHR.send` and sniffs the page's own outgoing aura requests for `aura.context`/`aura.token` (and opportunistically `userId`), since the page fires several on load anyway. Self-heals on token rotation without needing bootstrap-script parsing.
- Chrome-hiding: `.siteforceLayoutDiv` (handoff's original guess) **does not appear in the HAR HTML** — confirmed the page is a pure Aura SPA shell (`<body class="null loading">`, no app markup until client-side render). Switched strategy to hiding by structural position instead: `body > *:not(#flame-reskin-root) { display: none }`. Robust to whatever internal class names Aura actually uses.
- **`portal-reskin.user.js` written** (project root) — `callAura()` helper, chrome-hiding + MutationObserver (re-appends root if Aura wipes `<body>`), and a minimal 4-tab shell (Calendar / My Bookings / Book Slot / Resources) wired to the read-only descriptors above. Booking submit tab shows availability only, has a note that submit isn't wired.
- **Smoke-tested in a real browser** — 2026-09-01, user ran it live via Tampermonkey. Found and fixed one real bug: `callAura()` wasn't unwrapping the Aura response envelope (`action.returnValue.returnValue`, not `action.returnValue`) — was silently breaking every tab. Fixed and confirmed working.

## Design pass (2026-09-01, same day) — full visual + IA rework

First pass (brass/brown accent, timeline-list calendar, 4 tabs incl. Resources) didn't land — user feedback: ugly color, "calendar tab doesn't even look like a calendar", wanted a real dashboard home page. Rebuilt:

- **Palette**: blue/indigo accent (`--accent: #5b8cff`) replacing brass. Cooler neutral grays.
- **New Home tab** (now the default landing tab, not Calendar): today's classes (compact rows), a "Coming up" column (next 6 days + class count, click → jumps into Calendar's day view on that date via shared `calState`), upcoming bookings (only rendered when any exist — no empty state clutter here).
- **Calendar rebuilt as a real grid** — day/week toggle, prev/next/today nav (no native date picker anywhere in the app now — `buildDayNav()` is shared between Calendar and Book Slot). Hour range trimmed to 9 AM–7 PM (user's actual class hours), 78px/hour rows. Event blocks show **venue → title → professor**, in that order — time-of-day text was dropped from the block entirely since it's redundant with the block's vertical position in the grid; venue is intentionally never truncated (title clamps to 2 lines instead if space is tight). Overlapping events get side-by-side lanes (`assignLanes()`, greedy interval packing).
- **Book Slot redesigned**: facility rail (vertical list, not chips) + resource dropdown + day-nav on the left, results fill the right column. Availability **auto-loads** on any selection change — the old manual "Check availability" button is gone.
- **Resources tab removed entirely** — user said they'd never use it. Its browse-only data (`CustomBookingController.getResources`) is still used internally to populate Book Slot's facility rail, just no longer has its own tab.
- **Stock-UI toggle** — small pill button, fixed bottom-left, *outside* `#flame-reskin-root` (so it survives the reskin being toggled off). Persists via `localStorage['flame-reskin-enabled']`. Clicking it adds/removes a `body.flame-reskin-off` class that CSS keys off of. **Found one real bug writing this**: the "off" override rule initially had lower CSS specificity than the base hide rule (fewer `:not(#id)` exclusions), so clicking the toggle updated its own label but never actually revealed the original page — verified this by scripting an actual click in the QA harness, not just eyeballing the markup. Fixed by matching the exclusion count.

### QA method (no live portal access in this environment)
Built `preview.html` (repo root) — loads the real `portal-reskin.user.js` unmodified, feeds it real captured data (from `aura_map.json`) via a `window.__FLAME_RESKIN_PREVIEW__` stub that `callAura()` short-circuits to when present (inert on the real site). Screenshotted with system `chromium --headless=new` (Playwright's own browser download was blocked in this environment). `?tab=<id>` picks the boot tab, `?auto=<scenario>` runs scripted clicks for interactive states (slot selection, cancel-confirm, toggle-off) since the CLI screenshot tool can't click anything itself — see the `<script>` blocks at the bottom of `preview.html`. Screenshots live in `.review/` (gitignore-worthy, not committed).

**Caught three real bugs this way, not just style nits**: `dateInput.valueAsDate` reads/writes in UTC, silently showing yesterday's date near midnight IST; `el.hidden = true` was defeated by the element's own `display:flex` class rule (author CSS beats the UA `[hidden]` rule at equal specificity); and a genuine stale-response race if the user switches tabs before the previous tab's data finishes loading (fixed with a `activeToken` guard in `switchTab()`).

## Files in project folder
- `parse_aura_har.py` — HAR → descriptor map parser. **Merges into existing `aura_map.json` by default now** (was: silently overwrote it — lost the first 24-call capture when the book/cancel HAR was parsed under the same output prefix, had to rebuild by re-parsing both HARs). Pass `--fresh` to force a clean overwrite instead.
- `aura_map.json` / `aura_map.md` — parsed output (29 calls mapped)
- `portal-reskin.user.js` — the userscript
- `preview.html` — local QA harness, see above. Not meant to be shipped/installed, just for screenshotting without a live portal session.
- `PRODUCT.md` — Impeccable design-skill product record for this project
- `.patch-tools/` — Android APK patch toolchain + in-progress artifacts (venv, build-tools, APKEditor.jar, hook.js, gadget-config.json, probe.js, pulled/merged/patched APKs, phone screenshots). See "Android APK patch attempt" above before touching anything in here.
- the HAR files
- this `HANDOFF.md`

### Follow-up polish (same session)
- Calendar event blocks: dropped time text from blocks (redundant with grid position per user), lead with venue (never clipped), added professor as a 3rd line when space allows.
- Calendar hour range trimmed 9 AM–7 PM (was 7 AM–8 PM) with taller 78px/hour rows — matches user's actual class hours, less scrolling.
- `.fr-content` widened (`max-width: 1320px`, was 1040) and centered (`display:flex; justify-content:center`) instead of hugging the sidebar — reduces wrapping in week view, especially professor names.

### Home tab redesign: week-strip date picker replaces "Coming up" column
User feedback: the vertical "Coming up" list of next-6-days was "kinda stupid". Replaced with a horizontal week-strip date picker (7 clickable date cells, prev/next-week paging) sitting *above* the classes list — clicking a day filters the classes list **inline on Home itself**, no tab jump, no refetch (pure client-side filter over the already-fetched `events` array).

- New shared component `buildDateStrip({ weekStart, selected, hasEvents, onSelect, onPrevWeek, onNextWeek })`, module-scope (near `buildDayNav`) — used by both Home and Calendar's mobile day view (see below). Deliberately has **no "Today" button**: today's cell gets a distinct tinted/outlined treatment (`.is-today`, separate from `.is-selected`'s solid fill) so it's always identifiable without one, and re-opening the Home tab already resets to today/this-week.
- Home's own state: `homeState = { weekStart, selected }`, reset to today/this-week on every fresh visit to the tab.

### Calendar tab: mobile-specific header restructure + date-strip reuse
User feedback: on mobile, wanted the Day/Week toggle moved to the same row as the "Calendar" title, and the day-mode nav swapped for the same week-strip Home uses (day-nav's simple prev/next/label doesn't need to keep its own row when the strip does more).

- New `.fr-cal-header` CSS grid (`grid-template-areas`) holding title/nav/toggle — **desktop**: title alone on row 1, nav+toggle on row 2 (unchanged from before); **mobile** (`max-width:760px`): areas swap to `"title toggle" / "nav nav"` — pure CSS reposition, same DOM, no JS branching needed for *that* part.
- What *does* need JS branching: mobile + day mode swaps `buildDayNav(...)` for `buildDateStrip(...)` (week mode keeps the simple nav on both breakpoints — the strip doesn't fit a "you're viewing a whole week" context). Detected via `window.matchMedia('(max-width: 760px)').matches` at render time. Page title also goes dynamic (`dayLabel(calState.date)` instead of static `"Calendar"`) in that specific case, matching Home's convention.
- Added a `resize` listener (debounced, 200ms) that re-renders the Calendar tab if it's currently active — rotating the phone across the 760px breakpoint needs a real re-render (different DOM structure), not just CSS reflow.

**Bugs found and fixed in this pass** (all via headless-Chromium screenshot QA against `preview.html`, not live device — see QA method above):
- **Hour-label top-half clipping**: an earlier fix for "9 AM overlaps the header" (added blanket `overflow:hidden` to every hour label) clipped the top half of *every* label, not just the first one — made the whole grid look ~30min offset. Real fix: only the *first* hour label (the one adjacent to the header, no row above it to safely straddle into) needs the clip; added a `.fr-cal-hour-label--first` modifier class instead of a blanket rule.
- **Day-mode "9 AM" still misaligned with its gridline after that fix**: root cause was different from the above — the gutter *always* rendered its 40px `.fr-cal-gutter-header` spacer (needed in week mode to align with each day column's `.fr-cal-day-header`), but day mode never renders a day-header on its single column at all, so the gutter's hours sat 40px lower than the actual grid with nothing to align to. Fixed by only rendering the gutter spacer `if (calState.mode === 'week')`.
- **Mobile: sticky hour-gutter (`position: sticky; left: 0`, added for week-view horizontal scroll) painted over the fixed bottom nav bar**: both had `z-index: 1`, and the gutter came later in DOM/paint order at that tie. Fixed by bumping `.fr-nav`'s mobile `z-index` to `5`.
- **Mobile week view was squeezing all 7 day-columns into the viewport width** instead of scrolling — user explicitly asked for horizontal scroll instead. Fixed: `.fr-cal-days` gets a `min-width: ${days.length * 112}px` floor in week mode only (day mode's single column still fills available width naturally), `.fr-cal-wrap` gets `overflow-x: auto` on mobile, gutter goes `position: sticky; left: 0` so hour labels stay visible while day columns scroll under them.

## Android patch — session 2026-09-01 (afternoon): live attach fully confirmed working, autostart still unresolved

Resumed exactly where the previous session parked (`.patch-tools/probe.js`). Findings, in order:

- **`evaluateJavascript` fully works on this device/app** — visually confirmed (probe set `document.body.style.background='red'`, screenshotted red bars at the page edges through the actual post-login portal WebView). Not blocked by CSP, sandboxing, or the Frida Java bridge.
- **Root cause of probe's first "silent failure"**: `Java.choose('android.webkit.WebView', ...)` found zero instances because the app shows a **native React Native login screen first** (Google/MyFLAME-credentials buttons) — the WebView doesn't exist until *after* sign-in navigates to the portal. Not a bug, just wrong timing in the probe. Fixed by polling `Java.choose` every 2s in a loop instead of a single-shot call.
- **Second real bug found via probe**: calling `evaluateJavascript` from Frida's own attach thread throws `"A WebView method was called on thread 'Thread-7'... must be called on the same thread"` — Android WebView is main-thread-affine. Fixed in the probe with `Java.scheduleOnMainThread(fn)`. **Does not affect `hook.js`'s production hook** — `onPageFinished` is always delivered on the main thread by the Android framework already, confirmed by the next result below.
- **`hook.js`'s actual hook logic is correct and works end-to-end under live attach.** Force-stopped the app, relaunched, and raced a live `frida -U -n MyFLAME -l hook.js` attach against it (scripted back-to-back in one shell command to remove human latency). Full log sequence observed:
  ```
  [flame-inject] hook installed, waiting for WebView...
  [flame-inject] setWebViewClient called: com.reactnativecommunity.webview.RNCWebViewClient
  [flame-inject] hooked com.reactnativecommunity.webview.RNCWebViewClient.onPageFinished
  [flame-inject] onPageFinished https://my.flame.edu.in/secur/frontdoor.jsp?sid=...&retURL=%2Fs%2F
  [flame-inject] injecting reskin into https://my.flame.edu.in/secur/frontdoor.jsp?...
  [flame-inject] onPageFinished https://my.flame.edu.in/s/
  [flame-inject] injecting reskin into https://my.flame.edu.in/s/
  ```
  Screenshotted the device immediately after: **the full custom reskin UI rendered** (Home tab, week-strip date picker, classes list, upcoming bookings, "Stock UI" toggle pill, bottom nav) — inside the real app, live. This is the first confirmed end-to-end success of the whole Android patch project.
  - Note: `onPageFinished` fires **twice** — once for the SSO `frontdoor.jsp` redirect page, once for the real `/s/` portal — because the URL substring check (`url.indexOf('my.flame.edu.in') !== -1`) matches both. Harmless (frontdoor.jsp has no real DOM to break), but worth tightening to match `/s/` specifically if this ever causes a double-injection issue.
  - The real WebViewClient class is confirmed to be `com.reactnativecommunity.webview.RNCWebViewClient` (react-native-webview's actual class, as expected).
  - **User's theory this session that the native login screen breaks the hook is ruled out by this evidence** — the hook was installed *while the native login screen was still showing* and fired correctly minutes later, after real sign-in, for the real portal URL. `WebView.setWebViewClient.implementation` is a class-level intercept that waits indefinitely; it doesn't need a WebView instance to already exist.

- **Autostart mode (`gadget-config.json` type:"script", the actual no-computer-needed production goal) still does NOT show the reskin** — rebuilt fresh from the same `hook.js` that just worked under live attach, installed, force-stopped, relaunched cold with no Frida CLI attached at all. Screenshot showed stock Siteforce UI, unchanged.
  - `libfrida-gadget.so` loads cleanly every time (confirmed via `nativeloader:` logcat line, "ok").
  - Confirmed via `adb shell ls -l` on the app's real extracted lib dir (`/data/app/~~<hash>/com.myflame-<hash>/lib/arm64/`) that `libfrida-gadget.so`, `.config.so`, and `.script.so` are all present at full/correct size — **not a packaging/extraction problem**.
  - Patched `hook.js` to mirror every log line through `android.util.Log.d` (tag `FlameInject`) in addition to `console.log`, on the theory that autostart mode has no attached client to receive `console.log` (per prior session's note). Rebuilt, installed, relaunched, captured logcat filtered on `-s Frida:V FlameInject:V AndroidRuntime:E "*:S"` for 25s — **zero matching lines**, not even the very first "hook installed, waiting for WebView..." line, and no crash/exception either.
  - This is a stronger negative result than previously thought: not just "no visibility", genuinely **zero evidence the script's `Java.perform` callback ever runs** in autostart/script mode on this device.
  - **In progress, not concluded**: added a *second*, Java-bridge-independent instrumentation layer — calls `liblog.so`'s `__android_log_print` directly via `NativeFunction`, bypassing `Java.use('android.util.Log')` entirely (since if the Java bridge itself is the thing not ready, a Java-bridge-dependent logger would be silent for the same reason as the thing it's trying to diagnose). Two checkpoints added: **A** = first statement in the file, before `Java.perform` is even called; **B** = first statement inside the `Java.perform` callback. Logcat tag `FlameInjectNative`. Rebuild was in flight when this note was written — **resume by checking whether A and/or B appear in `adb logcat -s FlameInjectNative:V "*:S"` after a fresh cold launch**:
    - **A absent** → gadget's script-mode never runs the script at all (a Frida/gadget/device-level limitation, not a JS bug — stop iterating on hook.js content, look for a different autostart mechanism, e.g. rooting the device to run frida-server as a boot service instead of the embedded script-mode gadget).
    - **A present, B absent** → script file runs, but `Java.perform`'s callback never fires (Java VM/bridge never becomes ready in this execution context) — fix would be polling `Java.available` via `setTimeout` + `Java.performNow` instead of the blocking `Java.perform` at load time.
    - **Both present, but no later `setWebViewClient called` line even after signing in during the test** → hook installs but genuinely doesn't intercept in this mode — a real hook-path difference between script-mode and listen-mode execution, needs further investigation.
  - **Test protocol for next time**: must actually sign in on the device during the ~25s+ capture window (not just sit on the native login screen) since `setWebViewClient` legitimately cannot fire before a WebView exists — the two most recent autostart tests left the app on the login screen the whole time, so absence of the *later* hook lines proves nothing; only absence of checkpoint **A** is real signal on its own.

### Corrections to prior session's stated fixes (both discovered this session, both real)
- **The `JAVA_TOOL_OPTIONS=-Xmx6g` OOM fix documented in the previous session is not actually being applied.** apktool's own `/usr/bin/apktool` launcher script hardcodes `javaOpts="-Xmx256M"` directly on its `java` command line (confirmed by reading the script) — this explicit flag wins over the environment-variable-injected one (JVM resolves duplicate `-Xmx` by using whichever is later on the effective command line, and the hardcoded one comes after). Builds have nonetheless been succeeding at the default 256M heap on this app/machine — so the OOM risk described in the prior session may be less consistent than believed, or was specific to a heavier parallel (`-j` > 1) build. If a real OOM recurs, the actual working override is apktool's own `-J<opt>` passthrough convention, e.g. `apktool build ... -JXmx6g` (or find where objection shells out to `apktool` and pass this way) — `JAVA_TOOL_OPTIONS` alone does not work here.
- **`objection patchapk -l hook.js` refuses to run without `-c <gadget-config.json>`** — errors with `A script source was specified but no gadget configuration was set.` For a plain listen-mode diagnostic build (no embedded script, external `frida -U -n ... -l script.js` attach instead), omit `-l` entirely rather than trying to pair it with a listen-type config.
- **USB connection to the device dropped twice mid-session** (`lsusb` showed nothing, `adb devices` empty) for no clear reason — cable/port reseat fixed it both times in seconds. Not a code issue, just flag it if a background command suddenly reports "no devices/emulators found" or a live Frida session says "Device lost" — check the physical connection before assuming a build/tooling problem.
- **MIUI/HyperOS shows an on-phone "Install anyway?"-style confirmation dialog** for `adb install` sometimes (not every time) even with USB debugging + "install via USB" already enabled once before — if `adb install` returns `INSTALL_FAILED_USER_RESTRICTED: Install canceled by user`, check the phone screen and tap through the prompt, then retry the same `adb install` command.

## Android patch — RESOLVED (2026-09-01, same afternoon session): autostart injection fully working

Closed out. Confirmed with a **cold launch, zero Frida CLI attached, zero PC involvement after install**: sign in → gadget auto-injects → full custom reskin UI renders (Home tab, week-strip date picker, classes list, upcoming bookings, "Stock UI" toggle pill, bottom nav). Logcat sequence on a clean cold launch:
```
[flame-inject] hook installed, waiting for WebView...
[flame-inject] setWebViewClient called: com.reactnativecommunity.webview.RNCWebViewClient
[flame-inject] hooked com.reactnativecommunity.webview.RNCWebViewClient.onPageFinished
[flame-inject] onPageFinished https://my.flame.edu.in/secur/frontdoor.jsp?...
[flame-inject] injecting reskin into https://my.flame.edu.in/secur/frontdoor.jsp?...
[flame-inject] onPageFinished https://my.flame.edu.in/s/
[flame-inject] injecting reskin into https://my.flame.edu.in/s/
```

### Root cause chain (each layer was real and had to be found in order)
1. Gadget `type:"script"` (autostart) mode's raw JS execution context has **no ambient `Java` global** and **no npm-style module resolution** on a bare, unbundled script file. The interactive `frida` CLI (used for every earlier "it works!" live-attach test) auto-bundles/polyfills `Java` for convenience — that convenience is exactly what was masking this for the entire first half of the session. A script handed directly to `objection patchapk -l <file>` gets none of that; it's just raw text dropped into the gadget's script slot.
2. Diagnosed via **`__android_log_print`-based logging that bypasses the Java bridge entirely** (`liblog.so`'s native export via `NativeFunction`, tag `FlameInjectNative`) — this was essential because the moment `console.log`/`android.util.Log` depends on `Java.*` being usable, a broken Java bridge silences your diagnostic along with the thing it's diagnosing. This is the key technique to reuse if any future gadget script-mode issue comes up: **always instrument with a Java-bridge-independent native log first.**
3. Fix attempt 1, `require('frida-java-bridge')` as a bare CJS call with no bundler: silently killed the entire script (zero log output at all, not even the native-log checkpoint before it) — bare `require()` has nothing to resolve against in the raw gadget engine.
4. Fix attempt 2, ran the script through **`frida-compile`** (a bundler for Frida agent scripts, conceptually the same role as webpack/esbuild but targeting Frida's JS engine) to properly inline `frida-java-bridge`. This got `Java` to resolve as a real object — but `typeof Java.perform` and `typeof Java.available` were still `undefined`. Cause: frida-compile's default bundle format is ESM, and `require()`'s CJS-interop shim doesn't correctly unwrap a bundled module's default export — you get back the wrapper, not the actual bridge API.
5. **Real fix**: use proper ES `import Java from 'frida-java-bridge';` syntax (not CJS `require()`) as the very first line of the script, matching frida-compile's native ESM bundling. Confirmed via logcat: `typeof Java.perform=function, typeof Java.available=boolean, Java.available=true` — then the whole hook fires correctly end to end.

### Exact working build command sequence (for any future rebuild of `hook.js`)
```bash
cd .patch-tools
source venv/bin/activate
export PATH="$PWD/build-tools/android-14:$PWD/venv/bin:$PATH"

# 1. hook.js must start with:  import Java from 'frida-java-bridge';
#    (NOT `require(...)` — CJS require's interop breaks the bundle's default export)

# 2. Bundle it. frida-compile and frida-java-bridge are REPO-ROOT deps, not
#    .patch-tools ones — declared in the root package.json (`dependencies`)
#    and installed at <repo>/node_modules. They have never lived in
#    .patch-tools/node_modules despite what this line used to say; earlier
#    sessions found them at /home/archer/node_modules instead (see the
#    2026-09-01 night entry). Run from .patch-tools with an explicit path:
#      ../node_modules/.bin/frida-compile hook.js -o hook.compiled.js -T none
#    .patch-tools/venv/bin/frida-compile also exists (it ships with the
#    python frida-tools) and is a separate program — the npm one is what
#    this pipeline is built on.
../node_modules/.bin/frida-compile hook.js -o hook.compiled.js -T none

# 3. Patch the merged (all 4 splits combined via APKEditor) apk with the COMPILED script, not raw hook.js
export JAVA_TOOL_OPTIONS="-Xmx6g"   # cosmetic only, see note below — real override is apktool's own -J passthrough if OOM ever actually recurs
objection patchapk -s flame-merged.apk -a arm64-v8a -c gadget-config.json -l hook.compiled.js -j 1

# 4. Install — do NOT uninstall first. objection reuses the same debug
#    keystore across builds, so `adb install -r` upgrades in place and
#    PRESERVES the logged-in session/cookies. `adb uninstall` wipes app data
#    and forces a full Google re-login — only do that if switching to a
#    differently-signed build (e.g. reinstalling real Play Store stock).
adb install -r flame-merged.objection.apk
```
`gadget-config.json` (autostart, no PC needed at launch): `{"interaction": {"type": "script", "path": "libfrida-gadget.script.so"}}`

Note on `JAVA_TOOL_OPTIONS="-Xmx6g"`: this genuinely works when set as an env var before calling `objection patchapk` (confirmed via its own "Picked up JAVA_TOOL_OPTIONS" log line) — objection's internal apktool invocation respects it. It's only when calling apktool *directly* (bypassing objection, e.g. for a manual manifest edit) that the env var is silently ignored and the `-J` prefix trick is needed instead (see below).

## Status bar + nav bar cosmetic fixes (2026-09-01, later same session)

The reskin's dark background didn't extend into the Android status bar (stayed white) or leave room above the system navigation bar (bottom tab bar got overlapped). Root cause and fix, in order of what was actually tried:

1. **`window.setStatusBarColor()` is a documented no-op** on this build — confirmed via `adb shell dumpsys package com.myflame | grep targetSdk` → `targetSdk=36`. Android mandates edge-to-edge display (no opt-out) for apps targeting SDK 35+, and ignoring `setStatusBarColor`/`setNavigationBarColor` is a listed, intentional consequence — not a bug in the hook, not something Frida broke.
2. **Tried downgrading `targetSdkVersion`** (36 → 34, in `apktool.yml`'s `sdkInfo` block after a manual `apktool d`/edit/`apktool b` round-trip, then fed through the normal `objection patchapk` pipeline) to disable the edge-to-edge mandate and make the deprecated color API work again. **This made things worse** — produced a double-height white status bar strip, likely because the app's own native code already explicitly opts into edge-to-edge independent of `targetSdkVersion`, and downgrading the platform target while that native call still fires created an inconsistent state. **Reverted immediately** (`adb install -r` back to the saved-good APK) — do not repeat this approach.
3. **Real fix**: instead of fighting the deprecated color-setter API at all, paint a real opaque `android.view.View` directly into the `DecorView`, anchored to the top with height read from `WindowInsets.getInsets(WindowInsetsType.statusBars()).top` (with a `status_bar_height` resource-id fallback if that's 0). This isn't a system color API, so it isn't subject to the edge-to-edge deprecation — same technique modern edge-to-edge-aware apps use themselves. Implemented as `paintStatusBarOverlay()` in `hook.js`, called from the existing `onPageFinished` hook. Dedupes via `decorViewGroup.findViewWithTag(...)` so repeat `onPageFinished` calls (frontdoor.jsp + `/s/`) don't stack duplicate views. Kept `insetsController.setSystemBarsAppearance(0, 8)` for icon color (light icons on dark bg) since that API genuinely still works and isn't deprecated.
4. **Real Frida gotcha hit along the way**: a hook's method argument (here, the WebView `view` passed into `onPageFinished`) is only a valid wrapper for the *synchronous* duration of that hook call. Using it later inside `Java.scheduleOnMainThread()`'s async callback throws `Error: Wrapper is disposed; perhaps it was borrowed from a hook instead of calling Java.retain()...`. Fix: `var retainedView = Java.retain(view);` synchronously, before scheduling the async callback, then use `retainedView` inside it. (`recolorStatusBar`'s Activity-resolution path never hit this because `Java.cast()` produces an independently-referenced wrapper, not a borrowed one — only the raw hook-argument `view` itself needed retaining.)
5. **Bottom nav bar overlap** (system nav bar covering the reskin's own `.fr-nav` tab bar): same root cause (edge-to-edge, content extends under system bars) but a different fix, since this one wasn't a native color deprecation problem — the reskin needs actual *layout* padding equal to the nav bar's height, not a native color/view fix. `env(safe-area-inset-bottom)` isn't reliably populated by this WebView, so `hook.js`'s `pushNavBarInset()` measures the real inset natively (`WindowInsets.getInsets(WindowInsetsType.navigationBars()).bottom`, resource-id fallback `navigation_bar_height`) and pushes it into the page via `evaluateJavascript` as a CSS custom property (`--flame-navbar-inset`, converted from device px to CSS px via `window.devicePixelRatio`). `portal-reskin.user.js` defines that variable with an `env(safe-area-inset-bottom, 0px)` default (harmless 0 on desktop Tampermonkey, where this whole problem doesn't exist) and uses it in `.fr-nav`'s bottom padding + height and `.fr-content`'s bottom padding, all inside the existing `@media (max-width: 760px)` mobile block.
6. Also **halved `.fr-content`'s top padding** (24px → 12px, mobile only) per user feedback once the status bar was properly dark — the old value was tuned for a white status bar with no visual continuity; once the status bar matches the page background, less breathing room reads better.

**`apktool -J` flag syntax** (only needed when calling apktool directly, not through objection): the flag must come *immediately after* `apktool` and *before* the subcommand — `apktool -JXmx6g b <dir> -o <out>`, not `apktool b <dir> -o <out> -JXmx6g`. The wrapper script only strips consecutive `-J`-prefixed args from the very front of the argument list.

**`apktool build`'s incremental-change detection can silently ignore edits** — editing `apktool.yml` alone (without touching any file under `res/` or the manifest XML text itself) may print `AndroidManifest.xml and resources have not changed` and reuse a stale cached build artifact. Always pass `-f`/`--force` to `apktool b` after any manifest/yml edit, then verify with `aapt dump badging <apk> | grep targetSdkVersion` before trusting the output.

### Loose ends, not urgent
- `onPageFinished` still fires twice (frontdoor.jsp redirect, then the real `/s/` page) since the URL match is a substring check — harmless (frontdoor.jsp has no real DOM), but could tighten to match `/s/` specifically if it ever causes visible double-injection flicker. `pushNavBarInset` also has no dedup guard (unlike the status bar overlay) — harmless since re-setting the same CSS variable value twice has no visible effect, but could add one for cleanliness.
- The current on-device install is this session's self-signed patched build (`installerPackageName=null`) — won't receive Play Store updates while installed this way. Not urgent; reinstalling stock from Play Store is a one-command revert (`adb uninstall com.myflame` then reinstall from Play Store) whenever a real app update is needed — just re-run the whole patch pipeline against the new APK afterward.
- Login-page reskin (item 1 in the original Next Up list below) is unrelated to any of this and still untouched.
- Scheduled slot auto-booker (item 4 below) can now actually be reconsidered as a next step, since the blocking dependency ("does the Android patch pipeline actually work") is resolved — still requires real native Android background scheduling work (`AlarmManager` + headless WebView), not started.

## Git tracking started (2026-09-01, evening)

Repo initialized in the project folder. `.gitignore` excludes `*.har` (live session tokens, huge), `*.apk`, `.patch-tools/` (1GB+ toolchain, regenerable), `.review/` (QA screenshots), and `.claude/settings.local.json`. Everything else (script, HANDOFF, PRODUCT, aura_map, preview harness, parser) is tracked.

## Small UX fixes (2026-09-01, evening) — mobile calendar, booking display, error handling

- **Mobile week calendar**: day-column min-width raised 112px → 150px so real venue names (e.g. "APJ Abdul Kalam 102") sit on one line instead of wrapping — the view already scrolls horizontally on mobile, so there's no cost to the extra width. Also removed `.fr-cal-event`'s own `overflow: hidden` (title/meta already self-clip via their own rules) so an exceptionally long venue name that still wraps grows the block instead of losing text.
- **Booking display simplified** (`formatBookingWhen()`, `cleanResourceName()` in `portal-reskin.user.js`): My Bookings and Home's "Upcoming bookings" were showing `${startDateTime} – ${endDateTime}` as two full raw `"dd/mm/yyyy, h:mm AM"` strings — since a booking is always one day, the date was stated twice for no reason. Now shows `dayLabel · start – end` (e.g. "Today · 8:00 AM – 9:00 AM"). Also stripped `resourceName`'s trailing parenthetical (`"Gym ( 6:00 am to 2:00 pm slot )"` → `"Gym"`) — that suffix is the facility's whole operating window, not the actual reservation, and is redundant with the time now shown clearly below it.
- **Book Slot confirm panel**: Purpose/Co-attendee inputs now only render for room-type facilities (`facility_Name !== 'Sports Facilities'`) — a gym/pool slot has nothing meaningful to fill in either field for.
- **Placeholder text contrast bug**: `.fr-input` had no `::placeholder` rule at all, so placeholders fell back to the browser's default (near-black) color on the dark input background — effectively invisible. Added `color: var(--text-secondary)`.
- **Booking refusal showed as success (real bug, found via a new HAR capture, `my.flame.edu.in_Archive booking failed more than 1 not allowed.har`, merged into `aura_map.json`)**: `CustomBookingController.createReservation` returns Aura `state: SUCCESS` with a plain message string *even when the booking is refused* — e.g. `"More than 1 bookings are not allowed for selected resource."` — no thrown exception, so the old code's try/catch never caught it and any returned string was rendered in the green `.fr-success-panel`. Fixed by checking the result for a real booking id (`/\bR-\d+\b/` — accepted bookings always name one, e.g. "...Booking Id R-714794..."); anything else renders in a new red/orange `.fr-error-panel` (`--danger`/`--danger-bg` tokens, already existed) and re-enables the submit button (previously stuck on "Booking…" forever for a refusal, since the old code assumed the awaited call succeeding meant the booking succeeded).
- **Full-tab reload on every nav click, fixed**: Calendar's day/week toggle and date nav, and Book Slot's date nav, all called `switchTab(id)` on every click — full `renderLoading()` spinner wiping the whole tab, plus a fresh `getAllScheduledEvents`/`getResources` network round-trip, just to change which date/mode is shown. Added `eventsCache` (module-level, mirrors the existing `bookingsCache` pattern) and cached `bookState.facilities`; Calendar's nav handlers now call a local `rerender()` that re-runs `renderCalendar(token)` directly against the cached data (no spinner, no refetch); Book Slot's day nav now calls a local `renderDayNav()` + `refreshAvailability()` instead of `switchTab('book-slot')` — only `resultsWrap` (which already had its own inline spinner) re-fetches, the facility rail/resource picker never re-render. Home's date-strip was already doing this correctly (pure client-side filter, no refetch) — it was the reference pattern, not part of the bug.
- Verified all of the above via `preview.html` + headless Chromium screenshots (`.review/`, not committed) — added reusable scenarios `book-confirm-room` (Conference Rooms, to check the fields+placeholder), `book-confirm-fail` (stubs the refusal string), `cal-next`, `book-slot-next` (date-nav re-render, no full reload) alongside the existing ones.

### Discussed, not built: caching + skeleton loaders
User asked about a "proper" cache system and skeleton loaders, prompted by the stock portal's own poor caching. What's now in place (this session) already covers the actual complaint — in-memory `eventsCache`/`bookingsCache`/`bookState.facilities`, populated once per page load, invalidated only when a mutation (booking/cancel) actually changes that data. Not yet done, worth considering later if it comes up again:
- **Cross-reload persistence** (`localStorage`/`sessionStorage`, e.g. 5–10 min TTL) so a hard refresh doesn't refetch — currently every full page reload starts cold, which is correct-but-slow, not broken.
- **Skeleton loaders** in place of the current full-tab spinner for the *first* load of a tab (the repeat-nav spinner is now gone per the fix above, so this is purely about first paint) — shaped placeholder rows instead of a centered spinner, standard perceived-performance polish. Not started; flagged as a possible future `$impeccable` pass over `renderLoading()`/`RENDERERS`, not attempted this session.

## Gyan chat tab (2026-09-01, evening)

New "Gyan" nav tab in `portal-reskin.user.js` (sparkle icon) — talks to the portal's own AI assistant. Mapped from a captured "book a gym slot via chat" + "what's for dinner" session (`my.flame.edu.in_Archive talk to gyan open close chat.har`, merged into `aura_map.json`), and confirmed the backend calls are real (Gyan's own `createSportsFacilityReservation` hit the identical "More than 1 bookings are not allowed" refusal our own booking code found — same underlying booking logic, different entry point).

**Protocol** (all via the same `callAura()`, but Gyan's calls use `namespace: "vnai"` instead of the usual `""` — `callAura()` now takes an optional 5th `namespace` param for this):
- Setup (once, cached in `gyanState`): `AiAssistantWindowController.getAssistant({assistantName:"Gyan"})` → `assistantId` + welcome/intro text; `getUserThread({assistantId, actorId:userId, createIfNotExists:true})` → `threadId`. Reuses `AiAssistantFlameCommunityWrapper.getAssistantContext()` for `threadContext`/`additionalKnowledgeFileIds` — factored that call out into a shared `getAssistantContext()` helper (was previously inlined only inside `resolveUserId()`, which only used the userId half of it) so both consumers share one cached fetch.
- Per message: `runModeration({message})` (fire-and-forget gate — never observed a rejection shape in the capture, so failures are swallowed rather than blocking the message) → `runAssistant({runRequest:{assistantId, actorId, message, threadId, threadContext, additionalKnowledgeFileIds}})`.
- **The actual mechanism, in `runGyanTurn()`**: this is a tool-calling relay loop, not one round-trip. The server executes its own tools server-side (file search for menu/faculty questions, the real booking backend for reservations) and hands back the already-computed result in `requiredActions[].functionResponse` — the client's only job is to echo each one straight back as a `toolResponses` entry (`{name, arguments, callId, response}`) in the next `runAssistant` call to unblock the next model turn, repeating until a response carries real `text`. One nuance confirmed against the capture: a `transfer_to_*` action (agent handoff, e.g. general → student-files sub-agent) also carries a `userQuery` in its `functionArgs`, which becomes that *next* call's top-level `message` instead of the usual empty string — a real "book me a gym slot" query bounced through two such transfers before any tool actually ran. Loop is capped at 8 steps so a malformed/looping response can't hang the chat forever.
- Not wired up: `deleteThread` (mapped, no "clear chat" UI built — chat history is just an in-memory `gyanState.messages` array, cleared on a real page reload same as everything else session-scoped).

**UI**: standard chat layout (`.fr-gyan-page`, flex column, `height:100%` inside `.fr-content` — this tab is the first to actually fill that space edge-to-edge rather than just scroll within it) — message bubbles (user right/accent, assistant left/elevated), composer pinned at the bottom, "*Gyan* is thinking…" bubble + disabled composer while a reply is in flight. State updates (pushing the reply, clearing the sending flag) happen unconditionally in `runGyanTurn()`'s caller regardless of which tab is active — only the DOM repaint is gated on `token === activeToken` — so switching away mid-reply and back later shows the finished exchange instead of getting stuck showing "thinking" forever (caught this specifically while reviewing the code, not from a screenshot — worth remembering as a pattern for any other tab that does a slow multi-step async action in the future).

One real mobile bug found via screenshot QA: Gyan's composer is the first tab content to actually reach the bottom edge of `.fr-content`'s padded box by design (every other tab has slack scroll space below its content, so nothing else has visibly collided with it before) — on mobile it visibly overlapped the "Stock UI" toggle pill (fixed `bottom:80px` on mobile). Patched at the time with a Gyan-specific `margin-bottom`; **superseded** by the shell rework below, which removes the floating pill (and the patch) entirely rather than special-casing this one tab.

QA: `preview.html` stubs added for `AiAssistantWindowController.getAssistant/getUserThread/runModeration/deleteThread/runAssistant` (the last one is a small stateful function exercising both the zero-hop and one-hop-relay paths), plus `auto=gyan-chat` scenario. Verified via headless-Chromium screenshots (empty state, a 2-message conversation showing both relay paths worked, mobile layout) — not against the live portal (no access in this environment, same limitation as the rest of the project).

## Gyan layout + a real crash, both fixed (2026-09-01, evening, same session)

Two follow-up reports from live use, both real:

- **Layout** (via `$impeccable layout`): a short exchange sat flush against the header with a dead gap down to the composer — opposite of every chat convention (WhatsApp/iMessage/ChatGPT all anchor the live edge of a conversation to the input). Fixed: `.fr-gyan-messages` is a flex column, `.fr-gyan-list` gets `margin-top: auto` (hugs the bottom when short, steps aside once content overflows so long-conversation scrolling still works), the empty/welcome state gets its own `margin: auto 0` centering instead — different kind of content (an explanation, not a conversation), different treatment.
- **Crash, root-caused from a live HAR the user captured**: `aura call failed (AiAssistantWindowController.runAssistant): [{"message":"List index out of bounds: 0"}]`. The failing request's own params showed `"threadId":null`. Root cause: `runGyanTurn()` never re-validated `gyanState` before using it — only `ensureGyanReady()` (called once, on tab mount) populated `threadId`, and the same-session `resetGyanThread()` (added to auto-recover from a *different*, now-disproven "stale thread" theory) nulls it on error. The very next send in the same tab visit went out with a null thread. Fixed by calling `ensureGyanReady()` at the top of `runGyanTurn()` itself (already a no-op once ready, so free) instead of trusting the caller. Kept the auto-reset-on-error + manual "New chat" button from the previous fix too — both still correct/useful, just weren't sufficient alone.

## Shell rework: sidebar → top bar, Stock-UI toggle redesigned twice (2026-09-01, evening, same session)

Two rounds — the first ("v1") shipped, then the user pushed back hard on one part of it and it was revised ("v2") in the same session. Documenting both because the reasoning for *why v1's mobile approach was wrong* is worth keeping.

**Both rounds, unchanged**: desktop's left sidebar → horizontal top bar. The old "FL" initials-square + "Portal" name stacked top-left of a vertical sidebar reads exactly like every app's account switcher; the same brand mark inline in a horizontal bar just reads as a logo. Replaced the initials square with an authored flame-glyph icon (`ICONS.flame`) + "FLAME" wordmark, tabs go horizontal with icon+label, desktop bar padding matched to `.fr-content`'s own `48px` left/right so the bar's edges line up with the page content below it (was `24px`, visibly narrower — caught on user review). Mobile keeps the existing bottom tab bar — same `.fr-nav` markup, repositioned by the media query, unaffected by any of this.

**v1 (shipped, then reverted)**: the floating "Stock UI" pill had collided with Gyan's composer (see above). Diagnosed it as systemic — any current or future tab's content could eventually reach that corner — and split into an in-shell desktop top-bar button plus a **new fixed mobile top strip** (`.fr-mobile-topbar`, 52px, reserved via `.fr-content`'s top padding, holding just the toggle). Reasoning at the time: reserved space is structurally safer than a floating overlay, mirroring how the bottom nav bar already reserves its own space.

**User's reaction, verbatim: "thats disgusting bro."** The structural argument was correct but missed the actual cost: permanent chrome on *every single mobile tab*, forever, just to host one rarely-used button — worse than the collision it fixed. **v2**: deleted `.fr-mobile-topbar` entirely, reverted `.fr-content`'s mobile top padding back to `12px`. Replaced with `attachHomeLongPress()` — a 550ms long-press on the Home tab (mobile-only, checked via `matchMedia('(max-width:760px)')` at fire time so it's inert on desktop where the explicit button is the real control) triggers the switch: a slow `background-color` fill for hold feedback (`.fr-nav-btn.is-pressing`, transition duration matches the hold delay so releasing early visibly un-fills), `navigator.vibrate(15)` if available, and the resulting tap-navigation suppressed (`stopImmediatePropagation` in a capture-phase click listener) so it doesn't also flash to the Home tab in the instant before root disappears. Net result: **zero permanent chrome on mobile** — the switching mechanism has no visual footprint instead of needing a smaller one. Long-press-for-secondary-action is an established mobile convention (app icons, list rows), not an invented gesture.

The off-state fallback pill (`#flame-reskin-toggle`, outside `#flame-reskin-root`) is unchanged by either round: still the only way to switch back *on* from the stock page (which has none of our DOM), now CSS-gated to `display:none` except under `body.flame-reskin-off` — since it only shows once root is already hidden entirely, it structurally can't collide with anything of ours regardless of viewport or which tab was last open, unlike the pre-rework version which was always in the DOM and always positioned.

Verified via `preview.html` screenshots each round — desktop top bar (multiple tabs), both toggle mechanisms (desktop click, mobile long-press — the latter via a real `dispatchEvent(new PointerEvent('pointerdown'))` and letting Chromium's `--virtual-time-budget` carry the 550ms timer), mobile with the v1 strip and then confirming it's fully gone in v2.

## Custom Resource picker (2026-09-01, evening, same session)

User: "the webview picker is really ugly" — Book Slot's Resource field was a native `<select>`, which on Android WebView renders as the platform's own full-screen wheel picker with zero reskin styling (a jarring drop out of the custom UI). Built `buildPicker({ariaLabel, onChange})`: a styled button + `role="listbox"` panel (open/close state, click-outside-to-close, Escape to close, `aria-expanded`/`aria-selected`) matching the rest of the design system (`.fr-picker*` CSS, reuses `--bg-elevated-2`/`--accent`/`--border` tokens). Exposes just `.value` (get/set) and `.setOptions(list)` rather than trying to imitate `<select>`'s full DOM API, since Book Slot only ever used that much of it. Rewired all five `resourceSelect` call sites (`currentResourceName()`, `refreshAvailability()`'s `resourceId`, `createReservation()`'s `resource`, and the two options-population sites — initial + on facility-category change) to the new `resourcePicker` object; dropped the manual `change` listener since `buildPicker`'s `onChange` callback covers it.

New `chevronDown` icon (rotates 180° via CSS when the panel is open). Verified via `preview.html` screenshots (desktop + mobile, panel open, selected item highlighted).

## Gyan "List index out of bounds: 0" — RESOLVED for real (2026-09-01, evening, same session)

The "Gyan layout + a real crash, both fixed" entry above documents two earlier attempted fixes that turned out to be wrong or insufficient. Both are now superseded — kept here for the debugging trail, not as current guidance:
1. First attempt: re-run `ensureGyanReady()` on every turn (still correct and kept, just wasn't the whole fix).
2. Second attempt: replicate the reference capture's two-call `getUserThread` sequence (`assistantId:""` then the real one), theorizing the first call primes something server-side. **Wrong** — confirmed via a second live HAR that this alone still comes back `{}`.

At that point `resetGyanThread()` was calling `deleteThread` automatically on every send error — with no confirmed-working recreate path, this was flagged as likely having permanently destroyed the user's one real thread. Removed the auto-delete (correct call, still true), but the underlying question — "does creation actually work at all via this API?" — was still open.

**Root cause, settled by a HAR of the actual "Start chat" click** (`my.flame.edu.in_Archive gyan start talk end.har` — opening Gyan, clicking Start chat, sending a message, receiving a reply, closing via the cross + "End chat" confirm): `getUserThread`'s `createIfNotExists` flag is flatly misleading — it **never creates anything**, only looks up a thread that already exists (`{}` otherwise, flag or no flag). Real creation is a **separate method**, `createNewThread({assistantId, assistantName, actorId})` — namespace `vnai` like everything else here — whose response is the **new threadId as a bare string** (not `{threadId: ...}` like `getUserThread`'s response shape). This is the exact sequence the stock UI's "Start chat" button fires: lookup (empty) → `createNewThread` → use the new id. The same HAR's "End chat" confirm also fires a real `deleteThread` — confirming that call is genuinely destructive, which is why the auto-delete removal a few commits back was the right instinct even before this was fully understood.

Fixed properly in `acquireGyanThread(userId)`: look up first, call `createNewThread` only if the lookup genuinely comes back empty. This is what `ensureGyanReady()` now calls instead of the old two-call `getUserThread` guesswork. With a confirmed-working recreate path, two things that were made deliberately conservative/local-only in the previous entry are now safe again:
- **"New chat"** really calls `deleteThread` again, trusting `acquireGyanThread`'s `createNewThread` fallback to give the next turn a fresh thread.
- The composer's error handler does a soft reset (`gyanState.ready = false`, forces re-validation next attempt) — safe because the lookup-first `acquireGyanThread` can't delete anything on its own. Still no *automatic* `deleteThread` on a generic error — that stays gated behind the explicit "New chat" button.

`preview.html`'s `getUserThread`/`createNewThread`/`deleteThread` stubs now track real stateful thread existence (a page-global flag) instead of always/never returning a threadId, so the harness actually exercises the lookup-miss → create → use path end to end, including "New chat"'s delete-then-recreate.

**Not yet confirmed live**: this was verified end-to-end in `preview.html` against stubs modeling the exact behavior from the HAR, but not yet re-tested against the real portal after this specific fix. Next real-device test should confirm a first message goes through cleanly (no error bubble) and that "New chat" produces a working fresh conversation, not another dead thread.

## Rebuilt and reinstalled the Android APK with today's changes (2026-09-01, evening, same session)

Phone reconnected mid-session. Ran the full build pipeline documented above ("Exact working build command sequence") against today's much-changed `portal-reskin.user.js` (Gyan tab, shell rework, custom picker, all the fixes above) for the first time since it was written.

- **`hook.js`'s embedded `SCRIPT` constant had to be regenerated** — it's a JSON-escaped string literal holding the whole userscript (see `hook.js`'s own file-header note: don't hand-edit, regenerate from source). No saved generator script existed from the original session (HANDOFF said "regenerate from the generator snippet in this session's history" — that history is gone across sessions), so wrote a fresh one-off Python snippet: find `var SCRIPT = "`, walk forward respecting `\`-escapes to find the matching closing quote, replace that whole literal with `json.dumps(open('../portal-reskin.user.js').read())`. Old literal 66,864 chars → new 97,258 chars (userscript grew from the day's work). Sanity-checked by copying to a `.mjs` temp file and running `node --check` (hook.js's top-level `import` makes plain `node --check hook.js` fail outside a module context — this is just a syntax-check workaround, not a real usage change).
- **`frida-compile`/`frida-java-bridge` weren't in `.patch-tools/node_modules`** as HANDOFF describes — turned out they'd actually been npm-installed at `/home/archer/node_modules` the whole time (npm walked up to the nearest `package.json`, found `/home/archer/package.json`, installed there instead of project-scoped). Confirmed via file mtimes this predates this session — not something introduced now, HANDOFF's description of the location was just imprecise. Left as-is rather than risk breaking a working setup; used `npx --prefix /home/archer frida-compile ...`.
- Ran the documented sequence unmodified otherwise: `frida-compile hook.js -o hook.compiled.js -T none` (546,014 bytes, up from 515KB) → `objection patchapk -s flame-merged.apk -a arm64-v8a -c gadget-config.json -l hook.compiled.js -j 1` → `adb install -r flame-merged.objection.apk` (reinstall in place, not uninstall+install — preserves the logged-in session, per the existing note on this). Verified the patched APK's `lib/arm64-v8a/libfrida-gadget.script.so` is exactly 546,014 bytes, matching `hook.compiled.js` byte-for-byte — confirms the new script actually made it into the build, not a stale cached one.
- Pushed a copy to the phone at `/sdcard/Download/MyFLAME-reskin.apk` and updated the project-root `MyFLAME-reskin-working.apk`.
- **Not yet confirmed live**: install succeeded and the gadget's script matches, but nobody has opened the app on-device yet this round to confirm Gyan's `createNewThread` fix (or anything else from today) actually works against the real backend. That's the next thing to check.

## Next up
1. **TODO (not done): reskin the login page too.** Currently the script fully bails out (`isLoginPage()`) and leaves the stock login UI untouched — login is Google SSO and touching that flow risked breaking auth, so left alone for now as the safe default. If revisited: same "full replace, calls stay" approach should work fine for the *portal's own* login chrome (buttons/branding around the Google button), since the actual OAuth handshake happens on Google's origin and this script never runs there anyway — the risk was blanking the button before it's clickable, not the auth flow itself. `isLoginPage()`'s path guess (`/login`, `/identity`, `/idp`) is unconfirmed — HAR only covers post-login traffic — verify the real login URL path first before attempting a login reskin.
2. This whole design pass was verified via `preview.html` + static/synthetic data + scripted clicks, never against the actual live logged-in portal (web side; the *Android app* injection was tested against the real live app, see above). Next real-browser smoke test should specifically check: the Home→Calendar day-jump (`calState` sharing), the stock-UI toggle surviving an Aura SPA re-render (MutationObserver re-append), and that `color-mix()` (used for event-block backgrounds) renders correctly in whatever browser Tampermonkey runs under. Same caveat applies to the newer work — Gyan chat's `threadId` fix specifically *was* confirmed against the real bug via a live HAR, but the layout fix, the shell rework (top bar, long-press-to-toggle), and the custom Resource picker have only been checked in `preview.html` so far, not live.
3. **Resume the Android APK patch** — see "Android APK patch attempt" above for exact state, tooling location (`.patch-tools/`), and the specific next diagnostic step (run `.patch-tools/probe.js` via live `frida -U -n` attach against the plain listen-mode build to confirm `evaluateJavascript` works at all on this device before re-suspecting the hook logic). Not urgent — Kiwi Browser + Tampermonkey is a working fallback in the meantime.
4. **Scheduled slot auto-booker** (raised by user, not started): booking only opens 24h before a slot; user wants to pre-select a slot and have it auto-book at T-24h. Discussed but deliberately deferred until the base Android patch either works or is abandoned — this needs *real native* Android background scheduling (`AlarmManager.setExactAndAllowWhileIdle` → `BroadcastReceiver`/`Service` → headless WebView reusing the existing `callAura`/`resolveUserId` JS logic to get a fresh session token and fire `createReservation`), which only makes sense to build on top of a working APK patch pipeline. Known open risks flagged to the user: (a) unknown whether the underlying Salesforce session survives 24h fully idle — if it doesn't, nothing can auto-recover since Google SSO can't be scripted; (b) OEM battery optimization (this is a Xiaomi/HyperOS device) commonly kills scheduled alarms/background work despite `AlarmManager`'s guarantees unless the app is manually whitelisted.

## Git repo pushed to GitHub + mobile title-gap fix + APK rebuilt/released (2026-09-01, night)

- **Private repo created**: `gh repo create myflame-portal-reskin --private --source=. --remote=origin --push` under account `rk4500` → https://github.com/rk4500/myflame-portal-reskin. Existing local git history (init'd earlier this session, see "Git tracking started" above) pushed as-is; `.gitignore` already excluded HARs/APK/`.patch-tools/`/`.review/` so nothing large or session-token-bearing went up.
- **Real bug fixed, found from a user screenshot**: mobile Home tab's class rows (2:15 PM / 3:15 PM ones in the screenshot) had a big dead gap between the time and the title — `.fr-row { justify-content: space-between }` (`portal-reskin.user.js`) has only 2 flex children (`fr-row-time`, `fr-row-main`), so `space-between` shoves `fr-row-main` all the way to the row's right edge. Looks intentional/fine on a wide desktop row (edge-to-edge really does read as one row), reads as a broken gap on mobile's narrower rows. **Not a `text-align` issue** — the title text itself was always left-aligned within its own box; the whole box was being flexed to the far right. Fixed with a mobile-only override inside the existing `@media (max-width: 760px)` block: `.fr-row { justify-content: flex-start; }`. Verified via `preview.html` headless-Chromium screenshots at both 390px (gap gone, title now hugs the time) and 1400px (desktop unchanged, confirms the fix is properly scoped to the mobile query only).
- **APK rebuilt with the fix**, following the documented pipeline exactly ("Exact working build command sequence" above): regenerated `hook.js`'s embedded `SCRIPT` json-string constant from the now-changed `portal-reskin.user.js` (same marker-walk approach as last time, written fresh again since the one-off generator script itself still isn't saved anywhere — worth actually saving it into `.patch-tools/` next time instead of re-deriving it each session), `node --check`'d the regenerated `hook.js` via a temp `.mjs` copy, `npx --prefix /home/archer frida-compile hook.js -o hook.compiled.js -T none` (546,293 bytes — `frida-compile`/`frida-java-bridge` confirmed still living at `/home/archer/node_modules`, matching last session's note), then `objection patchapk -s flame-merged.apk -a arm64-v8a -c gadget-config.json -l hook.compiled.js -j 1`. Verified `lib/arm64-v8a/libfrida-gadget.script.so` inside the output APK is exactly 546,293 bytes, matching `hook.compiled.js` byte-for-byte. Copied to project-root `MyFLAME-reskin-working.apk`.
- **No phone connected this session** (`adb devices` empty) — could not `adb install -r` or re-verify on-device. Build is untested live; only verified via the preview-harness screenshots and the byte-match check above.
- **Uploaded as a GitHub release**: `gh release create v2026.09.01 MyFLAME-reskin-working.apk --title "MyFLAME reskin — 2026-09-01" --notes "..."` → https://github.com/rk4500/myflame-portal-reskin/releases/tag/v2026.09.01. This is now the easiest way to get the APK onto the phone (download from the release, no PC/adb needed) as an alternative to the existing `/sdcard/Download/MyFLAME-reskin.apk` push — **that on-device copy is now one build behind** (predates this fix); worth an `adb push` or downloading the release directly next time the phone's connected.

### Loose end — resolved same session
- `hook.js`'s `SCRIPT`-regeneration script saved to `.patch-tools/regen-hook-script.py` (gitignored, local tooling only) instead of being reconstructed from HANDOFF prose each time. Run from `.patch-tools/` after any userscript change, before `frida-compile`.

## Working rules: no auto-commit/push (2026-09-02)

User: only commit/push when explicitly told to. Added `CLAUDE.md` at project root with this rule. Making/staging changes and rebuilding the APK without asking is still fine — the restriction is specifically on `git commit`/`git push`. **As of this entry, the fixes below are made locally but NOT committed or pushed** — next session (or later this one) should check `git status` before assuming anything past the last real commit is on GitHub.

## Mobile alignment fix scoped correctly; double/triple loading-screen flash fixed (2026-09-02)

Two real bugs from live user testing of the previous build:

- **Yesterday's `.fr-row { justify-content: flex-start }` mobile override was too broad** — it also hit My Bookings' rows, breaking the status pill (`fr-badge`)/Cancel button's right-alignment (they rely on `space-between` to push right; class-schedule rows don't have a pill/button, they were the only ones that needed the override). Fixed by scoping via `:has()`: `.fr-row:has(.fr-row-time) { justify-content: flex-start; }` — `.fr-row-time` only exists on class-schedule rows (grepped to confirm: exactly one usage site, the Home tab's classes list), so this can't accidentally match a booking row. Verified via `preview.html` screenshots: mobile Home (class rows still fixed) and mobile My Bookings (pills/Cancel button still pinned right).
- **Triple-loading-screen flash on Android app launch, reported live**: user described RN native splash → white stock loader → reskin flashes in **desktop layout** → covered by another white stock loader → reskin finally loads mobile-correct with its own data-loading spinner. Root-caused in `.patch-tools/hook.js`:
  1. `onPageFinished`'s URL match (`url.indexOf('my.flame.edu.in') !== -1`) matched **both** the SSO `frontdoor.jsp` redirect page *and* the real `/s/` portal page (this was already flagged, unactioned, in the "Loose ends, not urgent" list from two sessions ago — turned out not to be harmless after all). The reskin got injected into `frontdoor.jsp` first — a transient, not-yet-viewport-settled page, which explains the "desktop-mode" flash the user described (media query likely not resolving correctly on that stub page) — then thrown away wholesale when the browser navigated on to `/s/`, forcing a second full paint-then-reinject cycle. **Fix**: narrowed the match to `url.indexOf('my.flame.edu.in/s/') !== -1` so the reskin only ever injects once, on the real portal page.
  2. Separately, Android `WebView` paints solid white by default until the page's own background CSS loads — since our reskin only hides/replaces the stock UI via `evaluateJavascript` at `onPageFinished` (i.e., *after* the page has already had time to paint), Aura's own white boot screen was visible for a beat before every injection, on both the (now-removed) `frontdoor.jsp` hit and the real `/s/` hit. **Fix**: set the `WebView`'s background color to the reskin's dark theme (`DARK_BG = 0xFF11131A`, matching `--bg`) synchronously inside the `setWebViewClient` hook (`installHook()`, fires the moment RN wires up the WebView — the earliest hook point available, well before any navigation starts) via `this.setBackgroundColor(DARK_BG)`. This is the same constant `paintStatusBarOverlay` already used for the status-bar overlay, just applied far earlier and to the whole WebView instead of a status-bar-height sliver.
  - Net expected effect: RN splash → (now dark, not white) brief WebView background while Aura boots → single reskin injection directly into the real mobile-correct UI → its own data-loading spinner. Removes one full paint-discard-repaint cycle and the intermediate desktop-mode flash entirely; the initial white flash during Aura's own boot (before `onPageFinished` fires at all) should now render as the app's own dark color instead of white, which won't eliminate the boot-time gap but should make it visually continuous instead of a jarring flash.
- **hook.js's embedded `SCRIPT` constant regenerated** via the now-saved `.patch-tools/regen-hook-script.py` (see loose-end entry above) to pick up the `:has()` CSS fix, then rebuilt through the standard pipeline (`frida-compile` → `objection patchapk -s flame-merged.apk -a arm64-v8a -c gadget-config.json -l hook.compiled.js -j 1`). Verified `libfrida-gadget.script.so` inside the output APK byte-matches `hook.compiled.js` (546,721 bytes). Copied to project-root `MyFLAME-reskin-working.apk`.
- **Not tested live — no phone connected this session** (`adb devices` empty). Both fixes are logically sound and match confirmed root causes from the live bug report, but the actual on-device flash sequence has not been re-observed after this build. Next session with the phone connected: `adb install -r` this APK (or download the eventual GitHub release once pushed) and watch the launch sequence again — specifically confirm (a) no more desktop-mode flash, (b) only one white/dark transition before the reskin appears, (c) status bar/nav bar insets still correct (untouched by this change, but worth a glance since `hook.js` was touched).
- **Not yet committed to git** — per the new no-auto-commit rule above, `HANDOFF.md`, `portal-reskin.user.js` (the `:has()` scoping), `.patch-tools/hook.js` (gitignored, not trackable anyway), and the rebuilt `MyFLAME-reskin-working.apk` (also gitignored) are all sitting as uncommitted/untracked local changes. The already-created GitHub release `v2026.09.01` still points at the *previous* build (pill-alignment bug present, double-flash present) — a new release should be cut once this build is confirmed working live and the user says to commit/push.

### Follow-up hardening: `setUseWideViewPort(false)` (2026-09-02, same session)

User asked why the desktop-mode flash happened at all and why the reskin even carries a desktop CSS path on an Android-only app. Real mechanism, confirmed by reasoning through Android `WebView` defaults (not yet independently verified live): `react-native-webview` defaults `useWideViewPort=true`, which makes `WebView` assume a virtual ~980px-wide viewport (then scale-to-fit) on any page lacking its own `<meta name="viewport">` tag — `frontdoor.jsp` has none (it's a bare redirect stub), so our `@media (max-width: 760px)` query evaluated against the fake 980px width and lost, rendering desktop layout. `/s/` itself does declare `width=device-width`, which is why it always rendered correctly. The `/s/`-only injection scoping (above) already removes the one page this actually hit — but added `this.getSettings().setUseWideViewPort(false)` in the same `setWebViewClient` hook as belt-and-suspenders, so *no* future page (viewport tag or not) can trigger the wide-viewport guess again. Confirmed this does not pin a fixed width or break responsiveness: with it `false`, `WebView` always lays out at the real device/widget CSS width regardless of any page's viewport meta — rotation, tablet widths, split-screen resize all still work normally; it only removes the "guess 980px if no viewport tag" fallback specifically.

The desktop CSS path itself (sidebar-to-topbar, wide `.fr-content`, etc.) stays in `portal-reskin.user.js` — it's not Android-specific, the same file is shared verbatim (via `hook.js`'s `SCRIPT` constant) with the real desktop Tampermonkey/Violentmonkey use case, and doubles as tablet-width support on Android. No file split, no dead-code removal — the CSS was never the bug, the wide-viewport guess was.

Rebuilt: no `portal-reskin.user.js` changes this round (native-only fix), so no `SCRIPT` regeneration needed — recompiled `hook.js` directly (`frida-compile` → `objection patchapk`, same command as always). Verified `libfrida-gadget.script.so` byte-matches `hook.compiled.js` (546,889 bytes). Copied to project-root `MyFLAME-reskin-working.apk`. **Still not tested live — no phone connected this whole session.** Still not committed, per the no-auto-commit rule.

### Discussed, not started: scheduled slot auto-booker (2026-09-02)

> **Superseded — see "Scheduled auto-booking, first version" (2026-09-03) at the end of this file.** None of the four native pieces below were needed: the user pointed out they open the app several times a day, which turns the whole problem from "wake the phone at T-24h" into "try again on the next launch". Kept here because the native path is still the only way to win a five-minute rush, so if the shipped version proves too slow, this is the design to come back to — including its two unverified risks.

User asked what it'd actually take. Laid out 4 required native pieces, none built yet:
1. Persist the booking intent (resource/date/slot) somewhere the native side can read — new UI + a native write, not built.
2. `AlarmManager.setExactAndAllowWhileIdle` fired at T-24h, needs a `BroadcastReceiver`/`Service` registered in the manifest — a real manifest edit, beyond anything `.patch-tools/`'s pipeline does today (it only patches smali + injects the gadget, never touches the manifest's component declarations).
3. Headless execution at fire time: spin up a background `WebView`, reuse the existing `resolveUserId()`/`callAura()` JS already in `portal-reskin.user.js` to get a fresh token off a live session, fire `createReservation`.
4. Result surfacing via a notification, since nothing's on-screen when the alarm fires.

**No longer blocking** (the shipped version never idles a day — it uses whatever session is live when you next open the app), but still required before any *native* version: manually test whether the Salesforce session survives 24h fully idle (leave the app signed in, untouched, try an aura call a day later) — if the session doesn't survive, nothing here can recover it (Google SSO can't be scripted headlessly) and the whole feature is dead on arrival. Second known risk, also unverified: HyperOS (this is a Xiaomi device) battery optimization commonly kills scheduled alarms unless the app is manually whitelisted, independent of `AlarmManager`'s official guarantees.

## Mobile layout pass: even boxes everywhere + cancel-confirm reorganized (2026-09-02, same day)

User report, all mobile, all the same underlying failure — a row/tile whose text ran wider than its box, so it wrapped and grew taller than its neighbours (and, in the cancel case, shoved the row's own content aside):

1. **Cancelling a gym booking** — the confirm step swapped the Cancel button for a question + two buttons *inside* the row's flex flow, so three wide children fought the title/time for one row's width and squeezed it into a sliver on the left, with everything visibly jumping as it appeared.
2. **Home → Classes** — a long course title ("Business Plan Development and Entrepreneurial Finance") wrapped to two lines and made that row taller, which reads as "this class runs longer" when it only means "this name is longer".
3. **My Bookings** — a cancellable row carries a status pill *and* a Cancel button; with "Tomorrow" in the meta line the trailing AM/PM wrapped onto its own line and grew the box.
4. **Book Slot tiles** — two-digit hours ("10:00 AM – 11:00 AM") overflowed the tile and wrapped the same way.

### Fixes (all in `portal-reskin.user.js` unless noted)

- **Cancel confirmation: the row's own title becomes the question** (`attachCancelConfirm`, `.fr-row.is-confirming`). Three earlier passes were rejected, all failing the same way — each *added* something to a row that had no room for it, then had to buy that room from the booking's own text:
  1. Question + two buttons swapped inline for the Cancel button → squeezed the title/time into a sliver.
  2. Question + buttons as an absolutely positioned overlay over the row's right end → no shift, but it covered the booking being cancelled ("i don't like the overlay").
  3. A drawer expanding downward, question on the lead line, buttons 50/50 below → wasteful and, measurably, uneven: a collapsed drawer still picked up the mobile `.fr-row { gap: 10px }`, so a cancellable row measured **80px against every other row's 70px**. (`.fr-row--confirmable { gap: 0 }` lost to the later media-query rule at equal specificity — a real ordering bug, not a rounding artifact.)

  What ships (user's own proposal): tapping Cancel rewrites the row's title from `Gym` to `Cancel Gym?` and swaps the one ghost button for `Yes` (danger) + `No` (ghost). The booking is named once, in the place already reading as its name, so the confirmation costs one extra button and nothing else. No expansion, no overlay, no reflow — **measured 70px for every row, idle and confirming, at both 360 and 390px**. The row also takes a `color-mix(in srgb, var(--danger) 12%, var(--bg-elevated))` tint and a danger-coloured title while armed, so an armed row is unmistakable at a glance.
  - `Yes` sits to the **left** of `No`, reversing the usual order on purpose: `No` lands on the exact spot the finger just left (the trigger's), so a double-tap can't cancel a booking. The danger fill keeps `Yes` equally findable from either side.
  - Failure replaces the *time* line with `Couldn't cancel: <msg>` in danger colour and turns `Yes` into `Retry`; the title above still says which booking failed, and the row still can't change size.
  - One row asks at a time (`row.__frCloseConfirm`) — two rows both reading "Cancel …?" is a genuine misread risk. Escape closes and returns focus to the trigger. Focus only moves to `No` on a keyboard activation (`click.detail === 0`), so a tap isn't left with a focus ring parked on a button it never targeted.
- **Rows can no longer grow**: `.fr-row-title` and `.fr-row-meta` are single-line + ellipsis, `.fr-row-main` is `flex: 1; min-width: 0` so it shrinks instead of pushing the pill/button off the edge, and full text is kept in a `title` attribute. `.fr-row-meta:empty::before { content: '\00a0'; }` reserves the meta line's height for a class row that has no room/faculty, so it doesn't sit shorter than its neighbours — and the class row now always appends the meta element (previously `if (meta)`). Note the CSS content value is written `'\\00a0'` in the source: `STYLE_CSS` is a template literal, where a bare `\0` followed by a digit is an illegal octal escape (this is a hard `SyntaxError`, caught by `node --check`, not a silent bug).
- **`compactTimeRange(start, end)`** (new) drops the on-the-hour `:00` from every displayed range: `"10:00 AM" + "11:00 AM"` → `"10 AM – 11 AM"`; a `:15`/`:30` slot keeps its minutes; an unrecognised format is left alone. Used by the slot tiles, the booking confirm panel title, and booking rows. This is what actually made the numbers fit — a two-up tile on a 390px screen has ~130px of text width, and `"10:00 AM – 11:00 AM"` needs more than that at any font size worth reading. It also dropped the *leading* meridiem when a range didn't cross noon (`"10 – 11 AM"`), which fit even better; user rejected that as "a little too minimal" and asked for both meridiems back on every range, in both the bookings list and the slot grid. Both are kept now. The regex tolerates the narrow no-break space (` `) newer ICU puts before the meridiem.
- **`shortDayLabel(date)`** (new) — `dayLabel`'s long weekday ("Wednesday, Sep 10") is right for a page heading but too wide for a booking row. Bookings now use the short form. It also fixes a real pre-existing bug carried over from `dayLabel`: it compared a *datetime* against midnight, so a 4 PM booking tomorrow rounded to 2 days out and displayed "Thu, Sep 3" while a 7 AM one the same day displayed "Tomorrow". Both ends are now floored to a date first. `dayLabel` itself is unchanged (its callers pass date-only values).
- **Mobile-only** (`@media (max-width: 760px)`): the status pill is dropped on a row that has a live Cancel button (`.fr-row-actions:has(.fr-cancel-wrap) .fr-badge`) — the button only ever renders on a booked reservation, so the pill restates it, and dropping it buys the text line ~70px. Pill and button moved into one `.fr-row-actions` column to make that rule expressible. Slot grid floor raised 130px → 146px (still two columns at 360px), tighter tile padding, slightly smaller tile time.

### QA
`preview.html`'s availability stub was serving three friendly slots (6 AM, 7 AM, 1 PM) — replaced with the real captured full 6 AM–2 PM gym window from `aura_map.json`, so two-digit hours and the noon crossing are always on screen. Its screenshot-determinism rule now also freezes the confirming row's colour transition (the virtual clock was catching it mid-flight and screenshotting a half-tinted row, which looked like a real bug). **New `?auto=measure` and `?auto=cancel-measure` modes** dump every `.fr-row`/`.fr-slot`/`.fr-datestrip-cell`'s measured height into a `<pre id="measure">` for `chromium --dump-dom` to read — "are these boxes actually the same height" is a question to answer with numbers, not by eyeballing a PNG. That is how the 80px-vs-70px drawer bug above was caught after it had already passed a visual review. Verified with `chromium --headless=new` at 390×844, 360×800 and 1280×900: home classes, My Bookings (idle + confirming), Book Slot grid, and the booking confirm panel — every box in a list is now the same height, and the confirm panel causes no movement at all.

**Not committed** (no-auto-commit rule). **Not rebuilt into the APK** — `portal-reskin.user.js` changed, so a new APK needs the `SCRIPT` regeneration + `frida-compile` → `objection patchapk` sequence documented above before the phone sees any of this.

## Rail tried and rejected; release cut; Actions investigated (2026-09-03)

**Released `v2026.09.03`** — APK rebuilt from `099875a`, uploaded as a GitHub release asset. Build was verified rather than assumed: the embedded `lib/arm64-v8a/libfrida-gadget.script.so` is byte-identical to `hook.compiled.js`, and the rebuild log has no `OutOfMemoryError` (the two "may have failed" lines objection prints are its standard boilerplate — check for the OOM string, not for those). Still not tested on-device; no phone connected.

**Rail: built, rejected, kept.** Branch `rail` (`aaa8948`, local only, never pushed) replaces the filled `.fr-row` card with a hairline `.fr-rail` list across Home and My Bookings, and unfills slot tiles and date-strip cells to match. User rejected it on sight ("i don't like this rail thing at all"). Branch is deliberately **not deleted** — don't remove it without asking. `master` has no rail code.

### Still open: long class titles truncate on Home

Master's Home rows ellipsise a long course name. These are the measured facts (reskin's own font, its own sizes), worth not re-deriving:

| Course | one line | lines @234px (today) | lines @322px (full width) | px to fit one line @234 |
|---|---|---|---|---|
| Applied Formal Methods | 187px | 1 | 1 | 20.1px |
| Principles of Machine Learning | 237px | 2 | 1 | 15.8px |
| Design and Analysis of Algorithms | 264px | 2 | 1 | 14.2px |
| Entrepreneurial Failure and Sustenance | 302px | 2 | 1 | 12.4px |
| Business Plan Development and Entrepreneurial Finance | 435px | 2 | 2 | **8.6px** |

- **Shrink-to-fit is ruled out** — the longest real course name needs 8.6px, half the size of the row above it, and unreadable on a phone. Asked and answered; don't revisit.
- The left time column costs 88px (78px min-width + gap) and is the whole problem: at full width, 5 of 6 real course names fit on one line, and all 4 real `room · faculty` strings fit (3 of 4 don't at 234px).
- Remaining candidates: **(A)** clamp the title to two lines with the height reserved, so rows stay equal — still truncates faculty; **(B)** move the time to a small line above the title so the text gets full width, reserving two title lines to keep cards equal.

### UI references the user wanted to look through

Named while discussing a broader overhaul; the user is deciding for themselves. What each is worth stealing:

- **Things 3** — the strongest case that deleting boxes makes a list *prettier*. Type and whitespace only, one blue accent, nothing else.
- **Structured** (iOS day planner) — a time rail done well; colour-coded dots carry category without carrying decoration.
- **Cron / Notion Calendar** — dark, hairline, restrained; proof a dark schedule UI can feel expensive with almost no colour.
- **Linear** — low-chroma dark, 1px borders instead of filled surfaces, obsessive consistency. The reference for "expensive without decoration".
- **Amie** — the counter-argument: big type, playful colour, still simple. Look here if the app should feel less severe.
- **Fantastical** — dense day lists that stay legible; strong time-column typography.
- **Citymapper** — vertical time rail with connectors, if a day should read as a journey.

A comparison board rendering Rail / Timeline / Quiet-slab against the real schedule was published as an artifact during that discussion. Note the finding it produced: a **timeline whose block height maps to duration is wasted on Home**, because every FLAME class is 55 minutes — block height would encode only the gaps between classes. Worth stealing for the Calendar tab, not for Home.

### Can the APK build run in GitHub Actions? Yes, with two prerequisites

Investigated, not built.

- **Signing is not the problem, contrary to first assumption.** `objection.jks` ships *inside the objection package* (`site-packages/objection/utils/assets/objection.jks`), so it is identical on every install of a given version. Pin `objection==1.12.5` and CI emits an APK signed `CN=Unknown, OU=objection, O=SensePost` — the same cert as local builds, so `adb install -r` still upgrades in place and preserves the logged-in session. Verify with `apksigner verify --print-certs`; SHA-256 of the current signer is `f7697c66…f46ce0fb`.
- **Prerequisite 1 — the base APK.** `flame-merged.apk` (33MB, the stock app merged from its 4 splits) can't be committed sensibly: binaries don't delta-compress, so every re-pull of the stock app adds another permanent 33MB to every clone. Put it in a private release asset and have CI `gh release download` it.
- **Prerequisite 2 — the toolchain sources aren't tracked.** `.patch-tools/` is ignored wholesale, but it is 1023MB across 32,736 files of which only ~120KB is source (`hook.js`, `gadget-config.json`, `regen-hook-script.py`, `probe.js`). Invert the ignore:
  ```gitignore
  .patch-tools/*
  !.patch-tools/hook.src.js
  !.patch-tools/gadget-config.json
  !.patch-tools/regen-hook-script.py
  !.patch-tools/probe.js
  ```
  Don't track `hook.js` as-is: 116KB of its 118KB is the pasted copy of `portal-reskin.user.js`, so every userscript edit would produce a second near-identical diff of the whole file in the same commit. Split it into `hook.src.js` (~2KB of real Frida logic with a placeholder constant) and let `regen-hook-script.py` fuse the two at build time.
- Everything else is free: ubuntu-latest ships the JDK and Android SDK, 16GB of RAM covers `-Xmx6g`, objection fetches the gadget itself, and the local patch takes under two minutes. Trigger on `v*` tags and the release upload becomes automatic.
- Keep the phone screenshots in `.patch-tools/*.png` ignored regardless — they show the portal with a real name and roll number on screen.

## Booking resource lists are sorted (2026-09-03)

`getResources` returns each facility's rooms in query order — `ARB001, ARB004, ARB002, ARB101, …` — which reads as no order at all in the picker. Sorted once in `renderBookSlot`, at the point the response lands, so the picker, the default selection (`resources[0]`) and anything else reading `bookState.facilities` all agree.

Two details that a plain `.sort()` gets wrong:
- **Numeric collation** (`Intl.Collator(undefined, { numeric: true })`) is what puts `ARB002` before `ARB101`; a string sort puts `"101"` before `"2"`. It also sequences Discussion Room A/B/C.
- **Operating windows are compared as times.** `Gym ( 3:00 pm to 11:00 pm slot )` would otherwise sort before `Gym ( 6:00 am to 2:00 pm slot )` on the bare digit 3. `splitResourceWindow()` peels the parenthetical off and compares real minutes, so the morning gym lists first — which matters, since that is the one booked daily.

Verified against the real captured resource lists for all three facilities, and in the harness DOM.

## Home-screen widget: asked, analysed, dropped (2026-09-03)

User asked how possible a widget showing the class schedule would be, then dropped it ("forget that"). The analysis is worth keeping, since the answer is counterintuitive.

**The class timetable is semester-static**, so a widget showing classes needs no live data at all — which removes the hard part (background auth) entirely. Four paths, cheapest first:

1. **Export to the phone's calendar (.ics).** Not a widget, and better than one. The events already carry everything iCalendar needs (`startDateTime`, `endDateTime`, `courseName`, `room`, `faculty`). Import once a semester and *every* calendar widget shows classes, plus notifications, plus offline, plus it outlives the session. Caveat: a WebView download inside the patched app needs a `DownloadListener` the stock app may not have, so generate it on the desktop userscript path or via a share intent.
2. **Termux:Widget / KWGT** — a real widget, no patching; needs a cookie exported from a live session and re-exported when it expires, since Google SSO can't be scripted.
3. **A separate companion app** — Kotlin, WebView login once, `CookieManager` holds the session, `CoroutineWorker` refreshes, `AppWidgetProvider` draws it. Reuses the call shapes in `aura_map.json`. The proper answer for a live widget, and it avoids the smali pipeline completely.
4. **Patching a widget into MyFLAME itself** — manifest `<receiver>`, new layout and `appwidget-provider` resources (new resource IDs inside an already-compiled `resources.arsc` is the fiddly part), a provider in smali, plus a refresh service. Most work, least payoff over 3. Don't.

## Scheduled auto-booking, first version (2026-09-03) — built, uncommitted

The rules, from the user (not derivable from the HAR, which only ever captured one date):
- **One booking per resource *class* per calendar day** — not per rolling 24h, and "class" is the resource without its operating-window suffix, so the 6 AM gym and the 3 PM gym are the same thing to the rule.
- **A slot is only bookable once it is less than 24h away.**
- Popular gym slots are gone within about five minutes of opening.

### Why this needs no native work at all

The earlier plan (see "Discussed, not built" above) was `AlarmManager` at T-24h waking a headless WebView — which needs a manifest edit the patch pipeline can't do, plus a session surviving a day idle. The user's own observation removed all of it: **the app gets opened several times a day, so an intent parked in `localStorage` and retried on every launch books anything uncontested with zero native code.** Works on desktop Tampermonkey too. The known limit, stated plainly rather than hidden: it will lose a five-minute rush unless the app happens to be open at the time. That is what this version is, not an oversight.

### How it works

- Intents live in `localStorage['flame-auto-book']`: resource, ISO date, slot start/end, state, last message.
- `runAutoBook()` fires at boot (after the aura token lands) and on a loop while the app is open — every 2 min normally, tightening to 15s once an intent's opening moment is within 30 min, since that tight loop is the only way this version ever wins a contested slot.
- Per intent: skip if not open yet; fail if the slot has already started; **pre-check `getReservations`** for an existing booking of the same class that day (turns a guaranteed refusal into a clear message); then `getResourceAvailability`, and only if the exact `startTime` has capacity, `createReservation`.
- Refusals arrive as `state: SUCCESS` with a plain string, so the text is the only signal: `/booking id/i` means booked, anything else is a failure carrying the portal's own words.
- No capacity yet? The intent **stays waiting** — someone may cancel, and there is still time on the clock.
- `AUTOBOOK_EARLY_MARGIN_MS` (30 min) starts attempts slightly before the calculated opening. "Less than 24h away" is our *model* of the rule, not something the API states; being early costs two cheap calls that answer "not listed yet", being late costs the booking.

### UI

- Book Slot's grid now merges live availability with the resource's **own full day**, derived from its operating-window suffix (`Gym ( 6:00 am to 2:00 pm slot )` → hourly slots) or, for resources without one, from times remembered in `localStorage['flame-slot-times']` after any earlier availability fetch. A slot the portal won't list yet is real and dated, just not open — it renders dashed with `Opens in 6h 17m` and schedules on tap.
- A "Booking automatically" list under the grid, with a Stop per intent.
- Results land as a banner above whatever tab is open (`.fr-banner-host` lives outside `contentEl`, which every tab render replaces), carrying the portal's own message and a Dismiss. Silent auto-booking is how you end up with a reservation you didn't know about.

### Harness

`preview.html` now models the 24h window in its availability stub instead of returning every slot regardless of date, so the schedulable path is reachable locally. New `?auto=schedule` / `?auto=schedule-twice` scenarios click the not-yet-open tiles, and `?seed=due` plants an already-open intent so the runner actually fires on load and its banner can be screenshotted — the one part of this that is otherwise only observable by waiting a day. Verified end to end in the harness: seeded intent → availability check → `createReservation` → success banner.

### Not verified against the real portal

The 24h model, whether `createReservation` accepts the exact `startTime` string taken from availability (it is passed through verbatim, which is what the real booking flow already does), and whether "class" really is the cleaned resource name. First real use will settle all three.

## Nothing wraps unless it is meant to; auto-booking is one slot per resource per day (2026-09-03, evening)

Two asks, both from live use of the uncommitted auto-booking build.

### Wrapping is now checked, not eyeballed

The reskin had no way to answer "does anything wrap that shouldn't" other than looking at a PNG, which is how a long room name in the Calendar survived three sessions of review. `preview.html` now has **`?auto=wrapcheck`**, paired with **`?long=1`**:

- `?long=1` replaces every string the portal supplies — course, room, faculty, resource, facility — with a much longer one. The layout is checked against the worst case; screenshots stay on real data.
- `?auto=wrapcheck` walks every element in `#flame-reskin-root` that renders text directly, and counts the lines it actually occupies from the client rects of a `Range` over its own text nodes. That is what the renderer did, not what the CSS claims — the CSS being the thing under test. Variants `wrapcheck-week`, `wrapcheck-cancel`, `wrapcheck-confirm` and `wrapcheck-sched` run the same check with an interactive state open, since a confirm or schedule row is where new text lands in a box that was already full.
- `WRAP_OK` inside that scenario is the registry of text that is *meant* to wrap: chat bubbles, empty states, the success/error panels, and the auto-book banner's note (the portal's own refusal string — a truncated reason is worse than a two-line banner). Add to it deliberately, with a reason; never to quiet a finding.
- It reports the number of text runs scanned, so "0 unplanned" can be told apart from "the page never rendered".

**The second half matters as much as the first**: `white-space: nowrap` converts a wrap into an overflow, so a clean wrap report alone proves nothing. The scenario also flags any element wider than its own box whose computed `overflow-x` is `visible` — `hidden`/`clip` is the ellipsis doing its job (an ellipsised line always measures wider than its box) and `auto`/`scroll` is a container that scrolls on purpose. That check is what caught the one genuine regression these fixes introduced, below.

Also added **`?auto=calfit`**, which dumps content height against box height for every calendar block. A block's height *is* the class's duration and cannot grow, so that ratio is the only way to see whether the clip is cutting something a reader needed.

### Fixes

- **Calendar event venue no longer wraps.** It was the one line deliberately allowed to, on the theory that a long room name should grow the block rather than lose characters. It grows it into the block below: block height is duration, so an extra line is an overlap, and `Chandragupta - Focus Room 201 W` is a real room name. Venue and professor are one line each now, the course title keeps its two-line clamp, and `.fr-cal-event` itself takes `overflow: hidden` so nothing can escape into a neighbour. Full text moved to the block's own `title` attribute.
  - The two-line title clamp is kept and registered as planned wrapping, because it measurably fits: `?auto=calfit` reports box 72px / content 72px for a 55-minute class at 360, 390 and 1280px. Every FLAME class is 55 minutes, so that is the real case, not a lucky one.
- **Everything else that could wrap and shouldn't**, all one line + ellipsis: `.fr-cal-day-header`, `.fr-slot-cap`, `.fr-sched-name`/`.fr-sched-note`, `.fr-banner-title` (the summary — its note still wraps on purpose), `.fr-facility-item`, `.fr-book-field-label`, `.fr-book-results-title`, `.fr-confirm-panel-title`, `.fr-group-heading`, `.fr-datestrip-weekday`/`-daynum`, and `white-space: nowrap` on `.fr-btn` and `.fr-badge` — a button label that wraps is always a bug.
- Home's class location (`.fr-row-meta`) already clipped correctly; it is now covered by the check rather than by assumption.
- **The one real regression, caught by the overflow half**: `.fr-book-layout`'s `1fr` tracks. A grid track's default minimum is `min-content`, so a single unbreakable child — a facility name that no longer wraps — widened its column past the viewport and took the whole page sideways (531px of content in a 450px box at 360px wide). Both tracks are `minmax(0, 1fr)` now.

Swept at 360×800, 390×844 and 1280×900 across all five tabs plus the four interactive states: 27 scenarios, zero findings. Row and tile heights are unchanged from the previous pass (70px rows, 68px date cells).

### One auto-booking per resource class per calendar day

The rule was already understood and was already enforced at fire time (`alreadyBookedThatDay`), but nothing stopped two intents being *scheduled* for the same gym on the same day — they simply produced one booking and one refusal later. Refusing at scheduling time is both clearer and cheaper.

- `resourceClassKey(name)` is `cleanResourceName()` lowercased: the 6 AM gym and the 3 PM gym are two `resourceId`s and one class, so scheduling either closes the day for both.
- `conflictingIntent(resourceName, isoDate)` finds the waiting intent already holding a class/day. `scheduleIntent` returns `null` when one exists.
- **The slot grid says so rather than silently ignoring the tap**: once a class/day is claimed, every other later-tile for that class is disabled and captioned with the slot that holds it (`6 AM – 7 AM scheduled`), with the full reason on its `title`. The scheduled tile itself stays interactive — tapping it still cancels. Confirmed across resources: scheduling the 6 AM gym disables every tile under the 3 PM gym for that date too.
- **`runAutoBook` keeps its own guard**, because `scheduleIntent` cannot help with intents already sitting in `localStorage` from before this rule, and because a booking made earlier in the same pass is not in `bookingsCache`'s snapshot. A `claimed` set is filled by both a successful `createReservation` and an `alreadyBookedThatDay` hit, and a later intent for a claimed class/day fails with the reason instead of being sent to the server.
- Manual booking is untouched — the rule as asked covers auto-booking, and the portal refuses a manual duplicate itself, which the error panel already surfaces.

Harness: `?auto=sched-rule` clicks three later slots for one resource and asserts one intent, one scheduled row, and seven disabled tiles naming the holder. `?auto=sched-rule-cross` does the same across the two gym resources. `?seed=twin` plants two same-class intents directly (the legacy-localStorage shape) and `?auto=seed-result` dumps what the runner did with them — one `done`, one `failed: Only one Gym booking a day`.

**Harness bug fixed while doing this**: `?seed=due` hardcoded "1 PM today", so running the harness after 1 PM silently produced a "the slot started before it could be booked" failure instead of the success banner it is documented to screenshot. Both seeds now search the gym's own window across today and tomorrow for the first hour that is still ahead and less than 24h out, so they work at any clock time. `?auto=schedule`/`schedule-twice` likewise now step two days out — one day out is mostly inside the 24h window and has no schedulable tiles left to click.

**Not verified against the real portal**, same as the rest of the auto-booking work. **Not committed, not rebuilt into the APK** — `portal-reskin.user.js` changed, so a new build needs `regen-hook-script.py` → `frida-compile` → `objection patchapk`.

## Code split into `src/`, with a build that also writes `hook.js` (2026-09-03, branch `build-split`)

`portal-reskin.user.js` had reached 2,755 lines in one IIFE, and `hook.js`'s embedded copy of it was regenerated by hand every time. Both are now built by one command.

```bash
npm install     # once
npm run build   # src/ -> portal-reskin.user.js -> .patch-tools/hook.js
npm run watch   # same, on every save
```

Measured on this machine: `npm run build` ~210ms end to end (mostly npm's own wrapper and node's boot); inside `watch`, a save rebuilds in **50-110ms**. The hook fuse is ~1.4ms of that, which is why watch does it too — skipping it bought nothing and left `hook.js` stale for exactly as long as you were iterating, which is the drift this build exists to prevent.

**Edit `src/`, never `portal-reskin.user.js`.** That file stays tracked — it is what gets pasted into Tampermonkey, what `preview.html` loads, and what a release ships — but it is generated now.

### Layout

One module per section of the old file: `aura.js`, `chrome-hide.js`, `styles.js` + `styles.css`, `toggle.js`, `icons.js`, `dom.js`, `dates.js`, `shell.js`, `autobook.js`, `tabs/{home,calendar,bookings,book-slot,gyan}.js`, `main.js`. 543 lines of CSS moved into a real `.css` file, which also let `.fr-row-meta:empty::before`'s content value go back to a single-escaped `'\00a0'` — the double escape only existed because it was inside a template literal.

Two things the single-file version got for free needed handling:

- **Shared mutable state.** `let bookingsCache = null` in one closure could be reassigned from anywhere; an ES module binding is read-only for importers. `src/state.js` holds the seven values genuinely written from more than one module (`ui.root`, `ui.contentEl`, `ui.bannerHost`, `ui.toggleBtn`, `ui.activeToken`, `cache.events`, `cache.bookings`) as fields on two objects, so the assignment goes through the object every importer shares. Anything written only by its own module stayed a plain `let` — `currentTab` and `calState` moved *into* the modules that own them.
- **A dependency cycle.** The shell named the five tab renderers while every tab imported `switchTab`/`renderEmpty` back out of the shell. `shell.js` now exports an empty `RENDERERS` and a `registerRenderers()`, which `main.js` calls at boot. The arrow points one way: tabs → shell, never back.

### Rollup, not esbuild

Tried esbuild first. It builds in 7ms and **discards ordinary comments** — it dropped roughly 400 comment lines, which in this repo is most of the reasoning recorded in this file. Rollup keeps the source's shape; the output is the same IIFE, same indentation, comments intact (431 lines of them against 405 hand-written before the split), 2,256 lines against the old 2,755. Its only plugin is eight lines inline in `build.mjs` turning `styles.css` into a string.

### Two real bugs found doing this, both by the harness rather than by reading

1. **The import inference missed every identifier used only inside a template literal.** The pass that generated the import statements blanked template literals before scanning for free identifiers, so `` `${shortDate(days[0])} – ${shortDate(days[6])}` `` looked like it referenced nothing. Calendar's week view silently did nothing on click — `shortDate` was an undefined global. Caught because `?auto=wrapcheck` reported 49 text runs on the week scenario where the pre-split run reported 102, i.e. the Week button had not taken effect. Fixed by keeping `${...}` bodies when stripping strings, then regenerating every import block from scratch.
2. **`build.mjs` filled the wrong `__USERSCRIPT__`.** `hook.src.js`'s header comment explains what the placeholder is, and so contains the token — an unanchored `replace()` took that first occurrence, producing a 130KB comment and a hook that injected nothing, with no error anywhere. Now the placeholder is matched *with its surrounding quotes*, asserted to appear exactly once, and the fused output is checked for `flame-reskin-root` before being written. This is precisely the drift the single command exists to prevent, so it fails loudly instead.

A third find, not a bug in the split: `?auto=book-confirm`, `book-confirm-fail` and `book-confirm-room` clicked `.fr-slot:not(:disabled)` on *today*, whose gym window has closed by evening — the same clock-dependence `?seed=due` had. They step to tomorrow now. `book-confirm-room` and `resource-picker-overflow` also matched facility names with `===`, which `?long=1` breaks; both use `startsWith` now.

### Harness: runtime errors are now a reported failure

A thrown `ReferenceError` renders as an empty or half-built tab, which in a screenshot reads as a styling problem — that is how the `shortDate` bug survived its first look. `preview.html` now traps `window.onerror`, `unhandledrejection` and `console.error` and dumps them into `<pre id="errors">` (or `no errors`) for `--dump-dom`, on every scenario. That plus `?auto=wrapcheck` is what makes a refactor this size checkable at all.

### Verification

- 66 scenario runs (every `?auto=` mode, with and without `?long=1`, at 360, 390 and 1280px): zero runtime errors.
- The 27-scenario wrapcheck sweep is byte-identical to the pre-split run.
- `?auto=measure` (70px rows, 68px date cells), `?auto=calfit` (72px box / 72px content) and `?auto=sched-rule` all report exactly what they did before.
- Android path confirmed end to end: `npm run build` → `frida-compile` → 580,938-byte `hook.compiled.js` containing `flame-reskin-root`, `fr-cal-event-venue` and the one-per-day rule text.

### Tracked files, and one deletion

`.gitignore` now ignores `.patch-tools/*` with exceptions for `hook.src.js`, `gadget-config.json` and `probe.js`, so the Frida agent is finally in version control — this was the prerequisite the CI note above identified. `hook.js` and `hook.compiled.js` stay ignored: 90% of `hook.js` is the pasted userscript, so tracking it would put a second near-identical diff in every commit. `node_modules/` ignored, `package-lock.json` tracked.

`.patch-tools/regen-hook-script.py` deleted — `build.mjs` does its job, and having two things able to write `hook.js` is how they drift.

### Confirmed on-device (2026-09-04)

Phone reconnected over wireless debugging (`adb connect`, Xiaomi 23127PN0CG / `houji`). Rebuilt the APK from this branch's output — `npm run build` → `frida-compile` → `objection patchapk`, embedded `libfrida-gadget.script.so` byte-matching `hook.compiled.js` at 580,938 bytes, no `OutOfMemoryError` — and `adb install -r` over the existing install (session preserved, no re-login). **The reskin renders.** That is the whole build pipeline validated end to end on real hardware, not just in the harness.

Worth noting for next time: `adb logcat` had already rolled past the launch by the time it was read, so the `[flame-inject]` lines were not captured. Not worth force-stopping a working app to chase them — nothing else injects the reskin, so the UI rendering *is* the evidence the hook fired. Clear the buffer *before* launching if the log lines themselves are wanted.

The `v2026.09.03.2` release was cut from `master` before this branch, so it ships the same code, just built by hand. Branch merged to `master` with `--no-ff` as `cae8696`; `build-split` kept, not deleted.

## Cold-launch: white Salesforce loaders removed with a native cover (2026-09-04)

A screen recording of a cold launch showed three loading phases before the reskin: the RN splash, then **two white Salesforce "Loading…" cards**, then our own dark spinner. About 7.75s to content, of which ~1.75s was white flashes and ~1.1s was our spinner.

### What ships

- **A native boot cover.** An opaque `FrameLayout` tinted `--bg` (`0xFF11131A`) with a centred indeterminate `ProgressBar` tinted `--accent` (`0xFF5B8CFF`), added to the `DecorView` in the `setWebViewClient` hook — the same place, and for the same reason, as the existing `setBackgroundColor(DARK_BG)`. Removed once `#flame-reskin-root` exists. `setClickable(true)` so taps can't reach a live portal nobody can see.
- **Cached first paint on Home** (`src/persist.js`). Last launch's events and bookings are kept in `localStorage['flame-data-cache']` and painted immediately, then repainted from the network. Home is the boot tab and the only one that needs it — by the time Calendar or My Bookings is opened, Home's own revalidation has filled the in-memory cache they read.
- **Cached aura credentials.** `aura.context` and `aura.token` persist in `localStorage['flame-aura-auth']` and are tried straight away on the next launch, with one silent retry against a freshly sniffed token if the stored one is dead. Measured basis: two separately captured HAR sessions share a token, so it survives page loads.

Result on-device: cover up for ~3.2s, removed 87ms after injection. No white at any point.

### Why CSS could not do this — three failed attempts, all the same shape

Injecting a `<style>` that hides `body > *` and paints the page dark, at `onPageStarted`, then at `onPageCommitVisible`, then on a 100ms re-apply loop. Each was better and none worked, because **a style injected into a document is only as durable as that document**, and a cold launch has three of them:

1. the SSO bounce `frontdoor.jsp`,
2. **the same page after it rewrites itself** to render Salesforce's own "Loading…" card — this is the one that replaces `<head>` and throws the style away,
3. finally `/s/`.

A re-apply loop can only shorten the flash to one tick: the wipe and the repair are always in that order. The user's own framing is what settled it — `setBackgroundColor` survives because it is *native*, so the fix belongs at the same layer, above the WebView where no document can reach it.

Note the page is only ever **hidden, never blocked**. It loads completely and normally behind the cover, which is what keeps the stock-UI long-press honest: revealing it shows a live, fully-loaded page rather than a blank one.

### Traps found the hard way — do not re-try these

- **`requestAnimationFrame` will not fire under an opaque native cover.** Two nested rAFs is the correct way to wait for a paint in a browser, and it deadlocked here: Android can skip drawing a fully obscured WebView, so no frames are produced and the callback never runs. The reveal was waiting on a paint the cover itself was preventing. Use existence plus a bounded give-up instead.
- **Frida's `setTimeout` does not reliably fire when called from inside a Java callback.** A 150ms grace scheduled from within `ValueCallback.onReceiveValue` (which runs on Android's main thread, not Frida's JS thread) never ran, and the cover hung with an empty log.
- **`evaluateJavascript(js, null)` reports neither success nor failure.** Three builds were shipped blind because every `catch` around it was empty. Registering a real `ValueCallback` that returns a status string — readyState, URL, body child count, computed background — is what finally located the fault, and it took one build to do what three guesses had not. **Instrument before the second attempt, not the fourth.** Both failure paths now log.

### Tried and rejected: injecting at `onPageStarted`

Injecting the whole reskin ~1.5s earlier works and was reverted anyway.

- Measured `onPageStarted` → `onPageFinished` on `/s/` across three launches: 1.383s, 1.508s, 1.644s. Actual saving was smaller — 3432ms → 2884ms of cover time, **548ms** — because the reskin only paints at `DOMContentLoaded`, 1.18s after injection.
- **It buys nothing visible.** Behind the cover, ready at 2.9s or 3.4s looks identical. The parts that *are* visible were already taken by the two caches.
- It costs a second injection point, a delivery guard, and double-boot semantics. The guard cannot live inside the script: rollup hoists every module body into one IIFE, so `aura.js`'s `fetch`/`XHR` patching (built line 171) runs long before any flag check in `main.js` (line 2405) — a second run wraps our own wrapper. The guard has to wrap the delivery instead.

Kept for the record because the reasoning about *why* it does not pay is the useful part.

### Also settled: what the reskin actually needs from Aura

Not its UI — but not nothing, either. `aura.context` **is** in the bootstrap HTML; the **token is not** (searched all 399KB — it is a 334-char `{"nonce":…}` blob that only exists because Aura's own JS fetches it). So Aura must run; we just never have to look at it. A HAR puts the page's first tokened request at **+0.297s** after the document loads, so auth is available almost immediately — the second loader completing is irrelevant to it.

### Harness

- `?auto=wrapcheck` (+`-week`/`-cancel`/`-confirm`/`-sched`) — line counts from `Range` client rects, plus an overflow half.
- `?long=1` — pathological strings for the above.
- `?auto=calfit` — calendar block content height vs box height.
- `?auto=coldpaint` with `?slow=<ms>` and `?seed=persisted` — proves the stale paint *and* the repaint: in-flight shows `STALE …`, settled shows the real name.
- `?auto=sched-rule`, `?auto=sched-rule-cross`, `?seed=twin`, `?auto=authstate`.
- `<pre id="errors">` on every scenario, trapping `onerror`/`unhandledrejection`/`console.error`.

**Not committed until this entry**: everything above. **The `v2026.09.03.2` release predates all of it.**

## Auto-booking UI: confirm panel, daily repeat, and four rule bugs (2026-09-04)

Autobook was a single tap on a tile. It now goes through a confirm panel, can repeat daily, and several rules that only looked right have been fixed.

### Autobook confirm panel

`openAutoConfirm()` mirrors `openConfirm()` — same panel, same fields, because it ends in the same `createReservation`, just made later by the runner from what was captured. Titled `Autobook 6 AM – 7 AM`, with Purpose/Co-attendee for room-type resources. **Autobook never sent those before**: they are stored on the intent now and passed through at fire time, so a scheduled room booking carries the same details a manual one does.

Copy is deliberately one line. The first draft explained the 24h window, the retry behaviour and the lack of a guarantee in a paragraph per case; the user's verdict was "no one is reading allat". The only distinction worth the space is **tries** versus **reserves**:
- `Tries as soon as it opens. Not a reservation.`
- `Full right now. Keeps checking and books it if it frees up.` (window already open)

"Repeat daily" carries no caption at all — the two words are the explanation.

### Daily repeat

`intent.repeat === 'daily'`. A series is **not** a rule that fires forever: each occurrence spawns the next only once it has been settled, either way. Consequences worth knowing:

- **One row in the list, never a queue.** There is only ever one waiting intent per series, labelled `… · daily`, and Stop ends the series outright because nothing spawns until something resolves.
- A refusal does not end a series — tomorrow is a fresh day.
- A **daily series claims every day from its own date onward**, not just its own. Without that you could set a daily gym autobook for tomorrow and still be offered Sunday as if it were free, only for the series to arrive and take it. Those tiles read `Daily autobook`.

### Four bugs, three of them mine from earlier in the same session

1. **The one-per-day claim never ran on open slots.** It was computed as `sl.kind === 'later' ? conflictingIntent(...) : null`, so a day already claimed by an autobook still offered every bookable slot on it as if nothing were pending. The claim is a property of the day, not of the tile's kind.
2. **A daily series died silently at the first hand-claimed day.** `spawnNextOccurrence` did `return` when the next day was already claimed, and since the next occurrence is only created on settle, nothing ever restarted it: a one-off gym on Friday plus a daily from Tuesday ran Tue–Thu and then never again, stopping at exactly the booking you had made yourself, with nothing to say so. It now steps over a claimed day (up to `SERIES_LOOKAHEAD_DAYS`, 7) instead of stopping at it.
3. **`scheduled` did not compare dates**, so a series reaching forward from an earlier day made a tile on a later day offer to cancel an intent that was not its own.
4. **Caption precedence was wrong**: `blocked` was tested before `bySeries`, so a day claimed by a series showed a time belonging to a different day.

### Manual booking on a claimed day: warn, don't block

Chosen deliberately over greying it out. An open slot stays clickable; the panel says `Stops your 6 AM – 7 AM autobook — one booking a day.` and the intent really is removed on success. A second *autobook* on a claimed day is refused outright instead, because unlike a manual booking it could never succeed.

### Ask before acting

The manual panel now calls `existingBookingFor(resourceName, isoDate)` when it opens and disables submit if that resource class is already booked that day — `Already booked Gym Today · 8 AM – 9 AM — one booking a day.` Previously it sent the request and rendered the portal's refusal as a red error. A failed lookup does not block the booking: the portal is still the authority.

### Captions

`Opens now` is gone. A slot whose 24h window has opened but which the portal still is not listing now says **`Slots full`** — that case happens whenever `getResourceAvailability` returns nothing for a resource, which puts *every* known slot on the `later` path including ones inside the window.

### Process note, worth more than any of the above

One of the edit scripts in this session aborted partway through, so a set of changes reported as applied had never been written — including the `bySeries` logic. The harness caught it, but only because a test was actually run against the claim. **A script that edits several places must be verified by its effects, not by its own success message.** The same session also shipped three Android builds that were guesses, for the same reason: no instrumentation before the second attempt.

### Harness

`?auto=series-covers` (a daily series locks the following day, with the right caption), `?seed=series-clash` + `?auto=seed-result` (a series steps over a hand-claimed day), `?auto=autopanel`, `?auto=confirm-warn`, `?auto=shiftcheck` (an overlay must not move the page), `?auto=banner-anim`. Scenarios that need a bookable or schedulable tile now **hunt for a day that has one** via `findDayWith()` instead of assuming an offset — hardcoding "tomorrow" made them pass or fail depending on the hour.

## Blocked Tile Refinement & UI Polish (2026-09-04)

Refined the UX and visual layout for blocked/autobooked slots based on user feedback.

### 1. Greyed-out/Blocked Tile Captions & Notice Layout
- **Removed Redundant Line Above Notice**: Removed `resultsTitle` (`<p class="fr-book-results-title">Gym · Today, Sep 4</p>`) above the results section so the notice banner sits cleanly at the top of the slot results, eliminating the redundant title line above the notice.
- **Restored Slot Status Captions on Greyed Tiles**: Blocked tiles now display their standard status captions (`Opens in Xh`, `Slots full`, or `Auto-booking ✓`) rather than rendering an empty string, removing the awkward blank space under the time text on greyed-out slots.
- **Notice Above Grid (`.fr-slot-notice`)**: Placed once above the slot grid when a day is covered by an autobook. Styled with dedicated `.fr-slot-notice` styles (`margin: 0 0 16px; padding: 10px 14px; background: var(--bg-elevated); border: 1px solid var(--border); border-radius: 10px; font-size: 0.8125rem; color: var(--text-secondary); line-height: 1.45;`), giving it proper spacing above the grid.
- **Simplified Explanation Strings**: Simplified `blockedReason()` strings used in tooltips, tap notices, and the notice banner above the grid:
  - Daily series: `'A daily autobook already covers this day. Stop it below to book this day yourself.'`
  - One-off: `'An autobook is already scheduled for this day. Stop it below to book this day yourself.'`
- **Interactive Explanations on Tap**: Blocked tiles remain clickable (using `.is-blocked` and `aria-disabled="true"`) to trigger a self-dismissing banner notification (`showNotice`) on tap.

### 2. Future Daily Series Clash Resolution & Auto-Replacement
- **Clash Detection (`futureDailyIntents`)**: When setting up a new daily autobook for an earlier date (e.g. Sep 4), the system detects any existing daily series for the same resource starting on a future date (e.g. Sep 6).
- **Confirmation Warning**: In `openAutoConfirm`, toggling **Repeat daily** dynamically displays a warning note when a future daily series exists:
  > `Replaces your daily autobook starting Sun, Sep 6 (8:00 AM – 9:00 AM).`
- **Automatic Replacement in `scheduleIntent`**: Confirming the new daily autobook automatically removes any future daily series for that resource class before saving the new intent. Because the earlier daily series runs indefinitely, all future dates remain covered without creating clashing or redundant intents.

### 3. Impeccable Refactor of Booking Confirmation Panel
- **Integrated Header Toolbar (`.fr-confirm-header`)**: Placed the slot time title (`Autobook 8:00 AM – 9:00 AM`) on the left and the compact `Daily` toggle switch inline on the right side of the header row, saving vertical space.
- **Form Inputs Grid (`.fr-confirm-inputs`)**: Purpose and Co-attendee input fields rendered in a 2-column grid (`1fr 1fr`), collapsing gracefully on narrow mobile screens.
- **Micro-Alert Callout Box (`.fr-confirm-warn`)**: Clash warnings rendered as a styled alert box with subtle red background tinting (`color-mix(in srgb, var(--danger) 12%, var(--bg-elevated))`), 1px border, and 8px border-radius.
- **Impeccable Typography & Micro-Copy**:
  - Applied `text-wrap: pretty` to `.fr-confirm-note` and `.fr-confirm-warn` to prevent lonely single-word wrap orphans.
  - Tightened helper copy strings: `"Currently full. Auto-checks for cancellations."` & `"Attempts booking as soon as window opens."`
- **Bottom Action Button Anchor**: Primary `Autobook` / `Confirm booking` button styled as a full-width bottom anchor button (42px height, bold 600 weight).

### 4. Fix Slot Kind Classification for Future Dates (>24h)
- **Problem**: In `refreshAvailability`, slots returned in `availabilitySlots` by the portal API were unconditionally tagged `kind: 'open'`. For dates >24 hours away (where the portal's 24h booking window has not opened yet), this caused:
  1. Slots on future dates to incorrectly open normal booking (`openConfirm`) instead of autobooking (`openAutoConfirm`).
  2. Blocked checks (`sl.kind === 'later' && !!claimedBy`) to evaluate as `false`, preventing blocked/greyed-out styling from applying on future days covered by a daily autobook.
- **Fix**: Updated slot parsing to compute `opensAt = start.getTime() - BOOKING_WINDOW_MS`. If `opensAt > Date.now()` (window not open yet), the slot is classified as `kind: 'later'`. This restores correct autobooking prompt behavior for future days and ensures greyed-out blocked tiles render properly when covered by an autobook.

### 5. Input Placeholder & Two-Tap Autobook Cancel Confirmation
- **Placeholder Cleanup**: Removed `(optional)` from `Purpose` and `Co-attendee` input field placeholders for Classrooms and Conference Rooms, displaying clean `Purpose` and `Co-attendee` labels.
- **Two-Tap Autobook Cancel Flow (`is-confirming-cancel`)**:
  - Tapping a scheduled autobook slot (`Auto-booking ✓`) changes its state to `.is-confirming-cancel` with a danger tint and caption **`Tap again to stop`**.
  - A 3-second auto-reset timer is initiated. If untapped for 3 seconds, the tile automatically resets back to `Auto-booking ✓`.
  - Tapping anywhere else on the screen (outside click listener) immediately cancels the prompt and restores the tile state.
  - Tapping the tile a second time while in `is-confirming-cancel` state confirms intent removal (`removeIntent`) and refreshes availability.

### 6. Gyan Tab Loading & Welcome Text Updates (`gyan-updates` branch)
- **Welcome Explanation Cleanup**: Cleaned `welcomeMessage` and `introductionText` fallbacks to sanitize stock Salesforce/Aura references to non-existent "Start chat" buttons, presenting clean welcome text ("Ask about classes, facility bookings, campus info, or anything else Gyan can help with.").
- **Non-Blocking Tab Mount**: Removed full-page loading spinner on tab mount. `renderGyan` paints the UI layout synchronously, initiating background context initialization without hiding the page.
- **Interactive Composer Textbox**: Composer `inputEl` remains **enabled and interactive at all times**, allowing users to type their queries immediately while context initializes or while an assistant turn is running.
- **Send Button Loading Spinner**: `sendBtn` displays a greyed-out spinning loader icon (`.fr-spinner-svg`) while initializing or waiting for assistant responses, disabling submission until ready while leaving the input field free to edit.

### 7. Smart Time-of-Day Gyan Prompt Chips (`gyan-updates` branch)
- **Time-Aware Mess Meal Intelligence (`getSmartGyanChips`)**:
  - `00:00 - 10:59`: Prompts **Breakfast today** & **Lunch today**.
  - `11:00 - 15:59`: Prompts **Lunch today** & **Dinner tonight** (skipping morning/past meals).
  - `16:00 - 18:59`: Prompts **Snacks today** & **Dinner tonight**.
  - `19:00 - 23:59`: Prompts **Dinner tonight** & **Tomorrow's breakfast**.
- **Contextual Chips**: Always includes **Next class** and **Sports slots** chips alongside meal chips.
### 8. Basic Asterisk Bold Support & Thread Disposal Guards (`gyan-updates` branch)
- **Sanitized Bold Parsing (`parseGyanBold` & `escapeHtml`)**:
  - Converts `**bold**` or `*bold*` to `<strong>bold</strong>` rendered in bold weight (`font-weight: 700`).
  - *Known Limitation Note*: On certain devices/browsers, asterisk-wrapped text might not render visually bold if system font weights do not support standard 700 weight variants.
- **In-Flight Turn Cancellation & Disabled "New chat"**:
  - The **New chat** header button is disabled (`opacity: 0.4`, `pointer-events: none`) while Gyan is initializing or generating a response (`gyanState.sending`), preventing broken thread states.
### 9. Slot Selection Smooth Auto-Scroll
- **Selection Auto-Scroll**: Tapping any valid open or schedulable slot brings the confirm / autobook button into view. Both the inconsistency noted here and the picker highlight in item 10 are resolved — see the entry below.

### 10. Custom Picker Option Highlight Behavior (`.fr-picker`)
- Selecting an option left the blue `.is-selected` highlight on the previously selected row. Resolved — see the entry below.

## Confirm-button scroll and picker highlight, both fixed and both measured (2026-09-04, later)

The two open notes above. Neither was as vague as "inconsistent" suggested; both had one specific cause, and both are now asserted by numbers in the harness rather than eyeballed.

### The scroll: `scrollIntoView` was the wrong instrument, and `requestAnimationFrame` was the wrong clock

Three separate faults, all in `scrollToConfirm`:

1. **`block: 'end'` aligns with the bottom of the scrollport, which on mobile is exactly where the fixed nav bar sits.** The button was scrolled to precisely the place something covers. `block: 'center'` (the previous commit's answer) does clear the bar, but it also moves the grid much further than it needs to from most slot rows, and it still can't account for a bar whose height includes `--flame-navbar-inset`.
2. **`scrollToConfirm` was declared inside `openConfirm`, and `openAutoConfirm` called it.** A function declaration is scoped to the function it is declared in, so the autobook panel had been throwing `scrollToConfirm is not defined` and scrolling nowhere at all. Confirmed against the HEAD build in the harness: `[errors] Script error.`, `scrollTop=0`.
3. **`requestAnimationFrame` never fired.** The rewrite initially measured from two nested rAFs — the textbook way to wait for layout — and instrumentation showed the callback simply never ran: `log: ["called"]` and nothing after it. Same starvation as the cold-launch cover: this renderer services no frames when it decides it has nothing to paint. rAF is not a dependable clock in this app; the cold-launch entry above says so, and it applies here too.

What ships instead (`scrollConfirmIntoView` + `confirmScrollDelta`, module scope in `src/tabs/book-slot.js`, called from both panels):

- **Go to the bottom, not the minimum distance.** The first rewrite travelled exactly far enough to reveal the button, which passed every check and still felt wrong — the panel is the last thing on the page, so opening it should land at the end of the page. `scroller.scrollTo({ top: scrollHeight - clientHeight })`. This is safe against the nav bar because `.fr-content`'s bottom padding (96px + `--flame-navbar-inset` on mobile) is larger than the bar, so the end of the content parks ~47px above it rather than behind it.
- **Verify rather than trust that.** `confirmScrollDelta` still exists and still runs: it reads the scroller's rect, subtracts the nav bar's height **only when its computed `position` is `fixed`** (the desktop bar is in flow and covers nothing), and returns the distance needed to put the button inside what's left, with a 12px gap. The bottom is the intent; the button being visible is the requirement, and if a future layout ever puts the two in conflict this is what keeps the button reachable.
- **Issue it synchronously.** The panel is already in the document when this is called and reading layout flushes it anyway, so a frame adds nothing — and, per fault 3, may never arrive.
- **Check the landing.** Smooth scrolling is a request: Android WebView drops it some of the time, headless Chrome ignores it outright, and the grid above can reflow after the panel opens, moving the bottom after the scroll was aimed at it. So 400ms later, anything short of the bottom is finished without animation — **unless a `touchstart` or `wheel` arrived first**, in which case the user has taken over and their scroll wins.

### The picker: the highlight is painted from `value`, and the click handler never repainted

`buildPicker`'s option click set `value` and the button label, then closed the panel. `paintPanel()` — the only thing that writes `.is-selected`/`aria-selected` — was called from `setOptions` and the `value` setter but not from the click path, so the panel kept the old row highlighted until something else repainted it. One `paintPanel()` call in the handler fixes it. `openPanel` now also scrolls the selected row into view, which matters for Class Rooms' 13 resources against the list's 280px `max-height`.

### Harness

Two new scenarios, both reporting PASS/FAIL with the numbers they judged on:

- `?auto=confirm-scroll` and `?auto=confirm-scroll-auto` (manual and autobook panels) — nav height, view band, button band, and `scrollTop` against the scroller's maximum, so it reports both `at bottom` and whether the button is clear. PASS at 390x780, 360x640, 360x500 and 1280x900, with and without `?long=1`. The autobook variant is the one that catches fault 2.
- `?auto=picker-reselect` — picks a different option, **reopens**, and asserts exactly one `.is-selected` and one `aria-selected`, on the option just picked. Reopening is the point; the stale highlight was only visible on a second open.

Both need `?tab=book-slot`. Full sweep of all 39 scenarios at 390 and 1280 (each on its own tab): zero runtime errors.

### Rebuilt and installed on the phone (2026-09-04, same session)

Full pipeline, exactly as documented above: `npm run build` → `frida-compile` → `objection patchapk` → `adb install -r` over the existing install, session preserved. `lib/arm64-v8a/libfrida-gadget.script.so` inside the output APK matches `hook.compiled.js` byte for byte (628,445 after the scroll-to-bottom change; 627,938 on the first build of the day). Copied to `MyFLAME-reskin-working.apk` and pushed to `/sdcard/Download/MyFLAME-reskin.apk`. Confirmed working on the phone by the user.

Logcat cleared *before* launching, which is what the previous entry said to do, so the injection line is finally in evidence: `hook installed` → `setWebViewClient` → `boot cover shown` → `onPageFinished /s/` → `injecting reskin` → `status bar overlay painted, height=134` → `navbar inset pushed: 48px device` → `boot cover removed (ready, 4650ms)`.

**Working rule set here, and it applies to every future rebuild**: this depth of checking is for troubleshooting only. A minor change gets built, installed and handed over — no full scenario sweep, no `force-stop` and relaunch, no logcat grep, no confirmation screenshot. The pipeline is known-good and the phone is in the user's hand; re-proving injection on every rebuild answers a question nobody asked.

**One thing to know about the dependencies**: `frida-compile` and `frida-java-bridge` are now declared in the root `package.json` under `dependencies` and committed. That install happened outside this session's commands, but it matches where the binaries actually resolve from (`<repo>/node_modules`) and makes the pipeline's requirements explicit instead of relying on a stray `npm install` somebody ran once, so it is kept.

### Pushed and released as `v2026.09.04.1`

`master` was pushed and the APK attached to a GitHub release: https://github.com/rk4500/myflame-portal-reskin/releases/tag/v2026.09.04.1, built from `d4a18ca` (the scroll + picker commit). The earlier `v2026.09.04` from the same day is one build behind it, from `1ee6fc8` (the autobook confirm panel work). `v2026.09.04.1` is the newest release and the build currently on the phone's `/sdcard/Download/MyFLAME-reskin.apk`.

## Skeletons on Home and Book Slot, and a repaint that stopped happening (2026-09-04, branch `skeleton-loaders`)

### The rule the skeletons follow

A skeleton is the **real element** with its unknown text swapped for a shimmering block of the same metrics — never a lookalike assembled from bare divs. `.fr-skel` is an inline-block holding a non-breaking space, so its line box is exactly one line of whatever type its parent uses: same font, same line-height, same baseline. Width is the only thing a caller passes, in `ch`, and it is a guess about content length that nothing in the layout depends on. That is what makes the loading and loaded screenshots line up row for row instead of settling into place.

Shimmer is a gradient sweep on `background-position`, 1400ms linear, disabled under `prefers-reduced-motion`.

### Home

- **Cold start only** — the title, the date strip and both headings need no data at all, so they are real from the first frame; three class rows and one booking row shimmer. Three, because that is a normal day and a skeleton that guesses high leaves a hole when the data lands short. The strip's has-events dots are the one thing that appears late, since which days carry classes is exactly what is not known yet.
- **The jitter is gone.** The stale cache paints, the request still goes out, and the answer is now compared against what is on screen (`sameData`, in `persist.js`) and **dropped when identical**. The timetable is semester-static, so the common case was tearing down the entire page to rebuild the same page a second after the tab opened. That was the jitter.
- **Upcoming bookings no longer disappears when empty.** It used to be omitted entirely, which left the page looking like it had ended early. It now says nothing is booked and offers the Book Slot tab — `renderEmpty` takes an optional `{ label, onClick }` action for this.

### Book Slot

The whole tab used to wait on `getResources` before painting a single pixel, and everything above the slot grid is drawn from that one list.

- **The resource list is cached on its own clock.** `flame-resources-cache`, 7-day TTL, separate from Home's blob because the two expire for different reasons: a day-old class list is nearly right, and a week-old resource list is simply correct — the gym and the classrooms do not change during a semester. Cached list paints immediately, the request still goes out, and only an answer that differs touches the UI (a plain repaint, since a renamed resource changes the rail and the picker together).
- **The slot grid's times are not a guess.** `knownSlotTimes(resource)` already derived them from the resource's own operating window — `Gym ( 6:00 am to 2:00 pm slot )` is eight hourly slots — falling back to whatever that resource last offered. So the grid is drawn with real labels in the real number of cards, and only the capacity line, the one thing that genuinely needs the server, shimmers.
- **The cards are filled, not rebuilt.** `createSlotShell` builds the box; when the answer lands those same nodes are looked up by `dataset.start` and mutated in place. Nothing about the grid moves. A card is only created late when the skeleton could not predict it, and anything predicted that the day did not have is dropped.
- **Past times are not predicted.** On today, a slot that has already started cannot be booked and the portal does not list it, so a skeleton built from the full operating window drew cards that vanished when the answer landed — open the tab in the afternoon and half the grid disappeared. The predicted list is filtered by the same `start > now` predicate the known-slot fill already used, so the skeleton grid is the size the real grid will be. Late enough in the day that nothing is left, there is nothing truthful to draw and it falls back to the spinner, then to "No open slots".
- **The claim note and the scheduled-autobook list are in the first paint too.** Both come from intents in `localStorage`, not from a request, so holding them back only meant pushing the grid down a line at the worst moment.
- The spinner is still the honest answer in the one case where nothing can be predicted: a resource with no operating window in its name and nothing remembered.

### Answered by the user, not assumed

Resource/day switches inside the tab get the **same skeleton every time** (not a dimmed hold, not a delayed one). Placeholders **shimmer** rather than pulse. And the empty bookings section should stay on the page with something to say, which is where the empty state's copy and its Book Slot button came from.

### Verification

Screenshots at 390x780 and 1280x900, loading against settled, on both tabs: rows and cards land in identical positions. Both start paths were checked, not only the empty one — cold (no cache, everything shimmers) and **warm** (cache present, which is what a real launch hits and where a mismatch between the predicted grid and the real one would show). One defect found and fixed in that pass — skeleton cards inherited `.fr-slot:disabled { opacity: 0.4 }`, which is the *blocked* look and also dimmed the card's time, the one thing already true. Targeted scenarios on the touched paths (`book-confirm`, `resource-picker-open`, `confirm-scroll`, `picker-reselect`, `autopanel`, `measure`): no errors. Impeccable's mechanical detector over the changed files: clean.

Built and installed on the phone. **Not merged and not pushed** — `skeleton-loaders` is local-only, has no upstream, and nothing here is in a release; `master` still ends at `d4a18ca` / `v2026.09.04.1`. Merging and cutting a release is the next step whenever the user says so.
