# Portal Reskin — Handoff

Custom frontend for the college portal (`my.flame.edu.in`), own account, personal use. Two fronts: a Tampermonkey/Violentmonkey userscript on desktop, and the Android app with the same script injected into its WebView by an embedded Frida gadget. Both ship the same `portal-reskin.user.js`.

This file is organised by subject, not by date — dates are kept on individual findings. It records root causes, things tried and rejected, and the state of anything unverified. It is not a changelog; the releases carry that.

## Current state

- `master` ships everything described here. `v1.1.0` carries the swipe-nav rework in full: the dual-pane crossfade, the live 1:1 drag preview, the touch-`:hover` fix, and the long-press settings menu — see the Shell section for all of it.
- The APK on the phone is `v1.1.0`, live-verified there this session (nav gestures, the settings menu, the touch-`:hover` fix). `v1.0.2`/`v1.0.3` were never confirmed on-device (built and released with no device attached) — `v1.1.0` is the first build since `v1.0.1` actually installed and used.
- Live-verified: the whole Android injection path, the reskin rendering, Gyan's thread creation, booking and cancellation, and (this release) the swipe/nav rework.
- Not live-verified: the autobooking 24h model (see its section), and the login page, which the script deliberately never touches.
- **Slow, deliberate swipes are still unreliable, genuinely unresolved** — see "Swipe: slow drags" in Shell. Fast flicks and nav taps are unaffected; `settings.motion` off (via the new long-press → Settings menu) reverts tab switching to no sliding at all if the swipe UX isn't wanted regardless.
- `src/settings.js` is new: a small persisted-settings stub, one flag (`motion`) so far, reachable from mobile's Home long-press → Settings.
- `.patch-tools/fast-rebuild.sh` is new (see "Source layout and build" / the patch-tools section) — cuts a script-only patch iteration from ~48s to ~7s by reusing a persistent decompiled+injected directory instead of re-running apktool's decompile step every time. Only valid until `flame-merged.apk` itself changes.
- Branches kept on purpose, not to be deleted without asking: `rail` (rejected design, below), `build-split`, `skeleton-loaders`, `gyan-updates`.

## Working rules

- **Never `git commit` or `git push` unless told to in that message.** Editing, staging, building and installing are fine unasked. Also in `CLAUDE.md`.
- **Verification depth scales with the change.** A minor change gets built, installed and handed over — no scenario sweep, no `force-stop` and relaunch, no logcat grep, no confirmation screenshot. The pipeline is known-good and the phone is in the user's hand. The deep checks below are for troubleshooting.
- **Instrument before the second attempt, not the fourth.** Three Android builds shipped as guesses because every `catch` around `evaluateJavascript` was empty. One build with a real callback found the fault.
- **A script that edits several places is verified by its effects, not by its own success message.** One edit script aborted partway and reported success; a set of changes believed applied had never been written.

## Platform: Salesforce Experience Cloud ("Siteforce")

All traffic is one RPC endpoint. There is no REST API on this domain — `/services/data/…` is absent, so don't go looking for an OAuth route.

- `POST /s/sfsites/aura?r=N&aura.ApexAction.execute=1` (`r=` is a request counter, ignore).
- Params are exactly `message`, `aura.context`, `aura.pageURI`, `aura.token`. `callingDescriptor` is literally `"UNKNOWN"`.
- `message` is URL-encoded JSON: `{"actions":[{"id":"…","descriptor":"apex://Controller/ACTION$method","params":{…}}]}`. **A no-arg method omits `params` entirely** — sending `{}` risks an Apex deserialization error.
- Response is plain JSON (no `while(1);` prefix): `{"actions":[{"id":"…","state":"SUCCESS","returnValue":{…}}]}`, matched by `id`. The real payload is **`returnValue.returnValue`** — missing that unwrap silently broke every tab once. Some payloads are **double-JSON-encoded** (the returnValue is itself a JSON string).
- Auth is `aura.context` + `aura.token`. `callAura()` does **not** parse the page bootstrap: it hooks `fetch` and `XHR.send` and sniffs the page's own outgoing requests, which self-heals across token rotation. Both are cached in `localStorage['flame-aura-auth']` and tried immediately on the next launch, with one silent retry against a freshly sniffed token if the stored one is dead (two separately captured HAR sessions shared a token, so it survives page loads).
- **What the reskin needs from Aura, settled 2026-09-04**: `aura.context` *is* in the bootstrap HTML; the **token is not** — searched all 399KB, it is a 334-char `{"nonce":…}` blob that exists only because Aura's own JS fetches it. So Aura must run; we just never look at its UI. A HAR puts the first tokened request at **+0.297s** after document load.
- The token expires; a rotation or an `INVALID_TOKEN` reply is recovered by re-sniffing. Chrome-hiding is structural, not by class name — `.siteforceLayoutDiv` and friends never appear in the HTML at all, since the page is a pure Aura SPA shell (`<body class="null loading">`) with no app markup until client-side render. So: `body > *:not(#flame-reskin-root) { display: none }`, with a `MutationObserver` re-hiding across the SPA's re-renders and re-appending root if Aura wipes `<body>`. The original page is hidden, never removed — which is what keeps the stock-UI toggle honest.

### Descriptors in use

- `StudentPortalCalendarCtrl.getAllScheduledEvents({userId})` — class schedule.
- `CustomBookingController.getResources()` — facility list, double-encoded.
- `CustomBookingController.getResourceAvailability({resourceId, bookingDate})` — slots, double-encoded.
- `CustomBookingController.getReservations({userId})` — the user's bookings.
- `CustomBookingController.createReservation({dateSelected, startTime, endTime, resource, bookingPurpose, coAttendee, userId})` and `cancelReservation({userId, bookingId})`.
- Mapped but unused: `PortalEventController.getAllEvents`, `PortalAllFeedsCtrl.getAllEvents`, `PortalCreateServiceAndSupportTicketCntrl.getCases`, `NavigationMenuItemsController.getNavigationMenuItems`, `ResourceController.getCategoriesWithResources`. Full shapes in `aura_map.json` / `aura_map.md` (29 calls).

**`userId` gotcha**: these methods want a Salesforce **User** id (`005…`). `PortalAllFeedsCtrl.getCurrentUserContactId` looks like the obvious call and returns a **Contact** id (`003…`), which is not interchangeable. `resolveUserId()` instead regexes `"The Salesforce user record Id is 005…"` out of `AiAssistantFlameCommunityWrapper.getAssistantContext()` (cacheable, no params).

**Booking replies are prose, and a refusal is still `state: SUCCESS`** — e.g. `"More than 1 bookings are not allowed for selected resource."` arrives with no exception, so a try/catch never sees it. Success is detected by a real booking id (`/\bR-\d+\b/`); anything else renders in `.fr-error-panel` rather than `.fr-success-panel`, and re-enables the submit button (it used to stick on "Booking…" forever, since an awaited call succeeding was taken to mean the booking had). `startTime`/`endTime` are passed through verbatim from availability, in that format (`"6:00 AM"`). `bookingPurpose`/`coAttendee` were empty in the capture — treated as optional, sent as `""`.

### Gyan's protocol

Gyan is the portal's own assistant. Its calls use `namespace: "vnai"` (`callAura()`'s optional 5th param).

- Setup: `AiAssistantWindowController.getAssistant({assistantName:"Gyan"})` → `assistantId` + welcome text; then a thread; `getAssistantContext()` supplies `threadContext` + `additionalKnowledgeFileIds` (shared with `resolveUserId()`, one cached fetch).
- **`getUserThread`'s `createIfNotExists` flag is a lie** — it never creates anything, returning `{}` either way. Real creation is a separate method, `createNewThread({assistantId, assistantName, actorId})`, whose response is the **new threadId as a bare string**, not `{threadId: …}`. `acquireGyanThread()` looks up first and creates only on a genuine miss. This was root-caused from a HAR of the stock UI's own "Start chat" click, after two wrong theories (re-running setup each turn — kept, but insufficient; replaying a two-call `getUserThread` sequence — simply wrong).
- **`deleteThread` is genuinely destructive**, confirmed by the same HAR's "End chat". It is wired only to the explicit **New chat** button, never to a generic error handler. An earlier auto-delete-on-error very likely destroyed the user's original thread.
- Per message: `runModeration({message})` (fire-and-forget gate; no rejection shape was ever observed, so failures are swallowed) then `runAssistant({runRequest:{…}})`.
- **`runGyanTurn()` is a tool-calling relay loop, not a round-trip.** The server runs its own tools and hands back the computed result in `requiredActions[].functionResponse`; the client echoes each straight back as a `toolResponses` entry (`{name, arguments, callId, response}`) in the next `runAssistant` call, until a response carries real `text`. A `transfer_to_*` action (agent handoff) also carries a `userQuery` in its `functionArgs`, which becomes the *next* call's top-level `message` instead of the usual empty string — "book me a gym slot" bounced through two transfers before any tool ran. Capped at 8 steps.
- `ensureGyanReady()` runs at the top of `runGyanTurn()` itself, not just on tab mount — trusting the caller is how a send went out with `threadId: null` and got `"List index out of bounds: 0"`.
- State updates land unconditionally; only the DOM repaint is gated on `token === activeToken`, so switching tabs mid-reply and coming back shows the finished exchange instead of a stuck "thinking". Worth copying for any other slow async tab.

## The reskin

`@match https://my.flame.edu.in/*`, `@grant none` — runs in the page's own context and inherits the session. Five tabs: Home, Calendar, My Bookings, Book Slot, Gyan.

### Shell

- Desktop is a horizontal top bar (was a left sidebar); mobile keeps the bottom tab bar. Bar padding matches `.fr-content`'s own 48px so their edges line up. The old "FL" initials square read as an account switcher; an authored flame glyph + "FLAME" wordmark reads as a logo.
- **The Stock-UI toggle has no permanent mobile chrome.** A desktop top-bar button; on mobile, a 550ms long-press on the Home tab (`attachHomeLongPress`) with a background fill whose transition matches the hold delay, `navigator.vibrate(15)`, and the resulting tap suppressed via a capture-phase `stopImmediatePropagation`. An intermediate version put a fixed 52px `.fr-mobile-topbar` on every mobile tab to host the button; the user's verdict was *"thats disgusting bro."* — the structural argument (reserved space beats a floating overlay) was right about the collision and wrong about the cost. The hold itself is unchanged; what it *does* changed — see the long-press menu below, which replaced the direct `setReskinOff(true)` call this bullet originally described.
- **The long-press now opens a small menu instead of switching straight to the stock UI** (2026-09-10, `toggle.js`): "Original UI" (same destination the hold used to go to directly) and "Settings" (new — the only way to reach `settings.motion` on mobile, where there's no other spare chrome for it). One tap further to the same place, in exchange for Settings having somewhere to live. Built once and reused across opens rather than rebuilt per press — there's nothing per-press about its content.
  - **Can't be a DOM child of the nav button** — nested `<button>`s are invalid HTML and unreliable to click — so it's a sibling appended to `ui.root`, positioned with `position: fixed` from the trigger's own `getBoundingClientRect()` at open time rather than a CSS anchor. No router, so "Settings" doesn't open a second layer: it swaps the same panel's content in place (`menuView`), which is also why there's no back button — closing and reopening (the same long-press) resets to the root view, and a second layer would have been more chrome than one switch is worth.
  - **The trailing click that releases the long-press outraced the menu's own outside-click listener.** Lifting the finger after the hold still fires a real `click`, arriving *after* `openMenu()` has already registered `document`'s capture-phase outside-click handler — and `document` is visited before the target in capture phase, structurally, so the *existing* `firedLongPress`/`stopImmediatePropagation` guard on the button (which stops the trailing click from re-triggering Home's own navigation) can't reach back far enough to stop this too; it runs too late in the same phase. Fixed by never treating the very next document click after an open as a dismiss (`suppressNextDocClick`) — the only click that could land in that window is the gesture's own trailing one, since the finger can't produce a second click without lifting first.
  - **Motion is a small scale-and-fade from the corner nearest the button, not a slide** — `transform-origin: left bottom`, `scale(0.92)→1` + opacity, ~160ms. Matches the app's existing "no lateral motion for chrome that isn't a tab transition" language (the nav pill's own reduced-motion handling, `.fr-confirm-panel`'s entrance) rather than borrowing the crossfade's sideways language for something that isn't a page.
  - **`buildSwitch` moved from `shell.js` to `dom.js`** so `toggle.js` (which needed it, for the Settings view's toggle) could import it without creating a cycle — `shell.js` already imports from `toggle.js` (`attachHomeLongPress`, `buildStockToggle`), so the reverse import would have been exactly the kind of cycle this codebase has one war story about already (see "Source layout and build"). `dom.js` has no imports of its own and already held the other small DOM primitives (`el`, `skel`, `morphHeight`), so it was the natural landing spot, not a new abstraction.
  - **`settings.motion` off reverts tab switching to how it worked before any of this session's rework, not a zero-duration version of it.** `transitionToTab()` calls plain `switchTab()` directly, skipping `crossfadeToTab()` entirely — one pane, no second pane ever created, exactly the pre-crossfade code path. `attachSwipeNav`'s `pointerdown` bails immediately when the setting is off — no drag-tracking, no neighbour preview, nothing — so the gesture doesn't exist at all rather than existing-but-instant. The only motion left with it off is `.fr-page`'s own existing fade-in, which was never part of what the setting turns off.
  - **Two things tried and reverted the same session, worth not re-trying:** equal-width nav buttons (`flex: 1` on `.fr-nav-btn`, to stop the shared pill's width changing per tab) looked right at first but "My Bookings" — the longest label — hit the flexbox `min-width: auto` trap and overflowed into the next button's box even after an attempted `min-width: 0` fix; visually indistinguishable from the wrong tab being highlighted. Reverted outright rather than chased further. Separately, giving `.fr-nav-pill` an explicit `top/bottom: 4px` inset (to fix what looked like a full-height highlight) was based on a wrong reading of the cascade — `.fr-nav-list` uses `align-items: center`, not `stretch`, so it was never actually full-bar-height to begin with, and the "fix" address a problem that didn't exist. Reverted to the original `top: 0; height: 100%`.
- The toggle's state is `localStorage['flame-reskin-enabled']`, applied as a `body.flame-reskin-off` class. The off-state fallback pill (`#flame-reskin-toggle`) lives outside `#flame-reskin-root` and is `display:none` except under `body.flame-reskin-off` — it is the only way back *on* from the stock page, and since it only exists once root is hidden it cannot collide with anything of ours. An early version of the off-rule had **lower specificity** than the base hide rule, so the toggle relabelled itself and revealed nothing; caught by scripting a real click, not by reading the markup.
- Tab switching guards against a stale response arriving after the user has moved on (`activeToken` in `switchTab()`).
- **`icons.book` was a clock (2026-09-04).** Keyed for the Book Slot tab it was drawn for, but the geometry is a stopwatch — circle, hands, crown — so Home's nothing-state and Gyan's `Sports slots` chip were asking for a book and rendering a clock too. Renamed `clock` across all six call sites; the glyph is unchanged, only the name is now true.
- **The active-tab highlight is one shared pill, not a per-button background** (2026-09-10). `.fr-nav-pill` sits absolutely positioned inside `.fr-nav-list`, inserted before the buttons in DOM order so it paints behind them without a `z-index` (an absolutely positioned box otherwise sits above static siblings regardless of source order, which is why `.fr-nav-btn` also carries `position: relative` — same auto stacking level, so DOM order decides). `positionNavPill()` in `shell.js` reads the active button's own `offsetLeft`/`offsetWidth` and slides the pill's `transform`/`width` to match, called from `switchTab()` right after `.is-active` moves. It skips the slide (`animate: false`) on the very first paint and on a breakpoint resize, both moments a jump would read as a glitch rather than a state change — there's nothing to slide *from* on first paint, and a resize means the bar itself just became a different shape (top bar ↔ bottom bar).
- **Swipe left/right on the content pane switches tabs, touch/pen only** (`attachSwipeNav` in `shell.js`). Direction-locked against a 14px move (was 10px, see "Swipe: slow drags" below) so `.fr-content`'s own vertical scroll is never mistaken for a swipe attempt, and a touch starting inside a genuinely horizontally-scrollable child (`.fr-cal-scroll`, Calendar's week view) is ignored outright so that element keeps its native panning untouched. Committing needs either 72px of drag or a fast-enough flick (`0.5px/ms`); rubber-bands (`×0.35`) past the first/last tab rather than wrapping or stopping dead. A mouse drag is left alone — desktop already has the top bar one click away, and hijacking a mouse-drag would fight text selection.
  - **First pass raced the browser's own scroll gesture and lost, silently.** `.fr-content` had no `touch-action` set, so on a slow, deliberate swipe the browser's default vertical-scroll handling could commit to the gesture before this code's `preventDefault()` — which only runs once the 10px axis-lock decides the drag is horizontal — ever got a chance to run; per spec, a `preventDefault()` called after the browser has already started its own default action for a touch sequence does nothing. A fast flick "worked" purely by accident, finishing before the browser's decision landed. Losing the race mid-gesture left `.fr-content` stuck mid-overscroll, with the page title pushed up above the visible frame — reported by the user as "the title is hidden," diagnosed from the code (this is a real touch-timing race, not something the local Chromium harness's synthetic `PointerEvent` dispatch reproduces) rather than from a screenshot. Fixed by setting `touchAction = 'pan-y'` at `pointerdown`, before any move is seen (on `ui.viewportEl` now, the stable element the listener actually binds to — see the crossfade rewrite below; it was still the single content element at the time this bug was found): native vertical scroll still runs entirely untouched (never involves this code at all), while horizontal panning was never a candidate the browser could commit to in the first place, so this code's own `preventDefault()` always lands in time regardless of how slow the drag is.
  - **A tap left a second highlight stuck on the wrong button.** `*:focus-visible`'s accent outline (base rule, `#flame-reskin-root *:focus-visible`) follows whichever element actually has focus, independent of `.is-active`/the pill. Every tab switch used to be a click on the very button that became active, so focus and the pill were always the same element and the ring was invisible under/around the same highlight. Swipe changes tabs without ever touching a nav button, so the ring stayed stuck on whichever button was tapped *last* while the pill correctly moved to wherever the swipe actually landed — two different buttons visibly lit at once, exactly as reported. `switchTab()` now blurs `document.activeElement` when it's a `.fr-nav-btn`, on every switch: the pill and the label colour already say which tab is current, a focus ring left over from a tap two tabs ago doesn't need to keep saying it too.
- **A nav tap gets the same directional slide a swipe does, collapsed to one motion regardless of tab distance** (`transitionToTab()` → `crossfadeToTab()` in `shell.js`). Direction comes from comparing tab order, not a finger: moving to a later tab exits left, an earlier one exits right — same sign convention a swipe's own `translate` already uses, so both paths share one `crossfadeToTab()`. Jumping several tabs at once (Gyan to Home, say) still plays as one exit and one entrance: nothing in between is ever rendered, since the mechanism only ever touches two panes' transforms and is ignorant of how far the index actually moved. Tapping the already-open tab is a no-op transition (`fromIdx === toIdx`) — `switchTab()` still runs, since that's what re-fetches, there's just nothing to slide.
  - **First version (2026-09-10) was exit-then-enter on one shared element, and the user called it "significantly slower" than the old tap-only nav, with a visible blank gap.** The current page animated fully off-screen (200ms), the container was snapped, transition-less, to the opposite edge, *then* `switchTab()` ran and slid the next tab back in (220ms) — two sequential animations with the pane sitting fully off-screen, showing nothing, in between. Root cause: rendering only started after the old page was already gone, so the render's latency (plus the snap itself) was pure dead time no matter how fast the render actually was.
  - **The fix is two real panes, not one.** `ui.contentEl` is no longer the stable element swipe listeners bind to — it's a pointer to *whichever pane is current*, reassigned every tab-to-tab move; `ui.viewportEl` (new, `position: relative; overflow: hidden`) is the stable one now, and holds one or two absolutely-positioned `.fr-content` panes. `crossfadeToTab()` creates a fresh incoming pane off to the side, renders the next tab straight into it (every tab renderer already writes to `ui.contentEl.replaceChildren(...)`, and reads it fresh each call, so repointing it before rendering is transparent to every tab file), then slides the outgoing and incoming panes past each other in one continuous motion — the outgoing pane continues smoothly from wherever a drag released it rather than snapping first. `attachSwipeNav` binds to `ui.viewportEl` but drags whichever pane `ui.contentEl` pointed to at the gesture's own `pointerdown` (captured once per gesture into a local `pane` var — `ui.contentEl` only ever changes *between* gestures, once a commit's `crossfadeToTab()` has already finished the one in progress).
  - **First cut of the fix assumed every renderer paints synchronously before its first internal `await`, and that assumption was simply wrong** — true for Home (reads warm cache), false for My Bookings, Calendar and Book Slot (all `await resolveUserId()`/`callAura(...)` before touching the DOM at all). Sliding the incoming pane in without waiting on the render showed an empty, backgroundless pane — reported as "the background becomes transparent and just shows page 3 all the time" going Home → My Bookings — that only got real content afterwards, with no animation on that part. Fixed by actually `await`ing the render before starting the slide; nearly free in the common case since the data it awaits is normally already warm by the time a second tab opens, and honestly slower only in the genuinely-uncached case, which is when waiting is correct anyway.
  - **The same fix incidentally caught a second, unrelated bug: Gyan never appeared at all.** `renderGyan` was the one renderer that wasn't `async`, and the pre-`await` version of `crossfadeToTab()` called `.catch()` straight on its return value — `undefined` for renderGyan — which threw synchronously and aborted the function before the slide-in ever ran. Switching to `await RENDERERS[nextId](token)` inside a `try` fixed it regardless of what the renderer returns, but `renderGyan` was also made `async` to match the other four — nothing about its body needed it, it was purely to remove the one inconsistent signature that had already caused a real bug once.
- **A swipe's neighbour tab renders and slides in live, 1:1 with the finger, not just after release** (2026-09-10, third pass — `buildNeighbor()`/`commitToNeighbor()` in `shell.js`). The moment axis-lock decides a drag is horizontal, direction is already knowable from `dx`'s sign alone, so the neighbour tab (`idx ± 1`) is rendered into a real, frozen (`.fr-content--frozen`, `pointer-events: none` — it's a preview, not something tapped yet) pane positioned just off the correct edge, and every `pointermove` after that updates *both* panes' transform in lockstep, a constant one-width offset apart. On release, `commitToNeighbor()` re-renders with a fresh token (cheap idempotent repaint if the preview's own render already landed, the real paint if it hadn't) and hands off to the same `settlePanes()` a tap crossfade uses. Rendered with the *current*, unbumped token while it's just a preview — building one is not a navigation, so `currentTab`/`activeToken`/the pill stay untouched until an actual commit.
  - **Same root cause as the second pass, one layer earlier.** The first cut of this assumed the preview's own render would have painted *something* real by the time it needed to be dragged into view — true often enough to look like it worked, false in general for the same reason as before (three of five renderers await before their first paint). Not actually a live bug by the time it shipped, since `commitToNeighbor()`'s re-render already covers it, but the reasoning is the same lesson repeated.
  - **Known, accepted gap: reversing direction hard enough mid-drag to want the other neighbour isn't supported.** Only one preview is built per gesture, fixed at axis-lock; a commit attempt past that point just fails closed (no `neighbor`, treated as a non-commit, springs back). Two rapid, opposite-direction gestures back to back before the first preview's render has resolved is the same family of gap and similarly unhandled — rare, and whatever stray content it could flash mid-drag is overwritten the moment a real commit's own authoritative render runs.
- **Swipe: slow drags remain genuinely unreliable, unresolved.** First theory was the axis lock's `|dx| > |dy|` comparison — even odds by default, and a slow drag's first several px carry more incidental vertical wobble than a fast flick's clean, committed line, so losing that coin flip locks the whole gesture to `'y'` with zero visible response (no spring-back either, since `axis === 'x'` gates everything past the lock). Biased the comparison toward horizontal (`SWIPE_AXIS_BIAS`, now 0.45 — dx only needs to clear 45% of dy) and widened the lock to 14px, reasoning a slow drag's first few px are a noisier sample. **Real screen recording with "Show taps" on disproved this as the (sole) explanation**: the touch dot shows ~120 CSS px of clean, dominantly-horizontal finger movement in the first ~250ms of one slow-swipe attempt — comfortably past both the lock and the 72px commit threshold, a drag no reasonable axis bias would lose — while the content pane doesn't move at all until far later. That's more consistent with touch events not reaching this code's `pointermove` listener during that window than with a losing axis-lock coin flip. Android "Show taps" draws its dot at the OS compositor level, independent of whether the WebView's web content actually receives the touch stream; this is a React Native app wrapping `react-native-webview`, and RN apps commonly layer a native gesture recognizer (screen-edge swipe-to-go-back, via `react-native-screens`/`react-native-gesture-handler`) above the WebView — `touch-action` only controls the *browser's* handling of events it actually gets, it has no power over a native recognizer intercepting the touch before the WebView sees it. Unconfirmed — a targeted on-screen `pointermove`-count overlay was built to settle it but never produced a reading (see below) before the investigation was dropped in favour of shipping what works. **If this gets picked up again**: confirm the interception theory first (the counter, or equivalent, actually needs to render — it didn't last time, worth checking `z-index`/stacking rather than assuming the theory is right) before touching this code further; the axis-bias/lock tuning above is left in place since it's harmless for fast swipes, not because it's known to help.
  - **The `pointermove`-count overlay never appeared on screen at all**, not even reading zero — genuinely unknown why (possibly a stacking/`z-index` issue specific to this WebView, possibly `debugCounts()` was never called — wasn't narrowed down before the investigation was shelved). A page-level `console.log` was tried first and also produced nothing in `adb logcat`, on an unfiltered capture during a real on-device gesture — this app's WebView does not appear to forward page console output to logcat at all (or forwards it somewhere this capture didn't reach), which is worth knowing before reaching for logcat as a debugging channel on this app again.
- **Rapid back-and-forth between two tabs could glitch — a stale transition-cleanup callback clobbering a pane a newer gesture had already taken over** (2026-09-10). `settlePanes()`'s `finish()` clears the incoming pane's inline `transform`/`transition` when its `transitionend` fires, or via a `setTimeout` fallback (duration + 60ms) if that gets cancelled or never fires. Switching tabs again inside that ~220–280ms window reuses the same pane as the *new* gesture's own — its `pointermove` writes `transform`/`transition` directly, live — and the *old* transition's cleanup still fires on schedule regardless, snapping the transform back to nothing mid-drag: a visible glitch, worse the faster you switch. Fixed by threading the commit's own token into `settlePanes()` and gating the style-clearing on `token === ui.activeToken` — `outgoing.remove()` stays unconditional (a different, older pane, always safe to discard), only the *style* write on `incoming` is skipped once something newer owns it.
- **Two nav buttons could show highlighted at once — a stuck `:hover`, not a pill bug** (2026-09-10). Reported from a real screenshot: Calendar (tapped a while earlier) stayed lit after a swipe landed on My Bookings, which was also lit — two independent boxes, confirmed by zooming in: Calendar's icon was plain white (`:hover`'s `color: var(--text)` cascading into the icon's `stroke: currentColor`), My Bookings' was the real accent blue only `.is-active .icon` sets. `:hover` on a touch device applies on tap and has no real "leave" event to clear it — nothing un-hovers Calendar once you swipe away without tapping the nav bar again. Not unique to nav: every `:hover` rule in `styles.css` had the same latent bug (row highlights, slot tiles, buttons sticking after a tap), so all of them — not just `.fr-nav-btn:hover` — are now gated behind `@media (hover: hover)`, true only for a device with a real pointer that can actually leave an element. Desktop (Tampermonkey, a real mouse) is unaffected; touch loses `:hover` styling entirely, which is the only way to lose the "stuck" failure mode with it. `.fr-gyan-chip`'s combined `:hover, :active` rule was split — `:active` (a real touch press, with a real release) stayed ungated, only its `:hover` half moved behind the media query. The user's own first theory (normalize nav button widths so the pill is always one size) wouldn't have fixed this — it was never a pill-geometry bug, the pill was correctly sized and positioned on My Bookings the whole time.
- `prefers-reduced-motion` — or the new `settings.motion` flag being off (`src/settings.js`, see Current State) — drops the pill's slide (CSS) and collapses every JS-driven transform — drag-follow, spring-back (`animateContentTo()`), crossfade (`crossfadeToTab()`, which checks it itself rather than routing through `animateContentTo()`) — to an instant cut with no animated frame, rather than disabling motion piecemeal.

### Home

- **Week strip** (`buildDateStrip`, shared with Calendar's mobile day view; `buildDayNav` is the simpler prev/next control it replaced, still used by Calendar's week mode and Book Slot) replaced a vertical "next 6 days" column the user called "kinda stupid". Seven cells, prev/next paging, dots for days carrying classes, and deliberately **no Today button** — today's cell has its own treatment (`.is-today`, distinct from `.is-selected`) and re-opening the tab resets to today anyway. Selecting a day filters the already-fetched events client-side; no refetch, no tab jump.
- Class rows are fixed height. `.fr-row-title`/`.fr-row-meta` are single-line + ellipsis, `.fr-row-main` is `flex: 1; min-width: 0`, full text on `title`. `.fr-row-meta:empty::before { content: '\00a0' }` reserves the meta line so a class with no room/faculty doesn't sit shorter.
- **The bookings section is one bucket, "Today" or "Upcoming", never both** (2026-09-10). It used to always say "Upcoming bookings" and list up to 5 from now on, which duplicated My Bookings' own day-sectioned list right below it on the same tab bar. Autobook rows on Home are read-only (`buildPendingBookingBadge()` in `autobook.js` — title/meta/`Pending` pill, no Cancel control, factored out of `buildPendingBookingRow()`'s now-shared `pendingIntentText()`); only My Bookings can cancel a watch. The skeleton's heading shimmers (`skel(8)`) instead of guessing either word, since which one applies isn't known until the fetch lands.
- **Cancelled bookings are dropped from Home outright, not dimmed** (2026-09-10, same day, second pass). The first version kept them, `.is-cancelled` and all, "same as My Bookings" — asked directly, the answer was there's no reason to: a glance view has nothing to do with a slot that never happened, unlike My Bookings' own Today section, which keeps them on purpose so "did I already use my gym slot" has an answer. So Home filters to `status === 'Booked'` before anything else, and the row markup here never carries `.is-cancelled` at all.
- **Today shows everything still ahead of it, uncapped; Upcoming still caps at one** (2026-09-10). A same-day booking whose own end time has already passed (book the 9–10 gym, it's past 10) drops out the same way a cancelled one does — `parseBookingDateTime(b.endDateTime) > now` — so a day that's run its course falls through to the Upcoming case instead of showing something over and done with. Real bookings and waiting autobook intents are built into one list (`{kind, start, …}`) and sorted together by `start` — an intent's `start` is `slotStartDate(intent.date, intent.startTime)` — rather than intents always trailing every real booking, so whichever is actually sooner shows first. `todayItems` is that combined list filtered to today; `isToday` is just whether that's non-empty. When it's empty, Upcoming shows `combined.slice(0, 1)` — the single nearest thing across both bookings and intents, same "one nudge, not a browse surface" reasoning as before, now correctly picking an autobook watch over a real booking when the watch fires sooner (a full slot autobooking for tomorrow outranks a confirmed booking for next week).
- **Never disappears.** Empty, it is `.fr-nothing` — one `.fr-row` exactly (same 14px band, reusing `.fr-row-title`/`-meta`), inverted to hairline dashed with a `Book a slot` link so it can't be mistaken for a booking. It used to be omitted entirely, which made the page look like it had ended early; the first replacement was `renderEmpty`'s page-sized state (64px padding, 32px icon, three lines) dropped into a list slot, standing three rows tall and shoving the page ~130px when it resolved.
- Bookings read `shortDayLabel · start – end` (`Today · 8 AM – 9 AM`, via `formatBookingWhen()`). Previously two full `dd/mm/yyyy, h:mm AM` strings, stating the date twice for a single-day booking. `cleanResourceName()` strips the trailing operating-window parenthetical (`Gym ( 6:00 am to 2:00 pm slot )` → `Gym`), which is the facility's window, not the reservation.
  - `shortDayLabel` also fixed a bug inherited from `dayLabel`: it compared a *datetime* against midnight, so a 4 PM booking tomorrow displayed "Thu, Sep 3" while a 7 AM one displayed "Tomorrow". Both ends are floored to a date now.

### Calendar

- A real grid with day/week modes, 9 AM–7 PM at 78px/hour (the user's actual class hours). Blocks read **venue → title → professor**; the time is dropped, being redundant with vertical position. Overlaps get side-by-side lanes (`assignLanes()`, greedy interval packing).
- Mobile restructures `.fr-cal-header` via `grid-template-areas` (`"title toggle" / "nav nav"`), and day mode swaps the simple nav for the week strip — that part needs a JS branch (`matchMedia` at render time) plus a debounced `resize` re-render, since rotating across the 760px breakpoint changes the DOM, not just the layout.
- Week view on mobile scrolls horizontally: `min-width: ${days.length * 150}px` on `.fr-cal-days`, `overflow-x: auto` on `.fr-cal-wrap`, and the hour gutter `position: sticky; left: 0`.
- **Nothing in a block wraps.** Block height *is* the class's duration, so an extra line is an overlap into the block below, and `Chandragupta - Focus Room 201 W` is a real room name. Venue and professor are one line each, the title keeps a two-line clamp (measured to fit: box 72px / content 72px for a 55-minute class at 360, 390 and 1280px), `.fr-cal-event` takes `overflow: hidden`, full text on `title`.
- Two alignment bugs worth not re-deriving: a blanket `overflow:hidden` added to fix "9 AM overlaps the header" clipped the top half of *every* hour label — only the first one needs it (`.fr-cal-hour-label--first`). And day mode rendered the 40px `.fr-cal-gutter-header` spacer that only week mode's day-headers need, sitting every hour 40px low.
- The sticky gutter painted over the fixed bottom nav at equal `z-index`; `.fr-nav`'s mobile `z-index` is 5.

### My Bookings

**The cancel confirmation happens inside the row** (`attachCancelConfirm`, `.fr-row.is-confirming`): the title rewrites from `Gym` to `Cancel Gym?` and the one ghost button becomes `Yes` (danger) + `No` (ghost). The booking is named once, in the place already reading as its name. Measured 70px for every row, idle and confirming, at 360 and 390px.

Three earlier passes were rejected, all failing the same way — each *added* something to a row with no room, then bought that room from the booking's own text:
1. Question + two buttons swapped inline for the Cancel button → squeezed the title into a sliver.
2. The same as an absolute overlay → no shift, but it covered the booking being cancelled.
3. A drawer expanding downward → wasteful, and measurably uneven: a collapsed drawer still picked up mobile's `.fr-row { gap: 10px }`, so a cancellable row measured **80px against every other row's 70px** (`.fr-row--confirmable { gap: 0 }` lost to the later media-query rule at equal specificity). That is a real ordering bug that had already passed a visual review — which is why `?auto=measure` exists.

Details: `Yes` sits **left** of `No` on purpose, so `No` lands where the finger just was and a double-tap can't cancel a booking. Failure replaces the *time* line with `Couldn't cancel: <msg>` and turns `Yes` into `Retry`; the row still can't change size. One row asks at a time (`row.__frCloseConfirm`). Escape closes and returns focus to the trigger; focus only moves to `No` on keyboard activation (`click.detail === 0`). On mobile the status pill is dropped from any row with a live Cancel button (`.fr-row-actions:has(.fr-cancel-wrap) .fr-badge`) — the button already implies it, and it buys ~70px of text width.

**Three sections, by calendar day, not by instant (2026-09-09).** `Upcoming` (day after today) / `Today` (same calendar day, regardless of whether the time has passed) / `Past` (day before today), replacing an earlier split that compared each booking's datetime against `now` — which put a booking from earlier today in Past, answering "what happened today" with the wrong list. A row stays cancellable only while its own time is still ahead of `now`, independent of which section it landed in.

**Cancelled rows are the row dimmed, not just the badge** (`.fr-row.is-cancelled { opacity: 0.55 }`) — the existing `.fr-badge.is-cancelled` colour alone wasn't enough to keep a cancelled booking from being misread as live at a glance. Within `Upcoming`/`Today` a cancelled booking sorts below every live one in that section, even if its own time is earlier — a cancelled slot sitting between two live ones reads as "is this one still on?". `Past` stays strictly chronological on purpose: sinking cancellations there would pile every cancellation ever made at the bottom of a section nobody re-checks anyway.

**A watched autobook intent shows up too, in whichever section its own date lands in** — a full slot being autobooked for today shows under `Today`, not always `Upcoming`. It renders as a real `.fr-row` (`buildPendingBookingRow` in `autobook.js`), same 70px rhythm as an actual booking, dashed instead of solid (`.fr-row--pending`, the same "not real yet" language `.fr-slot--later` already uses). Title says what it is (`Autobook · Gym`), meta says when and its status (`Tomorrow · 1 PM – 2 PM · Opens in 16h 33m`, `· Daily` appended for a repeating series), and the pill says `Pending` (`.fr-badge.is-pending`) rather than repeating "Autobook" — the same what/status split a real row makes between title and pill, just with "what" carrying one extra word.

**Within Today/Upcoming, an intent sorts by its own start time alongside the live bookings, not always after them** (2026-09-10). It used to be a fixed order — every real booking in the section, then every pending intent — so a watch opening in twenty minutes sat below a confirmed booking three days out. `buildSection` now merges the section's live (non-cancelled) bookings and its pending intents into one list sorted by start (`slotStartDate(intent.date, intent.startTime)` for an intent), and appends cancelled bookings after that, sorted among themselves the same as before — the cancelled-sinks-to-the-bottom rule stays, only the intent's fixed position changes. `Past` is untouched: it has no intents to merge and was never passed any, so it keeps the old plain, strictly-chronological render path.

*Where the explanation went, and why*: a real booking row has no third line, and a boxed card with its own heading was tried and rejected earlier the same session as too tall. The first fix for that tried a hover `title=` tooltip on the pill — wrong, caught before shipping: this is a touch app first (Android WebView), and nothing on it can be hovered. Always show real info, never hide it behind an interaction that doesn't exist on the actual device — the countdown (`Opens in 16h 33m`) is drawn directly into meta instead, exactly how Book Slot's own grid already draws `Opens in 6h 17m` straight onto a dashed slot tile with no tap required. The full "tries while the app is open" sentence is dropped — the dashed border and the `Pending` pill already say "not guaranteed" on their own, so the countdown is the only fact meta actually needs to add. `intent.message` (a live status like `Full — still watching.`) still overrides the countdown when present, same priority as before. Cancel follows the exact pattern a real booking's Cancel does: `Cancel ` prefixes onto the title rather than replacing it (`Cancel Autobook · Gym?`, same shape as `attachCancelConfirm`'s `Cancel ${name}?`), the pill stays put, and the row's own `.is-confirming` tint (background + danger title colour) is the same mechanism a real booking's cancel already gets, for free. Stopping an intent is a synchronous `localStorage` edit, so unlike a real cancel there's no failure state to hold — `Yes` always succeeds.

The first version of this instead wrapped `buildScheduledList` (the widget `Book Slot` already shows under its grid) in a dashed **card** with its own `Booking automatically` heading, nested inside the day section. Rejected as too tall — the card's own padding plus a redundant heading (the section already says `Upcoming`/`Today`) added real height on top of rows that were themselves taller than a real booking row. `buildScheduledList`/`buildScheduledRow` are unchanged and still used as that boxed list under `Book Slot`'s grid, where there's no day-section heading already doing the framing; `buildPendingBookingRow` is the separate, row-shaped variant for here.

### Book Slot

- Facility rail + resource picker + day nav on the left, results on the right. Availability **auto-loads** on any selection change; the old "Check availability" button is gone. The separate Resources tab was deleted — its data still feeds the rail.
- **`buildPicker` replaced the native `<select>`**, which Android WebView renders as a full-screen platform wheel with no styling ("really ugly"). A styled button + `role="listbox"` panel (`.fr-picker*`, reusing the existing tokens, plus a `chevronDown` icon that rotates 180° when open): click-outside and Escape to close, `aria-expanded`/`aria-selected`, exposing only `.value` and `.setOptions()` because that is all the tab used. Opening scrolls the selected row into view, which matters for Class Rooms' 13 resources against a 280px `max-height`.
  - The highlight is painted by `paintPanel()`, which the option-click handler never called — it set `value` and the label and closed, leaving the old row highlighted until something else repainted. Only visible on a *second* open, which is why `?auto=picker-reselect` reopens before asserting.
- **Resources are sorted** once in `renderBookSlot`, where the response lands, so the picker, the default selection and everything else agree. Two things a plain `.sort()` gets wrong: numeric collation (`Intl.Collator(undefined, {numeric:true})` puts `ARB002` before `ARB101`, and sequences Discussion Room A/B/C), and operating windows compared as **times** — `splitResourceWindow()` peels off the parenthetical so `Gym ( 6:00 am … )` sorts before `Gym ( 3:00 pm … )` rather than on the bare digit 3. The collator is pinned to **`'en'`, not `undefined`** (2026-09-04): `firstLiveResource()` takes the default selection from this order, so the device's locale would have decided which resource is preselected — ordering here is behaviour, not presentation.
- **The grid shows the resource's whole day**, merging live availability with slots derived from its operating-window suffix, or from times remembered in `localStorage['flame-slot-times']` for resources without one. A slot the portal won't list yet is real and dated, drawn dashed with `Opens in 6h 17m`.
- **A window whose slots have all started is answered locally (2026-09-04).** `knownSlotTimes` reads the operating window out of the resource's own name, so `windowSpent(resource, isoDate, now)` settles the day without asking and `refreshAvailability` returns before the fetch — the old path spun and resolved into a bare `No open slots`. An unparseable slot time makes it **false**: not knowing is not knowing it is over. The cutoff is the last slot's *start* (1:00 pm for the 6 am–2 pm gym), never the window's 2 pm end, which was never bookable. Same predicate fixes the default selection: resources sort by start time, so `resources[0]` was the earliest window and after 1 pm the one option guaranteed dead — `firstLiveResource()` takes the first still live for the selected date, at mount and on a facility change.
- **A spent window is annotated, never disabled.** `buildPicker` options take an optional `note` — `.fr-picker-option-note`, plus `.fr-picker-btn-note` on the collapsed button, its own element because the label ellipsises and a concatenated status would be the first thing a long resource name truncated away. The gym reads `Bookable tomorrow`; selecting it gives `6 AM – 2 PM is bookable from tomorrow` and one action: the same resource *class*'s live sibling window (matched on `cleanResourceName`, so a spent gym never offers a squash court), else `Try tomorrow`. It stays selectable because the status belongs to the resource **and the date** — tomorrow's 6 am gym is bookable *now*, one tap on the day nav away, which is also why the copy says "bookable from" rather than `Opens tomorrow`. Disabling globally breaks that; disabling per-date changes the picker's contents under the user as they step days, so `paintResourceOptions()` reruns on every date step instead. Same rule the slot tiles already follow via `blockedReason()`: never swallow the tap, say why. The other two states match — `No open slots` / `Every slot for this day is taken or past.` + `Try tomorrow`, and a server refusal through `renderEmpty` rather than a bare `.fr-rail-meta` line — and all three keep `buildScheduledList` below them, which the old bare-paragraph path dropped.
- Slots returned in `availabilitySlots` used to be tagged `kind: 'open'` unconditionally by `refreshAvailability`, which on dates more than 24h out opened the *manual* panel and defeated every blocked-tile check. `opensAt = start - BOOKING_WINDOW_MS` now decides: still in the future means `kind: 'later'`.
- **Slot times are compared on the minute, never on the text (2026-09-04).** The grid drew every slot twice on another student's phone — `6 – 7` beside `6 – 7`, a whole two-column grid of pairs — and never on ours. Derived times were formatted with `toLocaleTimeString`, and the portal's own strings are a fixed `"6:00 AM"`: locales disagree on the dayPeriod's case (`en-IN` writes `am`) and on the space before it (ICU 72+ uses U+202F), so `bookable.has(known.startTime)` missed and the known-slot fill re-added a slot availability had already returned. It looked random because only resources whose *name* carries an operating window derive their times at all — everything else replays remembered server strings, which match by construction — and only slots still ahead of `now` survive the fill's own filter. `slotTimeKey()` (minutes since midnight, `raw:<lowercased>` when unparseable) is now the key for the dedupe map, the skeleton shells' `dataset.start`, the scheduled-tile intent match and `runAutoBook`'s fire-time slot match. `minutesToClock` builds `"6:00 AM"` by hand: these strings are matched against the portal's, so rendering them through a locale was the category error. `dates.js`'s `ZERO_MINUTES_RE` already spelled `[\s\u00a0\u202f]` — the same trap, caught once before and not generalised. Free speed as a side effect: `toLocaleTimeString` with an options object builds a fresh `Intl.DateTimeFormat` per call (53µs against 1.25µs for a reused one — V8's cache only covers the no-options shape), and `knownSlotTimes` is called at four sites per render, so an 8-slot window spent ~3.4ms per refresh inside ICU. Now ~0.03ms.
- `Opens now` is gone; a slot inside its window that the portal still isn't listing says **`Slots full`** — that is what an empty `getResourceAvailability` means for every known slot.
- **Confirm panel**: title `compactTimeRange(start, end)`, Purpose/Co-attendee only for room-type facilities (`facility_Name !== 'Sports Facilities'`) — a gym slot has nothing to put in either. `.fr-confirm-header` is a toolbar with the title left and the `Daily` toggle inline right; `.fr-confirm-inputs` is a 2-column grid collapsing on mobile; `.fr-confirm-warn` is a tinted callout (`color-mix(in srgb, var(--danger) 12%, var(--bg-elevated))`); the primary button is a full-width 42px anchor at the bottom. `text-wrap: pretty` on `.fr-confirm-note` and `.fr-confirm-warn` so a single word doesn't wrap alone.
- **It asks before acting**: on open, `existingBookingFor(resourceName, isoDate)` disables submit with `Already booked Gym Today · 8 AM – 9 AM — one booking a day.` rather than submitting and rendering the portal's refusal in red. A failed lookup does not block — the portal is still the authority.
- **Selecting a slot scrolls the button into view** (`scrollConfirmIntoView` + `confirmScrollDelta`, module scope, called from both panels). Three faults, all real:
  1. `scrollIntoView({block:'end'})` aligns to the bottom of the scrollport — exactly where the fixed nav bar sits. `block:'center'` clears it but overshoots from most rows and still can't account for `--flame-navbar-inset`.
  2. `scrollToConfirm` had been declared *inside* `openConfirm` while `openAutoConfirm` called it, so the autobook panel had been throwing `scrollToConfirm is not defined` and scrolling nowhere.
  3. `requestAnimationFrame` never fired — same starvation as the cold-launch cover. **rAF is not a dependable clock in this app.**

  What ships: go to the bottom (`scrollTo({top: scrollHeight - clientHeight})`), because the panel is the last thing on the page and landing at the end is what feels right — the first rewrite travelled the minimum distance, passed every check, and still felt wrong. `.fr-content`'s bottom padding (96px + inset) is larger than the bar, so the end of content parks ~47px clear of it. `confirmScrollDelta` still verifies: it subtracts the nav bar's height **only when its computed `position` is `fixed`**, and returns the distance needed to put the button inside what's left with a 12px gap. Issued synchronously. 400ms later anything short of the bottom is finished without animation — **unless a `touchstart` or `wheel` arrived first**, in which case the user has taken over.

### Autobooking

The rules came from the user; the HAR only ever captured one date. Booking opens **less than 24h** before a slot, only **one booking per resource *class* per calendar day** (class = the resource without its window suffix, so the 6 AM and 3 PM gym are one thing), and popular gym slots go within about five minutes of opening.

**No native work was needed.** The original plan was `AlarmManager.setExactAndAllowWhileIdle` at T-24h waking a headless WebView — a manifest edit the patch pipeline can't do, plus a session surviving a day idle. The user's own observation removed all of it: **the app is opened several times a day, so an intent in `localStorage` retried at every launch books anything uncontested with zero native code.** Works on desktop too. The known limit, stated rather than hidden: it loses a five-minute rush unless the app happens to be open.

- Intents in `localStorage['flame-auto-book']`: resource, ISO date, slot start/end, purpose/co-attendee, state, last message, `repeat`.
- `runAutoBook()` fires at boot (after the token lands) and every 2 min while open, tightening to **15s once an intent's opening moment is within 30 min** — that tight loop is the only way this version wins a contested slot. `AUTOBOOK_EARLY_MARGIN_MS` (30 min) starts attempts slightly early: the 24h rule is *our model*, not something the API states, so being early costs two cheap calls and being late costs the booking.
- Per intent: skip if not open; fail if the slot has started; **pre-check `getReservations`** for the same class that day (turns a guaranteed refusal into a clear message); then availability, and only on real capacity, `createReservation`. No capacity yet means the intent **stays waiting** — people cancel, and there is still time on the clock.
- **Autobook goes through a confirm panel** (`openAutoConfirm`), mirroring `openConfirm` because it ends in the same `createReservation`, just made later. Purpose/Co-attendee are stored on the intent and passed at fire time — autobook used to drop them. Copy is one line, deliberately: the first draft explained the window, the retry behaviour and the lack of a guarantee in a paragraph per case, and the verdict was *"no one is reading allat"*. The only distinction worth space is **tries** vs **reserves** — `Tries as soon as it opens. Not a reservation.` / `Full right now. Keeps checking and books it if it frees up.` "Repeat daily" carries no caption; the two words are the explanation.
- Results land as a banner above whatever tab is open (`.fr-banner-host` lives outside `contentEl`, which every render replaces), carrying the portal's own words and a Dismiss. Silent auto-booking is how you end up with a reservation you didn't know about.

#### Native background autobook (exploration, 2026-09-10 — branch `native-autobook`, unmerged)

Not shipped, not on master. The localStorage/open-app model above wins everything except a slot that opens while the app hasn't been opened in a while, and the five-minute contested gym rush specifically. Whether that gap is worth real native surgery is still an open question — this is the investigation, not yet the answer.

**What true background needs is smaller than first framed.** The reflex plan was hand-written smali — wrong, or at least not necessary: the real toolchain is already available (`javac` is already in the rebuild-from-nothing list; `d8` is already in `build-tools/`, same zip that provides `aapt`/`apksigner`/`zipalign`), so a receiver can be written as real Java, compiled and dexed normally, then merged into the APK as its own `classesN.dex`. apktool then only needs to touch the manifest — a new `<receiver>`, plus `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM`, `RECEIVE_BOOT_COMPLETED`, `WAKE_LOCK`, `POST_NOTIFICATIONS`. One missing input, not yet sourced: an `android.jar` to compile against — the same direct-download approach that got `build-tools_r34` without going through `sdkmanager`'s license flow should work here too.

**Two real gates, both unresolved, and the whole plan hinges on either:**
- **Does the Aura token/session survive ~13h fully idle?** Unknown. `flame-aura-auth` is known to survive *some* page loads (two HARs shared one token), but nobody has tested the actual idle gap a native alarm would need. If it doesn't survive, the alarm fires against a dead token with no way to recover — no page load means no re-sniff (see `sendAura`'s `INVALID_TOKEN` handling).
- **Google SSO cannot be scripted if the session does die — a hard wall, not a hard problem.** Google's login flow actively detects and refuses embedded WebViews (`This browser or app may not be secure`), and real 2FA needs a human physically present regardless. Worth knowing before spending effort on anything else here, since it caps what native code could ever recover from.
- **HyperOS may kill the receiver regardless of a correct manifest.** Whitelisting (autostart + no battery restriction) raises the odds, doesn't guarantee it — MIUI-descended battery management is known to kill background work stock Android's own AlarmManager rules would otherwise allow.

**The exact-alarm design beats a ported poll loop.** The existing 2-minute JS poll only exists because browser/WebView JS has no OS-level "wake at time X" primitive — that constraint disappears with real `AlarmManager`. The native version should compute one `setExactAndAllowWhileIdle` at `slot_start - 24h - margin` (replacing 24h of polling outright), plus a short 15-30s retry burst right at that moment for the contested-slot rush, not a sustained interval for the whole window. Cheaper on battery and far less likely to be throttled by Doze than a naive port of the current loop would have been.

**Token bridging is not the hard part.** `localStorage['flame-aura-auth']` is already page-scoped and survives across script runs — a background path that stays inside the WebView sees it for free. A pure-native path (no WebView) can't read WebView's own storage format directly, but the Frida hook already has full Java access while the app is foregrounded and can trivially copy token+cookie into the app's own `SharedPreferences` on every launch. What's actually hard is *freshness* (the gate above), not access.

**Plumbing test (2026-09-10 night): alarm + receiver + notification confirmed working end to end, including surviving a real kill.** Built as a self-contained test — `AutobookReceiver` (real compiled Java, `javac` + `d8` against a sourced `android.jar`, merged into the APK as its own `classes5.dex`; apktool only touched the manifest) posts a plain notification with no Aura/token dependency, so this proves the mechanism in isolation from the token questions above.
- `scheduleTestAlarm()` in `hook.src.js` schedules one `setExactAndAllowWhileIdle` ~90s out on every launch, checking `canScheduleExactAlarms()` first (granted immediately on this phone — no settings detour needed).
- **`am kill` (mimics the OS reclaiming a backgrounded process) → alarm survived, process resurrected, receiver ran** — confirmed via `dumpsys alarm`'s per-app wakeup counter and `ps` showing the process back. This is the scenario that actually matters (user just stops using the app).
- **`force-stop` → alarm cancelled outright** (`dumpsys alarm`: `Reason=pi_cancelled`). Confirmed Android behavior, not HyperOS-specific: force-stop is a deliberate "fully quiesce this app" action distinct from the OS reclaiming memory, and it explicitly cancels all pending alarms. Matters only if the user manually force-stops the app from Settings — which the existing per-launch reschedule already recovers from for free, same as the localStorage-intent model already does.
- **The first real fire produced no notification, and the cause had nothing to do with alarms or backgrounding**: `POST_NOTIFICATIONS` (Android 13+) is a runtime permission — declaring it in the manifest is not enough, and nothing had yet triggered the actual grant dialog, so it sat `granted=false`. `NotificationManager.notify()` silently no-ops on a denied permission — no crash, no log line, which is what made a permission gap look identical to a timer failure. Granted via `adb shell pm grant` (stand-in for a real dialog tap) and the same alarm-fired path then produced a real, live `NotificationRecord`. **The production version needs an actual in-app runtime-permission request** (once, gated on not-already-granted) — this was tested via adb specifically to keep it out of scope for this pass.
- Manual `adb shell am broadcast -n <pkg>/<receiver>` (explicit intent, to trigger the receiver on demand without waiting on the real alarm) was inconclusive — no crash, but also no confirmed notification from that path specifically; the real alarm-triggered path is what was actually verified twice. Worth knowing if this manual-trigger shortcut gets reached for again — it isn't proven to work the same way.
- **Confirmed against the real gesture, not just an adb proxy for it**: swiped the app away from Recents by hand (stronger than `am kill` — an actual task removal, not a simulated one, and nothing like `force-stop`) with the alarm already scheduled and pending. It fired anyway, notification visually confirmed on-device (not just present in `dumpsys`). This is the scenario that was actually in question this whole exploration, and on this phone, in this short-horizon test, it held.
- **Runtime `POST_NOTIFICATIONS` request added to the production hook** (`requestNotificationPermission()` in `hook.src.js`, SDK 33+ gated, `checkSelfPermission` first so it doesn't re-prompt once answered) — the adb-grant workaround above was only ever a stand-in to unblock testing; this replaces it so a real install doesn't need adb at all.
**The real Aura call, from native code with no WebView, confirmed working end to end (2026-09-11).** Layered on top of the proven plumbing above rather than mixed into it, on purpose — so a failure would read as "the Aura call broke", not "the timer broke". `AutobookReceiver.fetchNextClass()` replicates `sendAura()`'s request shape by hand in Java (`org.json`, `HttpURLConnection`, the same `message`/`aura.context`/`aura.token`/`aura.pageURI` fields, plus an explicit `Cookie` header — a native call has no browser cookie jar, unlike the JS path's `credentials: 'same-origin'`) against `StudentPortalCalendarCtrl.getAllScheduledEvents`, then picks the soonest `startDateTime` still ahead of now (`java.time.Instant`/`OffsetDateTime`, matching the ISO strings the portal returns) and posts it as a notification. Fired twice for real, both times via the actual **swipe-the-app-away** gesture (not an adb proxy for it) — no crash, a real answer (`"No more classes today."`, correctly reflecting no more real upcoming events, not an error string) both times.

Two real bugs shipped and caught live before that worked, both instructive:
- **A crash, not a silent failure**: the first version of the background thread used an inline `new Runnable() {...}`, and `d8`'s dex output silently omitted the compiler-generated `AutobookReceiver$1` class — the alarm fired, the receiver started, and the process crashed with `NoClassDefFoundError` the moment it tried to spin that thread (`ActivityManager: App crashed`, confirmed in logcat). The dex file just didn't contain a class it needed; `d8` gave no warning. Fixed by using a **named** static inner class instead (`FetchTask`) — see trap list below, this exact anonymous-class shape turned out to hit a real internal bug in this `d8`/R8 build, not just a which-files-did-I-pass mistake.
- **`evaluateJavascript`'s `ValueCallback` wraps a string result in an extra layer of JSON encoding.** The evaluated JS returns `JSON.stringify({...})` — a string — so the callback's `value` is `JSON.stringify` of *that string*, not the raw text. One `JSON.parse` only unwraps back to a plain string (not an object); `parsed.auth` was silently `undefined`, and `JSON.stringify(undefined)` is the literal JS value `undefined` (not the string `"undefined"`), which is what threw `SyntaxError: unexpected token: 'undefined'` one level down. Needs `JSON.parse(JSON.parse(jsonStr))`. The earlier token-log-only version never hit this because it only ever wrote `jsonStr` straight to a file without re-parsing it as an object — harmless there, silently wrong the moment real parsing was needed.

- **Not yet tested: survival over a real multi-hour idle gap** (Doze's deeper restrictions bite over hours, not the ~90s-to-a-few-minutes span exercised here) and **HyperOS's own explicit background-restriction toggle for this app specifically** (current tests ran at the OS's default state, whatever that is — never checked or overridden in Settings). Both are still open before this generalizes to "works over a real 24h autobook window."

**Token-survival test, running (started 2026-09-11 00:21 IST).** `src/aura.js` (`TOKEN_LOG_KEY` / `flame-token-log`, temporary — drop once this is settled) logs a fingerprint (last 8 chars only, never the raw token) plus a timestamp on every `load`/`store`/`rejected` event, readable via `window.__flameTokenLog()`. `hook.src.js`'s `scheduleTokenLogDump()` pulls that ~4s after every `/s/` injection and writes it to `getExternalFilesDir(null)/flame-token-log.json` (`adb pull`-able with no live Frida attach needed) — necessary because **the production gadget runs autostart/script mode, which opens no listen port, so `frida -U -n` has nothing to attach to** against a normal install. Within-session data collected so far (several relaunches, minutes apart): the stored token round-trips correctly every time (zero `rejected` events), but a fresh token also gets stored within ~1s of every launch regardless — expected (the page's own Aura JS fires its own request every load) and not informative on its own about a real idle gap. A clean baseline was captured at 00:21 IST (freshest stored fingerprint `1wv-iWA=`, saved to the local scratchpad as `token-log-baseline.json`) — the app must stay unopened until the real gap has passed; opening it early only adds another data point, it doesn't invalidate the test, but the useful read is whichever gap ends up longest. Read-back plan: pull the file again, check whether a `load` with that same fingerprint is followed by a `rejected` (token didn't survive) or nothing (it did).

**A second, independent check of the same question is riding on the 8h native alarm below** — its `AutobookReceiver.fetchNextClass()` call uses whatever was bridged into `SharedPreferences` at the 00:22:56 IST launch, untouched for the full 8h. A real answer (not `"Call failed"`/`INVALID_TOKEN`-shaped text) in that notification is a second, native-path confirmation that the token survived; a failure there while the JS-side reload succeeds would actually be informative too — it'd mean the *cookie* (native-only, no browser to refresh it) is the piece that didn't hold, not the token itself.

**8h test alarm scheduled for 2026-09-11 08:22:56 IST**, from the same launch that captured the token-log baseline above. Confirmed still live and pending via `adb shell dumpsys alarm` (not the firing-history section — the actual scheduled-alarm queue): `RTC_WAKEUP #57: tag=*walarm*:com.myflame/com.flame.autobook.AutobookReceiver`, `origWhen=2026-09-11 08:22:56.332`, `exactAllowReason=policy_permission` (still riding as a real exact alarm, not silently downgraded). **Reschedules on every cold relaunch, further out from whenever that relaunch happens** — `testAlarmScheduled`'s guard is a plain in-memory JS var, reset on every fresh process start, and `PendingIntent.FLAG_UPDATE_CURRENT` means a second `scheduleTestAlarm()` call overwrites the existing pending alarm rather than adding a second one. So: the app must not be reopened before 08:22:56, or the target time pushes later each time. A background app switch alone (no process death) doesn't touch it — only a real cold launch does.

**The 8h alarm fired dead on time (08:23:00 IST) with a real answer, not an error — and that answer exposed a second, unrelated bug (2026-09-11 morning).** `dumpsys notification --noredact` showed `android.text=No more classes today.`, with none of `fetchNextClass()`'s own error prefixes (`HTTP …`, `Call failed: …`, `Aura call failed: …`) — meaning the request round-tripped clean, `state: SUCCESS`, a real `returnValue.returnValue` events array came back. So the token+cookie bridged into `SharedPreferences` at the 00:22:56 launch really did survive the full 8h idle gap untouched — settles the gate this whole exploration hinged on, independent of the JS-side log (which never got a second data point: it wasn't touched again until the next cold launch just before this alarm fired, consistent with the app staying closed as required). But "No more classes today." was wrong — real classes existed that day. Root cause is `parseInstant()`, not the token: the portal's own `startDateTime` strings carry no zone/offset at all (`"2026-09-11T10:00:00"`, checked against every sample in `aura_map.md`), so both `Instant.parse()` and `OffsetDateTime.parse()` throw on *every* real event, `parseInstant()` catches both and returns `-1` for all of them, and `describeNextClass()`'s `startMs < 0` check filters every one out unconditionally — the message was always going to say "no more classes" that morning, full calendar or empty one. Fixed with a third fallback: `LocalDateTime.parse(s).atZone(ZoneId.systemDefault()).toInstant()` — no zone required, read as local time, matching how the JS side's own `new Date(str)` (same zone-less string) already treats it.

**Compiling the native receiver, concretely (2026-09-11) — one new trap.** `javac -cp android-34.jar -d native-out/classes native-src/…/AutobookReceiver.java` failed straight into `d8`: `Unsupported class file major version 70` — this machine's system `javac` is a JDK newer than d8's R8-based toolchain accepts (major 70 = a JDK far past what Android tooling parses), and it defaults to emitting class files at its own version rather than anything Android-compatible. `javac --release 17 -cp android-34.jar -d native-out/classes …` fixes it — `--release` cross-compiles to an old class-file version *and* restricts the API surface to that release's stdlib, not just a version-number tag. Then `d8 --lib android-34.jar --output native-out native-out/classes/…/AutobookReceiver.class native-out/classes/…/AutobookReceiver\$FetchTask.class` — both `.class` files passed explicitly (the named inner class from the `NoClassDefFoundError` trap above still needs to be included by hand, `d8` doesn't discover it from the outer class alone) — dexes clean to `native-out/classes.dex`, copied to `classes5.dex` for `native-test-build.sh` to zip in. `javac` compiles source to JVM bytecode (`.class`, stack-machine bytecode Android can't run); `d8` retargets that bytecode to Dalvik/ART's register-machine `.dex` format Android actually executes, resolving cross-references between the two `.class` files into one linked `.dex` in the process — which is exactly why both files must be handed to the same `d8` invocation together rather than dexed one at a time.

**The fix itself is unverified — device dropped off adb mid-check.** `hook.src.js`'s `TEST_DELAY_MS` was temporarily dropped from 8h to 3min to get a fast readback rather than waiting another 8h (`AutobookReceiver.java` recompiled per the trap above, redexed, `native-test-build.sh` run, `flame-native-test.apk` installed, app force-stopped and cold-relaunched to make sure the new gadget script actually loaded rather than an already-running process's stale one). Confirmed via logcat the real `triggerAt` was genuinely ~169s out despite the log line's own *hardcoded* "+8h" text (`scheduleTestAlarm()`'s `log()` call never interpolated `TEST_DELAY_MS`, just says "+8h" unconditionally — cosmetic, not a functional bug, not fixed). The phone then vanished from both wireless adb and USB (`lsusb` showed nothing — charge-only cable/port or an unconfirmed USB-debugging prompt, user is rebooting to sort it) before the 3-minute alarm fired, so the parseInstant fix has not actually been seen to produce a real class name yet — only reasoned through. **`TEST_DELAY_MS` is still 3min, not reverted to 8h** — do that before any real idle test, and confirm the fix for real once the phone's back on adb.

Token chain, unbroken since last night's 00:21 IST baseline: `1wv-iWA=` → `KK7tpY0=` (00:24 relaunch) → `H3SEOAY=` (10:51 relaunch, current). Zero `rejected` events anywhere in it. Saved to `.patch-tools/token-log-morning-2026-09-11.json` for an evening re-check against whatever `flame-token-log.json` shows by then.

**The read-only plumbing test (`fetchNextClass`/`getAllScheduledEvents`) is retired — replaced with the real thing (2026-09-11 afternoon).** `AutobookReceiver` now drives the actual `flame-auto-book` intents `src/autobook.js` already manages, not a fixed test alarm: it loads the bridged waiting intents, runs the same per-intent state machine `runAutoBook()` runs in JS (claimed-day guard, 30-min early margin, `getReservations` pre-check, `getResourceAvailability` capacity check, `createReservation`, the same `/booking id/i`-style success detection), and ports `spawnNextOccurrence()`/`conflictingIntent()` too, so a daily series keeps advancing while the app stays closed — leaving that JS-only would have silently stalled any series for as long as the app wasn't opened, which is exactly the case this feature exists for.

- **One self-rescheduling alarm, not one per intent.** After every run (or every foreground stand-down), `scheduleNextWake()` recomputes and re-arms a single `setExactAndAllowWhileIdle` from whatever's still `waiting` — soonest `opensAt - 30min`, or a tightened retry once inside that window. The tight retry is **bounded** on purpose: 20s wakes for 5 minutes (matching the documented contested-gym-slot window), falling back to a calmer 2min cadence after that rather than hammering the alarm indefinitely, which the JS side's `setInterval` can afford to do for free but a real OS exact-alarm can't.
- **Token failure suspends the whole chain, deliberately, rather than retrying.** `INVALID_TOKEN` (same string-match `sendAura()` uses client-side) aborts the run, cancels the pending alarm outright, and sets a `token_dead` flag `scheduleNextWake()` refuses to arm anything past — native has no way to refresh a dead token itself, so continuing to retry would just burn battery against a session that can't succeed. `hook.src.js`'s per-launch bridge clears the flag and re-arms right after a fresh token lands, since a successful page load always implies one.
- **The receiver stands down when the app is foreground-visible, rather than learning to paint its own popup.** `startAutoBookLoop()`'s existing JS-side poll already produces the normal in-app banner (`paintAutoBookBanner()`) on a faster cadence whenever the app is open — so `onReceive()` just checks its own process importance (`ActivityManager.getRunningAppProcesses()`, no extra permission needed since self-inspection has always been unrestricted) and, if foreground, skips the Aura calls entirely and just reschedules, leaving that pass to the JS loop. (This was true of the background-*alarm* notification path specifically — see below for the separate JS-driven title-hook notifications added later the same day, which fire regardless of foreground state on purpose.)
- **Two one-way bridges, both piggybacking the existing per-launch flow, no new bidirectional JS↔Java interface.** `window.__flameIntentsSnapshot()` (new, `src/autobook.js`) joins `__flameAuthSnapshot` in the same ~4s-after-injection poll `scheduleTokenLogDump()` already runs, written to a new `pending_intents` pref key by `bridgeIntentsToPrefs()`. The reverse direction skips a live push entirely: the receiver just writes its full post-run intents array to `pending_intents_result` + a `native_updated_at` timestamp, and `hookClient`'s onPageFinished handler reads those **before** injecting the main `SCRIPT`, evaluating one tiny synchronous snippet that folds it into `localStorage['flame-auto-book']` if newer than what's already there — so by the time `main.js`'s `boot()` runs, native's results are already in place for `paintAutoBookBanner()` to show, no async race against boot.
- **Verified shapes, not assumed** (checked against `aura_map.md` before trusting either parse path): `getReservations`' outer `returnValue` is a real JSON array already (`bookingId`, `startDateTime`/`endDateTime` as `"DD/MM/YYYY, h:mm AM"` — a different format from `getAllScheduledEvents`' ISO strings, needed its own `parseBookingDateTimeMillis` port of `dates.js`'s `parseBookingDateTime`). `getResourceAvailability`'s outer `returnValue` is a string that's *itself* JSON on real availability (`{"availabilitySlots":[...]}`) but plain refusal prose otherwise ("No slot available.", "Booking date cannot be in the past.") — parsed with a try/catch exactly like the JS side's own `slots = (JSON.parse(raw)||{}).availabilitySlots||[]`. `createReservation`'s outer `returnValue` is always a plain prose string, success detected the same way the shipped JS does (`.toLowerCase().contains("booking id")`), not the older `R-\d+` pattern this file's own booking-replies paragraph describes elsewhere — the shipped JS is the ground truth, not that older prose.
- **Live-verified, same afternoon**: real intent scheduled from the UI, bridged (`intents bridged to prefs (310 bytes)`), and the computed alarm matched hand-traced math exactly via `dumpsys alarm` — first at `opensAt - 30min` (the original margin), then re-verified at `opensAt` exactly once the margin was removed (below).

**The mid-session gap above is now mostly closed — a real event-driven bridge, not a wider poll (2026-09-11 afternoon).** Investigated whether Frida could react to page events instead of polling `localStorage`, the same way `onPageFinished` already reacts to navigation. It can, via the **title-change hack**: `WebChromeClient.onReceivedTitle(view, title)` fires on every `document.title` write, predates `addJavascriptInterface`, needs no custom bridge object, and is hookable the identical way `onPageFinished` already is (`hookClient` → `hookChromeClient`, resolving the RN app's concrete `WebChromeClient` class — `RNCWebViewManagerImpl$setupWebChromeClient$1` — at runtime). `saveIntents()` in `src/autobook.js` now routes every write through `signalNativeIntentsChanged()`, which sets `document.title = 'FLAME_INTENT:' + JSON.stringify({intents, event})` — no visible effect, nothing in this app's UI reads `document.title`. **Confirmed live, fired correctly** on a real scheduled intent and a real cancellation, both times bridging + rescheduling within the same event tick, not the next page load. A redundant 5s poll (`startIntentsPoll`) is still running alongside as a fallback belt-and-brace, not yet removed.
- One genuine Frida trap hit and fixed along the way: building the confirmation notification *through Frida's Java bridge* directly threw `"argument types do not match any of"` on `NotificationChannel`'s and `Notification.Builder.setContentTitle`'s `CharSequence`-typed params — Frida won't auto-box a raw JS string into an interface-typed parameter the way it does for a concrete `String` one (same class of bug `writeTokenLog` already hit once with `FileWriter`). Fixed the same way: delegate to real compiled Java (`AutobookReceiver.notifySettledFromJson`/`notifyCancelledFromJson`/`notifyScheduledFromJson`, plain `String` JSON payloads in, real parsing and formatting happens in `javac`-compiled code, zero ambiguity).
- **Real, nicely-formatted notifications now fire for all three lifecycle moments, from *inside* the app** (not just the background alarm path) — scheduling a watch, it settling, and cancelling one. All three share `shortDayLabel()`/`compactTimeRange()` ports of `dates.js`'s own functions, so the notification finally reads like the rest of the app instead of a raw ISO date dump. Deliberately fire regardless of foreground state for this JS-driven path — unlike the background-alarm path, this one only ever runs *because* the app is open, so there's no "which surface should show it" question to ask.
  - Wording went through a few live rounds before landing: the working title language ("Watching Gym" / "Stopped watching Gym") was replaced with **the exact terms the in-app UI already uses** — `pendingIntentText()`'s row label is literally `Autobook · ${name}`, and `attachCancelConfirm`'s confirm question is `Cancel ${name}?` — so the notifications now read `Autobook · Gym` (scheduled, `(daily)` suffix for a series), `Cancelled Autobook · Gym` (verb-first, matching `Booked Gym`/`Couldn't book Gym`'s own shape), and unchanged `Booked Gym — …`/`Couldn't book Gym — …` for settling. Reusing existing app vocabulary instead of inventing notification-only wording.
  - `.gitignore` gap found and fixed the same session: `.patch-tools/*` is blanket-ignored with individual `!` exceptions for the handful of real source files in there (`hook.src.js`, `gadget-config.json`, `fast-rebuild.sh`) — `native-src/` and `native-test-build.sh` were never added to that exception list when `native-autobook` started as a quick plumbing test, so `AutobookReceiver.java` — the entire native receiver's source — was **never trackable by git at all** until this was caught. Both added to the exception list.
  - Notification icon is still the stock `android.R.drawable.ic_dialog_info` (a generic "i" bubble) — swapping to a better-fitting stock icon (an alarm/clock glyph) is a one-line change; a real branded icon (the reskin's own flame glyph) needs actual drawable resources added to the APK, which means `apktool` recompiling `resources.arsc` — everything so far has only ever merged in a dex, never touched app resources, so that's a materially bigger, not-yet-attempted step.
- **Two real scheduling bugs found live and fixed, not preemptively:**
  1. The tight 20s retry burst was anchored to `opensAt - 30min` (the early-margin point), not the real `opensAt` — since the burst only lasts 5 minutes, it fully expired **25 minutes before the slot actually opened**, leaving a calm 2-min cadence covering the one moment that actually needed 15-30s granularity. Re-anchored to the true `opensAt`.
  2. **The 30-minute early margin itself was removed entirely, from both sides** — first from native (this file's own earlier reasoning framed it as model uncertainty; corrected live: the 24h rule is confirmed, not a guess, so the only real justification left was clock/network skew, and a network call is seconds at most, not 30 minutes), then the **same bug was still live in `src/autobook.js`'s `AUTOBOOK_EARLY_MARGIN_MS`**, caught because it fired for real: an autobook attempt 30 minutes before the portal's actual window returned `"...select a slot within 1 day."` — a genuine portal refusal for being early, which the existing `/booking id/i` check correctly didn't match, so it fell into the generic-refusal branch and got marked **permanently failed** instead of retried. Both native and JS now wake exactly at `opensAt`, and both now specifically recognize `"within 1 day"` in a `createReservation` refusal as *not* terminal (stays `waiting`, same treatment as `"Full"`), so a similarly-early attempt from clock skew alone can no longer kill an intent outright.
- **Flat 2-min retry for the ~23h55m quiet middle of a 24h window replaced with exponential backoff** (2→4→8→16→32→60min, capped) — a flat cadence there was ~720 wasted calls per watched intent for something contested only in two narrow windows. Computed fresh each call from elapsed time since the opening burst ended (`backoffInterval()`), not a persisted attempt counter, so scheduling stays a pure function of `now`/`opensAt`/`start` like everything else here. A **closing burst** (tight 20s again) was added symmetrically for the final 15 minutes before the slot starts, since cancellations/no-shows free up capacity near the deadline too, not just at opening.
- **A new lifecycle notification for token suspension** — previously silent: the chain would stop and nothing told you why. `notifyTokenDead()` now fires "Autobook paused — reopen the app to continue" (with a count of affected watches) the moment `token_dead` gets set, same one-shot-per-suspension timing as the suspend itself.
- **One transient native crash observed, unrelated to any of the above** — a `SIGSEGV` inside `libart.so`'s `MarkCompact` GC (`HeapTaskDaemon` thread, `Cause: null pointer dereference`) during one cold launch mid-session. Zero frames in the backtrace touch app or Frida code — entirely inside ART's own garbage-collector internals. The process was relaunched by the OS automatically and booted clean immediately after; a known-shaped risk of heavy Frida method-hooking interacting with newer concurrent/mark-compact GC implementations, not something traced to any specific change here. Worth knowing about if a cold launch ever silently fails to produce the expected `flame-inject` log lines — check for exactly this signature before assuming the new code is at fault.
- **Known, accepted gap, narrowed but not eliminated:** the title-hook signal above covers any change made *through this app's own JS* (`scheduleIntent`/`removeIntent`/`runAutoBook`). It does **not** help if the process is genuinely killed within the same few seconds a change was made and before the signal's own round-trip completes — a vanishingly narrow window now, versus "until the next full relaunch" before.

**The one-per-day rule** is enforced at scheduling time as well as fire time. `resourceClassKey()` is `cleanResourceName()` lowercased; `conflictingIntent(resourceName, isoDate)` finds the waiting intent holding a class/day and `scheduleIntent` returns `null`. The grid says so rather than swallowing the tap: other tiles for that class go `.is-blocked` + `aria-disabled`, keep their normal status caption (`Opens in Xh`, `Slots full`, `Auto-booking ✓` — rendering an empty string left an awkward blank under the time), carry `blockedReason()` on `title`, and stay tappable to raise a self-dismissing `showNotice` banner. `blockedReason()` is one sentence: `A daily autobook already covers this day. Stop it below to book this day yourself.` for a series, `An autobook is already scheduled…` for a one-off. The redundant `.fr-book-results-title` line above the grid (`Gym · Today, Sep 4`) was removed so a single `.fr-slot-notice` sits cleanly at the top when a day is covered (`An autobook is already scheduled for this day. Stop it below to book this day yourself.`). `runAutoBook` keeps its own guard (`alreadyBookedThatDay`, filling a `claimed` set from both a successful `createReservation` and an existing-booking hit) regardless — for intents already in `localStorage` from before the rule, and because a booking made earlier in the same pass isn't in the cache snapshot.

**Manual booking on a claimed day warns rather than blocks** — the panel says `Stops your 6 AM – 7 AM autobook — one booking a day.` and really removes the intent on success. A second *autobook* on a claimed day is refused outright, since unlike a manual booking it could never succeed.

**Daily repeat** (`intent.repeat === 'daily'`) is not a rule that fires forever: each occurrence spawns the next only once it has settled, either way. So there is only ever one waiting intent per series (`… · daily`), Stop ends it outright, and a refusal doesn't kill it — tomorrow is a fresh day. A series **claims every day from its own date onward** (`Daily autobook` on those tiles), otherwise a daily gym set for tomorrow would still offer Sunday as free. Setting an earlier daily series **replaces** a later one for the same class (`futureDailyIntents`, warned about in the panel: `Replaces your daily autobook starting Sun, Sep 6 (8:00 AM – 9:00 AM).`).

**A daily series can also be started from an ordinary booking (2026-09-05).** The `Daily` toggle now sits in the manual confirm panel's header too, on the same `buildSwitch`, with the same `futureDailyIntents` clash warning (`dailyClashText`, one wording shared by both panels). Before this, "every day" could only be said from a day whose slots the portal will not sell yet — you had to walk the day nav forward to a `later` tile to start a series, which is a strange place to have to go. The difference is one line, shown only once the toggle is on: `Books now. Each following day is tried as its window opens.` — this slot is booked outright, the rest are attempts.

The series itself is not a special case: `startDailySeries()` builds the same intent shape and hands it to `spawnNextOccurrence`, so it lands on the first day ahead that nothing else claims and obeys the same replacement rule (an earlier series supersedes a later one). Three things it does deliberately: it arms **only after** `createReservation` really returns a booking id, it runs **after** `removeIntent(claimedBy.id)` so the day that booking freed is a day the series may step onto, and it **saves nothing** when the lookahead finds no free day — replacing a future series in favour of a series that does not exist is worse than doing nothing, and the panel says so (`Daily not started — an autobook already covers the next 7 days.`). Success adds one line under the green panel: `Daily autobook starts Tomorrow. Stop it below.` — `shortDayLabel`, matching the row in `Booking automatically` it points at. The list is rebuilt in place (`repaintScheduledList`) rather than through `refreshAvailability`, which clears the panel being read.

**A fifth rule bug, from the same family (2026-09-05).** `spawnNextOccurrence` steps over days claimed by an *intent*, but the two settle paths that end an occurrence before `createReservation` never called it at all: `alreadyBookedThatDay` (a day claimed by a real **booking**) and `Date.now() >= start` (an occurrence whose slot began while the app was closed). Both marked the intent `failed` and `continue`d, so a series met the first day you had booked by hand — or the first morning you didn't open the app — and silently never ran again. Exactly bug 2 below, through a different door. Both spawn now; `?seed=series-clash` + `?auto=seed-result` shows the series stepping over the one-off to the day after.

Four rule bugs, found together (2026-09-04):
1. The one-per-day claim was computed as `sl.kind === 'later' ? conflictingIntent(…) : null`, so a claimed day still offered all its *open* slots. The claim is a property of the day, not of the tile's kind.
2. `spawnNextOccurrence` did `return` on an already-claimed next day, and since the next occurrence is only created on settle, nothing restarted it — a daily series ran until the first day you had booked by hand and then silently never again. It steps over claimed days now, up to `SERIES_LOOKAHEAD_DAYS` (7).
3. `scheduled` didn't compare dates, so a series reaching forward offered to cancel an intent belonging to a different day.
4. Caption precedence tested `blocked` before `bySeries`, showing a time from the wrong day.

Stopping a scheduled tile is **two taps**: the first arms `.is-confirming-cancel` with a danger tint and `Tap again to stop`, auto-resetting after 3s or on any outside tap; the second removes the intent (`removeIntent`) and refreshes availability.

**Not verified against the real portal**: the 24h model, whether `createReservation` accepts the exact `startTime` taken from availability (passed verbatim, as the real flow does), and whether "class" really is the cleaned resource name. First real contested use settles all three.

#### Voice/Assistant booking — explored, not started (2026-09-11)

The idea: "Hey Google, book my gym slot" without opening the app at all. Discussed, not built — no code changed for this. Three real paths surfaced, in order of how well they actually fit what "invisible, no app-open" means:

- **Android App Functions (`androidx.appfunctions`, Android 16/API 36) is the API actually built for this** — `AppFunctionService` is a `Service`, the same invisible-background model `MediaSessionService` gives Spotify's "play a song" (Assistant/media controls talk to the Service directly, no Activity, no window, by design), just generalized by the platform to any function any app wants to expose instead of being locked to the media domain. Confirmed this session that our own headless-native pattern already works end to end for the hard part — `AutobookReceiver`'s 2026-09-11 morning test (alarm fired at 08:22:56 against a **killed** process, notification posted 08:23:00, ~4s cold-start-to-answer) is functionally the same shape App Functions would need: dead process → OS spins it up → native HTTP call with bridged token/cookie → real answer, no WebView touched. The gap isn't "can this run headless" — that's solved. It's two things: (1) the Jetpack library generates its XML metadata schema via **KSP** (Kotlin Symbol Processing) at real Gradle build time from `@AppFunction`-annotated Kotlin, and our pipeline is hand-invoked `javac`+`d8` with no Gradle/KSP step at all; (2) as of mid-2026, Gemini's own ability to discover/call App Functions is still **private preview, trusted testers only** — unconfirmed whether it works on a normal sideloaded install.
  - **Chosen approach for the KSP gap, if this gets picked up: borrow Gradle once, don't fight KSP standalone.** Considered running KSP's command-line/standalone mode (`com.google.devtools.ksp:symbol-processing-cmdline`) directly, same spirit as sourcing `android-34.jar`/`build-tools` outside Android Studio — rejected for now: needs Kotlin (this codebase's native side is Java-only so far, `AutobookReceiver` included), and KSP's version has to exactly match the Kotlin compiler's, which is normally pinned automatically by Gradle and is a real source of opaque failures when hand-assembled. Cheaper plan: spin up one disposable real Gradle/Android Studio project, write the one `@AppFunction` method there, let Gradle's fully-wired KSP setup generate the XML the supported way, then copy just that output file out and hand-merge it into the APK from then on — same "merge the artifact in afterward, don't fight the framework for it" move already used for `classes5.dex`. Lower risk because it rides the path Google actually tests, at the cost of needing Gradle/Android Studio installed at all, even just once.
- **Classic App Actions (`shortcuts.xml` custom intents)** — stable since ~2019, still current docs (updated Feb 2026), no KSP/Gradle needed at all (resource XML + manifest `<meta-data>` + one small trampoline `Activity`, apktool-editable and `javac`+`d8`-compilable exactly like `AutobookReceiver` was). But its fulfillment type is documented as `actions.fulfillment.DEEPLINK` — explicitly "opens the app using a deep link" / "launches an Activity." No documented Service-based, no-Activity fulfillment for arbitrary custom intents — that headless treatment is reserved for the media/smart-home domains specifically. Best available with this path: `android:theme="@android:style/Theme.NoDisplay"` + an immediate `finish()` in the trampoline `Activity`'s `onCreate()` (must fire **before** `onResume()` runs, or Android throws) — hides the window, but the process still cold-starts underneath exactly like the App Functions case. If the trampoline reuses the app's own WebView/JS booking flow instead of a native HTTP call, it also pays this app's full cold-boot cost — measured this session at `boot cover removed (ready, 6600ms)` just to get the portal page ready, before a booking call even starts. Doing it native-only (`AutobookReceiver`-style, bridged token/cookie, raw `HttpURLConnection`) stays in the same ~4s ballpark as the alarm test instead.
- **Tasker + AutoVoice is the workaround people actually use today, and needs none of the above.** Google granted Tasker a special App Actions integration (`"Assistant Action"`) so `"Ok Google, ask AutoVoice to <phrase>"` / `"Ok Google, run my task in Tasker"` triggers a Tasker Task directly — Tasker's own entry point is already an invisible trampoline, and a Task runs headless by default. Tasker has a plain HTTP Request action, and this repo already has the entire Aura call shape reverse-engineered (`aura_map.md`: `message`/`aura.context`/`aura.token`/cookie, `CustomBookingController.createReservation`'s params) — a Tasker task could fire that POST directly, given a token/cookie fed to it the same way `bridgeAuthToPrefs()` already extracts one. This bypasses MyFLAME's app process, the WebView, App Actions on our own manifest, and App Functions' preview gate entirely, because Tasker is the thing actually registered with Assistant, not us. Real trade: it's *Tasker* holding the token/cookie and making the call, not MyFLAME — a separate app to trust and maintain, not a change to this codebase at all.

### Gyan tab

- `.fr-gyan-page` is a flex column filling `.fr-content` edge to edge — the first tab to do so; history lives in an in-memory `gyanState.messages`, cleared on a real reload like everything else session-scoped. `.fr-gyan-list` gets `margin-top: auto` so a short exchange hugs the composer (every chat convention does; the first version had it flush to the header with a dead gap below) and steps aside once it overflows. The welcome state gets `margin: auto 0` instead — an explanation, not a conversation.
- **Mount is non-blocking**: the layout paints synchronously and context initialises in the background. The composer stays enabled the whole time — while initialising and while a turn is running — and the **send button** carries the spinner and the disabled state. Waiting to be allowed to type was the worst part of the old spinner.
- **Time-of-day prompt chips** (`getSmartGyanChips`), always alongside `Next class` and `Sports slots`: before 11:00, breakfast + lunch; to 16:00, lunch + dinner; to 19:00, snacks + dinner; after that, dinner + tomorrow's breakfast.
- `parseGyanBold` + `escapeHtml` render `**bold**`/`*bold*` as `<strong>`. *Known limitation*: on devices whose system font lacks a 700 weight this may not look bold.
- **What is disabled, and when.** `inputEl` never is — typing works immediately. Send, **New chat** and the prompt **chips** share one predicate, `gyanBusy()` = `sending || loadingReady`, so a turn can't be orphaned mid-flight. The chips need their own `disabled` on top of that: they submit by dispatching the composer's own submit event, which does not care that `sendBtn.disabled` is set, so one tapped during init used to fire a turn the UI was visibly refusing.
- **Busy means a request is in flight — not `!gyanState.ready` (2026-09-04).** Keyed off `!ready`, a failed init or a failed turn (whose `catch` clears `ready`) left Send and New chat disabled with nothing left to re-run init: the tab was dead until you left it and came back. An init that finished and failed is a retry waiting to happen, so the composer unlocks and the next send starts a fresh `ensureGyanReady()` — which is also kicked off *before* the first `paintMessages()`, since it sets `loadingReady` synchronously and the opening frame should carry the spinner rather than an enabled Send for one tick.
- The stock welcome text references a "Start chat" button that doesn't exist here; the fallbacks are rewritten (`Ask about classes, facility bookings, campus info, or anything else Gyan can help with.`).

### Loading, caching and motion

**The rule a skeleton follows here**: it is the **real element** with its unknown text swapped for a shimmering block of the same metrics — never a lookalike assembled from bare divs. `.fr-skel` is an inline-block holding a non-breaking space, so its line box is exactly one line of whatever type its parent uses: same font, line-height and baseline. Width is the only thing a caller passes, in `ch`, and it is a guess about content length that nothing in the layout depends on. Shimmer is a `background-position` gradient sweep, 1400ms linear, off under `prefers-reduced-motion`.

- **Home** paints its title, date strip and headings from the first frame — none need data. Three class rows and one booking row shimmer; three because that is a normal day, and a skeleton that guesses high leaves a hole when the data lands short. The strip's has-events dots are the one thing that appears late, since which days carry classes is exactly what isn't known.
- **Home's first paint comes off the last launch's data**, kept in `localStorage['flame-data-cache']` (`src/persist.js`). Home is the boot tab and the only one that needs it — by the time Calendar or My Bookings is opened, Home's own revalidation has filled the in-memory cache they read.
- **The stale-repaint jitter is gone.** The cache paints, the request still goes out, and the answer is compared against what is on screen (`sameData`, `src/persist.js`) and **dropped when identical**. The timetable is semester-static, so the common case was tearing down the whole page to rebuild the same page a second after the tab opened.
- **Book Slot** used to wait on `getResources` before painting a pixel, and everything above the grid is drawn from that list. It is cached on its own clock — `flame-resources-cache`, 7-day TTL, separate from Home's blob because they expire for different reasons: a day-old class list is nearly right, a week-old resource list is simply correct.
- The grid's times are not a guess: `knownSlotTimes(resource)` derives them from the operating window (`Gym ( 6:00 am to 2:00 pm slot )` is eight hourly slots), falling back to what that resource last offered. Real labels, real card count; only the capacity line shimmers. `createSlotShell` builds the box and the answer **fills the same nodes** by `dataset.start` — nothing moves. A card is created late only if the skeleton couldn't predict it, and anything predicted that the day didn't have is dropped.
- **Past times are not predicted.** A slot that has already started can't be booked and the portal doesn't list it, so a skeleton built from the full window drew cards that vanished — open the tab in the afternoon and half the grid disappeared. The predicted list uses the same `start > now` predicate the fill already used. Late enough that nothing is left, there is nothing truthful to draw and it falls back to the spinner, then to "No open slots".
- The claim note and the scheduled-autobook list are in the first paint too — both come from `localStorage`, so holding them back only pushed the grid down a line at the worst moment.
- The spinner survives in the one honest case: a resource with no window in its name and nothing remembered.
- One defect from that pass, worth remembering: skeleton cards inherited `.fr-slot:disabled { opacity: 0.4 }` — the *blocked* look — which also dimmed the card's time, the one thing already true.
- **`morphHeight` (`src/dom.js`)** animates the bookings section between heights (280ms) for the case geometry can't remove: the skeleton stands one booking tall and the day holds three. Removing the jump comes first — a 130px shift can't be animated into something pleasant. **No `requestAnimationFrame` in it**: the second height is written synchronously after an `offsetHeight` read flushes layout. `transitionend` is the fast cleanup path and a timer does it regardless, so a dropped transition leaves a correctly sized element rather than one frozen at a stale height. `.fr-morphing` carries the transition so reduced-motion can take it away.
- In-memory `cache.events` / `cache.bookings` / `bookState.facilities` are populated once per load and invalidated only by a real mutation. Nav clicks used to call `switchTab()` — a full spinner and a fresh round-trip just to change the date — and now re-render against the cache; only Book Slot's results wrap refetches.

### Layout rules that were hard-won

- **`compactTimeRange(start, end)`** drops an on-the-hour `:00`: `"10:00 AM" + "11:00 AM"` → `"10 AM – 11 AM"`; `:15`/`:30` keep their minutes; an unrecognised format is left alone. This is what actually made the numbers fit — a two-up tile at 390px has ~130px of text width. It also dropped the *leading* meridiem when a range didn't cross noon (`"10 – 11 AM"`), which fit even better and was rejected as "a little too minimal"; both meridiems are kept. The regex tolerates the narrow no-break space newer ICU puts before the meridiem.
- Everything that shouldn't wrap is one line + ellipsis: `.fr-cal-day-header`, `.fr-slot-cap`, `.fr-sched-name`/`-note`, `.fr-banner-title` (its note wraps on purpose), `.fr-facility-item`, `.fr-book-field-label`, `.fr-book-results-title`, `.fr-confirm-panel-title`, `.fr-group-heading`, `.fr-datestrip-weekday`/`-daynum`, plus `white-space: nowrap` on `.fr-btn` and `.fr-badge` — a button label that wraps is always a bug.
- **`white-space: nowrap` converts a wrap into an overflow**, so a clean wrap report proves nothing on its own. That is why `?auto=wrapcheck` also flags any element wider than its box whose computed `overflow-x` is `visible`. It caught the one real regression these fixes introduced: `.fr-book-layout`'s `1fr` tracks default to a `min-content` minimum, so a single unbreakable facility name widened its column past the viewport and took the page sideways (531px of content in a 450px box at 360px). Both tracks are `minmax(0, 1fr)` now.
- Mobile's `.fr-row { justify-content: flex-start }` (fixing a dead gap between time and title on Home, caused by `space-between` with only two flex children) was too broad and broke My Bookings' right-pinned pill and Cancel button. Scoped with `:has()`: `.fr-row:has(.fr-row-time)` — `.fr-row-time` exists only on class rows.
- `.fr-input` had no `::placeholder` rule, so placeholders fell back to near-black on a dark field — invisible. `(optional)` was later dropped from the Purpose/Co-attendee placeholders.
- Slot grid floor is 146px (still two columns at 360px).
- In `styles.css` the `\00a0` escape is single-escaped. It was `\\00a0` only while the CSS lived in a template literal, where a bare `\0` followed by a digit is an illegal octal escape — a hard `SyntaxError`, caught by `node --check`.
- `dateInput.valueAsDate` reads and writes in **UTC**, silently showing yesterday near midnight IST. There is no native date input anywhere in the app now.
- `el.hidden = true` is defeated by an element's own `display:flex` class rule — author CSS beats the UA `[hidden]` rule at equal specificity.

### Still open: long class titles truncate on Home

Measured in the reskin's own font and sizes, worth not re-deriving:

| Course | one line | lines @234px (today) | lines @322px (full width) | px to fit one line @234 |
|---|---|---|---|---|
| Applied Formal Methods | 187px | 1 | 1 | 20.1px |
| Principles of Machine Learning | 237px | 2 | 1 | 15.8px |
| Design and Analysis of Algorithms | 264px | 2 | 1 | 14.2px |
| Entrepreneurial Failure and Sustenance | 302px | 2 | 1 | 12.4px |
| Business Plan Development and Entrepreneurial Finance | 435px | 2 | 2 | **8.6px** |

- **Shrink-to-fit is ruled out** — the longest real name needs 8.6px, half the row above it. Asked and answered; don't revisit.
- The left time column costs 88px (78px min-width + gap) and is the whole problem: at full width 5 of 6 real course names fit on one line, and all 4 `room · faculty` strings fit (3 of 4 don't at 234px).
- Candidates: **(A)** clamp the title to two lines with height reserved, so rows stay equal — still truncates faculty; **(B)** move the time to a small line above the title so text gets full width, reserving two title lines to keep cards equal.

## Source layout and build

```bash
npm install     # once
npm run build   # src/ -> portal-reskin.user.js -> .patch-tools/hook.js
npm run watch   # same, on every save
```

**Edit `src/`, never `portal-reskin.user.js`.** That file stays tracked — it is what gets pasted into Tampermonkey, what `preview.html` loads and what a release ships — but it is generated. `npm run build` is ~210ms; a watch rebuild is 50–110ms, of which the hook fuse is ~1.4ms, which is why watch does it too: skipping it bought nothing and left `hook.js` stale for exactly as long as you were iterating.

One command produces both artifacts on purpose. `hook.js` carries the whole userscript as a string constant, and an APK built from a stale constant runs the *old* UI while every other check still passes — a failure this project hit more than once while the two were regenerated by hand.

Modules: `aura.js`, `chrome-hide.js`, `styles.js` + `styles.css`, `toggle.js`, `icons.js`, `dom.js`, `dates.js`, `shell.js`, `autobook.js`, `persist.js`, `state.js`, `tabs/{home,calendar,bookings,book-slot,gyan}.js`, `main.js`.

Two things the single-file version got for free:

- **Shared mutable state.** `let x = null` in one closure could be reassigned from anywhere; an ES module binding is read-only for importers. `src/state.js` holds the values genuinely written from more than one module (`ui.root`, `ui.contentEl`, `ui.bannerHost`, `ui.toggleBtn`, `ui.activeToken`, `cache.events`, `cache.bookings`) as fields on two objects, so assignment goes through a shared object. Anything written only by its owner stayed a plain `let`.
- **A dependency cycle.** The shell named the five renderers while every tab imported `switchTab`/`renderEmpty` back out of it. `shell.js` exports an empty `RENDERERS` plus `registerRenderers()`, which `main.js` calls at boot. Tabs → shell, never back.

**Rollup, not esbuild.** esbuild builds in 7ms and **discards ordinary comments** — it dropped ~400 comment lines, which in this repo is most of the recorded reasoning. Rollup keeps the source's shape: same IIFE, comments intact.

Two bugs the split produced, both caught by the harness rather than by reading:
1. The import inference blanked template literals before scanning for free identifiers, so `` `${shortDate(days[0])} …` `` looked like it referenced nothing — Calendar's week view silently did nothing, `shortDate` being an undefined global. Caught because `?auto=wrapcheck` reported 49 text runs where the pre-split run reported 102.
2. `build.mjs` filled the wrong `__USERSCRIPT__`: `hook.src.js`'s header comment explains the placeholder and therefore contains the token, and an unanchored `replace()` took that first occurrence — a 130KB comment and a hook that injected nothing, with no error anywhere. The placeholder is now matched **with its surrounding quotes**, asserted to appear exactly once, and the fused output is checked for `flame-reskin-root` before being written.

`.patch-tools/regen-hook-script.py` was deleted when `build.mjs` took over its job — two things able to write `hook.js` is how they drift.

## Local QA harness

`preview.html` loads the unmodified built script and feeds it real captured data through a `window.__FLAME_RESKIN_PREVIEW__` stub that `callAura()` short-circuits to (inert on the real site). No portal session needed. Screenshots via system `chromium --headless=new` (Playwright's own download is blocked in this environment); output in `.review/`, gitignored.

```bash
chromium --headless=new --window-size=390,844 \
  --screenshot=shot.png "file://$PWD/preview.html?tab=home"
```

- `?tab=<id>` picks the boot tab. `?auto=<scenario>` runs scripted clicks for states a screenshot can't reach alone — including real `dispatchEvent(new PointerEvent('pointerdown'))` gestures, with Chromium's `--virtual-time-budget` carrying timers such as the 550ms long-press.
- **`?auto=wrapcheck`** (+ `-week`, `-cancel`, `-confirm`, `-sched`) walks every element rendering text directly and counts the lines it actually occupies, from the client rects of a `Range` over its own text nodes — what the renderer did, not what the CSS claims, the CSS being the thing under test. Plus the overflow half described above. `WRAP_OK` is the registry of text *meant* to wrap (chat bubbles, empty states, success/error panels, the autobook banner's note — a truncated refusal reason is worse than a two-line banner); add to it deliberately, with a reason, never to quiet a finding. It reports the number of runs scanned, so "0 unplanned" can be told from "the page never rendered".
- **`?long=1`** swaps every portal-supplied string for a much longer one. Pair it with wrapcheck; screenshots stay on real data.
- **`?auto=measure` / `cancel-measure`** dump every `.fr-row`/`.fr-slot`/`.fr-datestrip-cell`'s computed height. "Are these the same height" is a question for numbers, not for a PNG — this is what caught the 80px-vs-70px drawer after it passed a visual review.
- **`?auto=calfit`** — calendar block content height against box height.
- **`?auto=confirm-scroll` / `-auto`** — nav height, view band, button band and `scrollTop` against the scroller's maximum, reporting both "at bottom" and whether the button is clear. PASS at 390x780, 360x640, 360x500, 1280x900, with and without `?long=1`. The autobook variant is the one that catches a panel calling a function it can't see.
- **`?auto=picker-reselect`** — picks another option, **reopens**, asserts exactly one `.is-selected` and one `aria-selected`.
- **`?auto=sched-rule` / `-cross`**, `?seed=twin` + `?auto=seed-result`, `?auto=series-covers`, `?seed=series-clash` — the one-per-day and daily-series rules, asserted.
- **`?auto=daily-from-book`** — a series started from a normal booking, end to end: the toggle is present, the note and the clash line say the right thing, the booking succeeds, the armed occurrence lands on the right day, the row appears without a grid repaint, and the day it reaches is claimed. It **walks forward to a day whose Confirm is live**, since the captured bookings already hold today and a disabled panel asserts nothing. Pair it with `?seed=daily-future` (a later daily to replace) and `?seed=week-claimed` (every reachable day taken by one-offs, the only way to reach `Daily not started`). `?auto=daily-panel` is the screenshot state — the panel with the toggle on, which nothing reaches by itself.
- **`?seed=due`** plants an already-open intent so the runner fires on load and its banner can be screenshotted — otherwise only observable by waiting a day. Both seeds search the gym's own window across today and tomorrow for the first hour still ahead and inside 24h, so they work at any clock time; they used to hardcode "1 PM today" and silently produced a failure banner after 1 PM.
- **`?auto=coldpaint`** with `?slow=<ms>` and `?seed=persisted` — proves the stale paint *and* the repaint (in-flight shows `STALE …`, settled shows the real name). `?bookings=<n>` sets how many upcoming reservations exist: `0` is the only way to reach the empty row, and >1 makes the section resolve taller than the skeleton stood.
- Others: `book-confirm`, `book-confirm-room`, `book-confirm-fail`, `resource-picker-*`, `cal-next`, `book-slot-next`, `gyan-chat`, `schedule`, `schedule-twice`, `autopanel`, `confirm-warn`, `shiftcheck`, `banner-anim`, `authstate`.
- **Runtime errors are a reported failure.** A thrown `ReferenceError` renders as an empty or half-built tab, which in a screenshot reads as a styling problem — that is how the `shortDate` bug survived its first look. `preview.html` traps `window.onerror`, `unhandledrejection` and `console.error` into `<pre id="errors">` (or `no errors`) for `--dump-dom`, on every scenario.
- Scenarios that need a bookable or schedulable tile **hunt for a day that has one** (`findDayWith()`); hardcoding an offset made them pass or fail by the hour. The availability stub models the 24h window and serves the real captured 6 AM–2 PM gym day, so two-digit hours and the noon crossing are always on screen. The confirming row's colour transition is frozen for screenshot determinism — the virtual clock kept catching it half-tinted, which looked like a real bug.

Last full sweep: 39 scenarios at 390 and 1280, zero runtime errors.

## Android

### What the app is

`com.myflame` is a **React Native app wrapping `react-native-webview`** pointed at `https://my.flame.edu.in/s/` — not Salesforce Mobile Publisher. Evidence: `libhermes.so`, `libreactnative.so`, `libfbjni.so`, `librnscreens.so`; logcat shows `RNCWebViewManager`/`RNCWebViewClient`; `assets/index.android.bundle` is precompiled **Hermes bytecode** (magic `C6 1F BC 03`), not patchable JS. It is a **split-APK install** (base + `arm64_v8a`/`en`/`xxhdpi`), and the arch split carries the whole RN runtime — any repackage must merge all four, via `APKEditor.jar` (`apkeditor m -i <dir> -o merged.apk`).

It is still a real `android.webkit.WebView` underneath, so the Frida-gadget approach applies; RN only means the hooked class is discovered at runtime rather than assumed.

**iOS**: a resigned IPA via AltStore/Sideloadly would work for a config/JS patch, but real runtime hooking without a jailbreak isn't viable. Not attempted, not worth it.

**Device**: Xiaomi 23127PN0CG (`houji`), Android 16 / SDK 36, wireless debugging via `adb connect`.

### The build pipeline

```bash
npm run build                       # writes .patch-tools/hook.js
cd .patch-tools && source venv/bin/activate
export PATH="$PWD/build-tools/android-14:$PWD/venv/bin:$PATH"
export JAVA_TOOL_OPTIONS="-Xmx6g"
../node_modules/.bin/frida-compile hook.js -o hook.compiled.js -T none
objection patchapk -s flame-merged.apk -a arm64-v8a \
    -c gadget-config.json -l hook.compiled.js -j 1
adb install -r flame-merged.objection.apk
```

- `hook.js` must start with `import Java from 'frida-java-bridge';` — a CJS `require()`'s interop breaks the bundle's default export.
- `frida-compile` and `frida-java-bridge` are **repo-root** deps, declared in the root `package.json` and installed at `<repo>/node_modules`. (`.patch-tools/venv/bin/frida-compile` also exists — it ships with the python frida-tools and is a different program; the npm one is what this pipeline is built on. Earlier sessions found these at `/home/archer/node_modules`, because npm walked up to the nearest `package.json`.)
- Patch the **merged** APK with the **compiled** script, never raw `hook.js`.
- **`adb install -r`, never uninstall first.** objection reuses the same debug keystore across builds, so this upgrades in place and preserves the logged-in session; `adb uninstall` wipes app data and forces a full Google re-login. Only uninstall when switching to a differently-signed build (e.g. back to Play Store stock). A side effect worth knowing: a self-signed build has `installerPackageName=null` and so never receives Play Store updates — reverting is `adb uninstall com.myflame` plus a Play Store install, after which the whole pipeline has to be re-run against the new base APK.
- Verify by checking that `lib/arm64-v8a/libfrida-gadget.script.so` inside the output APK byte-matches `hook.compiled.js`, and that the build log contains no `OutOfMemoryError` — the two "may have failed" lines objection prints are its standard boilerplate; grep for the OOM string, not for those.

**`.patch-tools/fast-rebuild.sh` skips `objection patchapk`'s decompile step on every iteration where only the script changed** (2026-09-10) — decompile and recompile were costing ~48s together, but only the recompile actually depends on anything that changes between iterations (the compiled hook script is one file, `lib/arm64-v8a/libfrida-gadget.script.so`); the base APK's smali and resources are the same every time until `flame-merged.apk` itself changes. `objection patchapk -k` (`--skip-cleanup`) leaves its decompiled-and-injected working directory behind instead of deleting it; that directory, moved to `.patch-tools/decompiled-persistent/`, is the reusable part. The script copies the new `hook.compiled.js` into it and runs only `apktool build` + `zipalign -p 4` + `apksigner sign` — the exact three commands objection's own `build_apk`/`zipalign_apk`/`sign_apk` run internally (read from `objection/utils/patchers/android.py` in the installed package, keystore/pass/alias included: `objection.jks`, `basil-joule-bug`, `objection`), just without the `apktool d` in front of them. Cuts a script-only iteration to ~7s. **Stale the moment `flame-merged.apk` changes** — regenerate `decompiled-persistent/` with a fresh `objection patchapk -k` run (and re-`cp` its temp dir over) whenever the base APK is rebuilt.
- `gadget-config.json` is `{"interaction": {"type": "script", "path": "libfrida-gadget.script.so"}}` — autostart mode, per objection's documented convention.
- `objection patchapk -l <script>` **refuses to run without `-c <config>`** (`A script source was specified but no gadget configuration was set.`). For a plain listen-mode diagnostic build — no embedded script, external `frida -U -n … -l script.js` attach instead — omit `-l` entirely rather than pairing it with a listen-type config.

**Rebuilding the toolchain from nothing**: `jdk-openjdk` + `android-apktool` from pacman/AUR (needs sudo, run by hand); `.patch-tools/venv/` is a Python venv with `pip install objection frida-tools`, which keeps the Python side out of sudo; `.patch-tools/build-tools/` is an extracted `build-tools_r34-linux.zip` (direct download — no `sdkmanager` or license flow) purely for the classic `aapt`, `apksigner` and `zipalign` binaries; `.patch-tools/APKEditor.jar` is a REAndroid/APKEditor release download.
- Tracked in `.patch-tools/`: `hook.src.js`, `gadget-config.json`, `probe.js`. Everything else there (venv, build-tools, decompiled smali, APKs, screenshots — ~1GB across 32,736 files) is ignored and regenerable. **Keep the screenshots ignored regardless: they show the portal with a real name and roll number on screen.**

### Bugs and traps, all confirmed against real behaviour

1. **`-a arm64` vs `-a arm64-v8a`.** The short form makes objection name the folder `lib/arm64/` instead of the real ABI name, so the linker never finds the gadget (`UnsatisfiedLinkError: … library "libfrida-gadget.so" not found`) even though the file is in the APK. Always pass exactly what `adb shell getprop ro.product.cpu.abi` returns.
2. **`OutOfMemoryError` during apktool's smali rebuild.** The default heap is too small for this app's multidex/RN tree, and one rebuild thread dies silently while the process still reports success and goes on to sign a **corrupted APK**. `JAVA_TOOL_OPTIONS=-Xmx6g` plus `-j 1`. Dangerous precisely because objection doesn't hard-fail.
   - The env var works **through objection** (it prints its own "Picked up JAVA_TOOL_OPTIONS" line) but is **silently ignored when calling apktool directly**: `/usr/bin/apktool`'s launcher hardcodes `javaOpts="-Xmx256M"` on the `java` command line, and an explicit later flag beats an env-injected one. Builds have nonetheless succeeded at the default 256M on this app and machine, so the OOM may be specific to a parallel (`-j` > 1) build. If one recurs while calling apktool directly, the real override is its own `-J` passthrough (`apktool -JXmx6g b …`).
3. **`Java.use('android.webkit.WebView')` crashes Frida's Java bridge if called too early** (`access violation accessing 0x0` inside `/frida/bridges/java.js`): the real implementation lives in the WebView provider's own classloader, which may not be in the process yet, and Frida's search picks up an incomplete stub. Wrap in try/catch + `setTimeout(retry, 1000)`.
4. **Frida spawn mode needs root.** On a jailed device `frida -U -f <pkg>` gives `Failed to spawn: need Gadget to attach on jailed Android`; only `-n <process>` attach works, which inherently races `setWebViewClient`. Fine for the autostart config (the gadget runs from `System.loadLibrary` time), but it means live attach cannot test the "catch the first `setWebViewClient`" behaviour — only the real autostart timing can.
5. **The WebView doesn't exist until after sign-in.** The app shows a native RN login screen first, so `Java.choose('android.webkit.WebView', …)` finds zero instances at startup. Poll. This does **not** mean the hook has to wait: `WebView.setWebViewClient.implementation` is a class-level intercept that waits indefinitely, and was confirmed installing while the native login screen was still up and firing correctly minutes later, after real sign-in. The real client class is `com.reactnativecommunity.webview.RNCWebViewClient`.
6. **WebView is main-thread-affine.** Calling `evaluateJavascript` from Frida's own thread throws `"A WebView method was called on thread 'Thread-7'…"`. Use `Java.scheduleOnMainThread`. This never affected the production hook — `onPageFinished` is delivered on the main thread already.
7. **A hook's method argument is only a valid wrapper for the synchronous duration of the hook call.** Using the `view` passed into `onPageFinished` inside a later `scheduleOnMainThread` callback throws `Wrapper is disposed; perhaps it was borrowed from a hook…`. `var retained = Java.retain(view)` synchronously first. (`Java.cast()` produces an independent wrapper, so paths using it never hit this.)
8. **`evaluateJavascript(js, null)` reports neither success nor failure.** Register a real `ValueCallback` returning a status string — readyState, URL, body child count, computed background. Both failure paths log now.
9. **Frida's `setTimeout` does not reliably fire when called from inside a Java callback.** A 150ms grace scheduled from within `ValueCallback.onReceiveValue` (Android's main thread, not Frida's JS thread) never ran.
10. **`requestAnimationFrame` will not fire under an opaque native cover** — Android can skip drawing a fully obscured WebView, so no frames are produced. Two nested rAFs is the textbook way to wait for a paint and it deadlocks here. Use existence checks plus a bounded give-up. This renderer starves rAF in other situations too (see the confirm-button scroll).
11. **`apktool -J` must come before the subcommand** — `apktool -JXmx6g b <dir> -o <out>`. The wrapper only strips `-J` args from the very front.
12. **`apktool b` can silently reuse a stale build** — editing `apktool.yml` alone prints `AndroidManifest.xml and resources have not changed`. Always `-f`, then verify with `aapt dump badging`.
13. `objection patchapk` needs the classic **`aapt`** specifically (not `aapt2`) for `aapt dump badging`; that is what `build-tools/` is there for, independent of apktool, which bundles its own `aapt2` and doesn't need a system one (its `--skip-resources` flag only skips its own decompilation step, and is unrelated to objection's check).
14. Two environment nuisances that look like build failures and aren't: **USB drops** (`lsusb` empty, `adb devices` empty) — reseat the cable before suspecting the tooling; and **HyperOS's on-phone install confirmation**, which appears intermittently even with USB debugging long since approved — `INSTALL_FAILED_USER_RESTRICTED: Install canceled by user` means look at the phone screen, tap through, and re-run the same command.
15. **`setTimeout` unreliable from *any* Java callback, not just `ValueCallback.onReceiveValue`** (2026-09-10) — confirmed again scheduled directly from inside `onPageFinished`: a one-shot `setTimeout(fn, 4000)` there never ran, no error, nothing. `setInterval` (as `revealWhenReskinPaints` already does) is the general workaround, not a one-off fix scoped to that specific callback — treat every Java-callback-originated `setTimeout` as suspect on sight.
16. **`FileWriter.write(String)` through Frida's Java bridge throws `write(): argument types do not match any of`** — overload resolution ambiguity the bridge can't settle for a plain JS string against `FileWriter`'s several `write` overloads. `FileOutputStream.write(byte[])` has exactly one overload and is unambiguous: `Java.use('java.lang.String').$new(str).getBytes()` then `fos.write(bytes)`.
17. **Wireless adb (Android 11+ wireless debugging) on this phone drops mid-command repeatedly** (2026-09-10) — goes `offline` or disappears from `adb devices` entirely a few seconds into a command, port changing on each reconnect. Most likely the same HyperOS background-kill aggression the native-autobook work above is trying to route around, this time pointed at adb's own idle socket rather than an app process. USB is markedly more stable for anything spanning more than a few seconds — prefer it for install loops or any test spanning a real idle gap.
18. **This `d8`/R8 build (8.2.2-dev) has a real internal bug on a specific anonymous-inner-class shape** (2026-09-11) — dexing a `javac`-compiled `AutobookReceiver$1.class` (from an inline `new Runnable() {...}`) threw `NullPointerException: Cannot invoke "String.length()" because "<parameter1>" is null` deep in R8's own class-graph code, reproducibly, whether passed as a bare file argument or packaged in a jar. Not an argument-passing mistake — a **named** static inner class implementing the same interface compiles and dexes cleanly with identical logic. If a `d8` invocation throws an internal (not a normal compile) error on one specific class and not others, suspect the class shape itself before the build command.
19. **A missing class in a dex file is not a build-time error — it's a runtime `NoClassDefFoundError` the moment that class is first touched.** The `d8` command in trap 18, run against only the outer class file (forgetting the compiler-generated inner class), *succeeded* and produced a dex file that was silently missing a class the code actually needed. The build reported success; the app crashed later, only once the alarm fired and tried to use the missing class. Always verify a hand-assembled dex's class list (`dexdump ... | grep "Class descriptor"`) against what the source actually compiled to, rather than trusting a clean exit code.
20. **`evaluateJavascript`'s `ValueCallback` wraps a string result in its own extra layer of JSON encoding.** If the evaluated JS itself returns `JSON.stringify(x)` (a string), the callback's `value` is `JSON.stringify` applied *again*, to that string. One `JSON.parse` only unwraps back to a plain string, not the real object — silently: `parsed.someField` is just `undefined`, no error, until something downstream (like `JSON.stringify(undefined)`, which is the literal value `undefined`, not the string `"undefined"`) throws somewhere unrelated-looking. Needs `JSON.parse(JSON.parse(jsonStr))` when the evaluated JS expression's result is itself a JSON string.

### How autostart injection was finally made to work (2026-09-01)

Live `frida -U -n` attach worked early and repeatedly; **autostart (`type:"script"`) mode showed nothing at all** — no reskin, and not one log line, not even the first statement of the file. The gadget `.so` loaded cleanly every time (`nativeloader: … ok`) and all three libs were present at full size in the app's extracted lib dir, so it was never a packaging problem. The chain, each layer real and only findable in order:

1. **The gadget's script mode has no ambient `Java` global and no module resolution.** The interactive `frida` CLI auto-bundles and polyfills `Java` as a convenience — which is exactly what masked this through every earlier "it works!" live-attach test. A file handed to `objection patchapk -l` is raw text dropped into the gadget's script slot and gets none of it.
2. **Diagnosed with a logger that does not depend on the Java bridge**: `liblog.so`'s `__android_log_print` called directly via `NativeFunction`, tag `FlameInjectNative`, with checkpoint **A** as the file's first statement and **B** as the first statement inside the `Java.perform` callback. This is the technique to reuse for any future script-mode problem — **the moment your diagnostic goes through `console.log` or `android.util.Log`, a broken Java bridge silences the diagnostic along with the thing it is diagnosing.** (Interpreting it: A absent means the script never runs at all — stop editing its contents; A present and B absent means the Java VM never becomes ready in that context, so poll `Java.available` + `Java.performNow` instead of a blocking `Java.perform` at load time.)
3. `require('frida-java-bridge')` as a bare CJS call **silently killed the whole script** — not even checkpoint A printed. Nothing resolves `require` in the raw gadget engine.
4. Running it through **`frida-compile`** got `Java` to resolve as a real object, but `typeof Java.perform` was still `undefined`: the default bundle format is ESM, and CJS `require()`'s interop shim hands back the wrapper rather than the bundled module's default export.
5. **The fix is ES syntax**: `import Java from 'frida-java-bridge';` as the very first line, matching frida-compile's ESM output. Logcat then reads `typeof Java.perform=function, Java.available=true` and the hook fires end to end.

**Test protocol, if this is ever re-run**: actually sign in on the device during the capture window. `setWebViewClient` legitimately cannot fire before a WebView exists, so an app left sitting on the native login screen proves nothing about the later lines — only the absence of checkpoint A is signal on its own.

### Status bar, nav bar and the cold launch

**Status bar** — `window.setStatusBarColor()` (and `setNavigationBarColor`) is a documented **no-op** at `targetSdk=36` (confirmed via `adb shell dumpsys package com.myflame | grep targetSdk`): Android mandates edge-to-edge for SDK 35+ and ignoring the deprecated colour setters is a listed consequence, not a Frida problem. **Downgrading `targetSdkVersion` to 34 made it worse** (a double-height white strip, because the app's native code opts into edge-to-edge independently) — reverted immediately, **do not repeat**. The fix is to paint a real opaque `android.view.View` into the `DecorView`, anchored top, height from `WindowInsets.getInsets(statusBars()).top` with a `status_bar_height` resource fallback (the nav-bar side uses `navigation_bar_height` the same way) (`paintStatusBarOverlay()`, deduped via `findViewWithTag`). Not a system colour API, so not subject to the deprecation — this is what edge-to-edge-aware apps do themselves. `insetsController.setSystemBarsAppearance(0, 8)` still works and is kept for light icons.

**Nav bar** — `env(safe-area-inset-bottom)` isn't reliably populated by this WebView, so `pushNavBarInset()` measures `getInsets(navigationBars()).bottom` natively and pushes it in as `--flame-navbar-inset` (device px → CSS px via `devicePixelRatio`). The stylesheet declares it with an `env(safe-area-inset-bottom, 0px)` default (harmless 0 on desktop) and uses it in `.fr-nav`'s padding and height and `.fr-content`'s bottom padding, inside the mobile media query.

**The triple loading flash** (RN splash → white stock loader → reskin in *desktop* layout → another white loader → reskin) had two causes:
- `onPageFinished`'s URL match hit both the SSO `frontdoor.jsp` bounce and the real page, so the reskin was injected into a transient stub and thrown away. Narrowed to `my.flame.edu.in/s/`.
- Android WebView paints solid white until the page's own background loads. `this.setBackgroundColor(DARK_BG)` (`0xFF11131A`, matching `--bg`) in the `setWebViewClient` hook — the earliest available point, before any navigation.
- The desktop-layout flash specifically: `react-native-webview` defaults `useWideViewPort=true`, so a page with no `<meta name="viewport">` (which `frontdoor.jsp` is) is laid out against a fake ~980px viewport and `@media (max-width: 760px)` loses. `setUseWideViewPort(false)` as belt-and-braces. It does not pin a width or break responsiveness — it only removes the "guess 980px" fallback.

**The cold-launch cover (2026-09-04)** — a recording showed ~7.75s to content, of which ~1.75s was white Salesforce "Loading…" cards and ~1.1s our own spinner. What ships is an opaque `FrameLayout` tinted `--bg` with a centred indeterminate `ProgressBar` tinted `--accent` (`0xFF5B8CFF`), added to the `DecorView` in the `setWebViewClient` hook and removed once `#flame-reskin-root` exists, `setClickable(true)` so taps can't reach a portal nobody can see. On-device: cover up ~3.2s, removed 87ms after injection, no white at any point.

**Why CSS could not do this** — three attempts (a `<style>` hiding `body > *` at `onPageStarted`, then at `onPageCommitVisible`, then on a 100ms re-apply loop), each better, none working: **a style injected into a document is only as durable as that document**, and a cold launch has three — the SSO bounce, *the same page after it rewrites itself* to render Salesforce's loading card (this is the one that replaces `<head>`), and finally `/s/`. A re-apply loop can only shorten the flash to one tick; the wipe and the repair are always in that order. `setBackgroundColor` survives because it is native, so the fix belongs at that layer.

**Tried and rejected: injecting at `onPageStarted`.** It works and was reverted. Measured `onPageStarted` → `onPageFinished` on `/s/`: 1.383s, 1.508s, 1.644s — but the actual saving was **548ms** (3432ms → 2884ms of cover time), because the reskin only paints at `DOMContentLoaded`, 1.18s after injection. Behind a cover, ready at 2.9s or 3.4s looks identical, and the visible parts were already taken by the caches. It costs a second injection point, a delivery guard and double-boot semantics — and the guard can't live in the script: rollup hoists every module body into one IIFE, so `aura.js`'s `fetch`/`XHR` patching runs long before any flag check in `main.js`, and a second run wraps our own wrapper. The reasoning about why it doesn't pay is the part worth keeping.

**Logcat**: clear the buffer *before* launching if the `[flame-inject]` lines are wanted; it rolls past a launch quickly. A healthy sequence reads `hook installed` → `setWebViewClient` → `boot cover shown` → `onPageFinished /s/` → `injecting reskin` → `status bar overlay painted, height=134` → `navbar inset pushed: 48px device` → `boot cover removed (ready, 4650ms)`. Absent that, the UI rendering *is* the evidence the hook fired — nothing else injects the reskin.

### Could the APK build run in GitHub Actions? Yes, with two prerequisites

Investigated, not built.

- **Signing is not the problem**, contrary to first assumption: `objection.jks` ships inside the objection package (`site-packages/objection/utils/assets/objection.jks`) and is identical on every install of a version. Pin `objection==1.12.5` and CI emits an APK signed `CN=Unknown, OU=objection, O=SensePost` — the same cert as local builds, so `adb install -r` still preserves the session. Verify with `apksigner verify --print-certs`; current signer SHA-256 begins `f7697c66…f46ce0fb`.
- **Prerequisite 1 — the base APK.** `flame-merged.apk` (33MB) can't be committed sensibly: binaries don't delta-compress, so every re-pull adds another permanent 33MB to every clone. Put it in a private release asset and `gh release download` it.
- **Prerequisite 2 — the toolchain sources**, already done: `.gitignore` inverts `.patch-tools/*` with exceptions for the three real source files. `hook.js` and `hook.compiled.js` stay ignored because 90% of `hook.js` is the pasted userscript, which would put a second near-identical diff in every commit.
- Everything else is free: ubuntu-latest ships the JDK and Android SDK, 16GB covers `-Xmx6g`, objection fetches the gadget itself, and the local patch takes under two minutes. Trigger on `v*` tags and the release upload becomes automatic.

## Tried and rejected

- **The rail** (branch `rail`, `aaa8948`, local only) — hairline `.fr-rail` lists replacing filled `.fr-row` cards across Home and My Bookings, with unfilled slot tiles and date cells to match. Rejected on sight: "i don't like this rail thing at all". **Branch kept deliberately; don't delete it without asking.** `master` has no rail code.
- **A permanent mobile top strip** for the stock-UI toggle — see the Shell section.
- **Shrink-to-fit class titles** — see the measurement table.
- **A home-screen widget** (asked, analysed, dropped: "forget that"). The analysis is counterintuitive enough to keep: **the timetable is semester-static**, so a widget needs no live data, which removes the hard part entirely. Four paths, cheapest first: **(1) export to the phone's calendar as `.ics`** — not a widget and better than one, since the events already carry everything iCalendar needs (`startDateTime`, `endDateTime`, `courseName`, room, faculty), and every calendar widget then shows classes, with notifications, offline, outliving the session (caveat: a WebView download needs a `DownloadListener` the app may not have, so generate it on desktop or via a share intent); (2) Termux:Widget/KWGT with an exported cookie, re-exported when it expires; (3) a separate Kotlin companion app holding its own `CookieManager` session — the proper answer for a live widget, and it avoids smali entirely; (4) patching a widget into MyFLAME itself — new resource IDs inside an already-compiled `resources.arsc` is the fiddly part. Most work, least payoff over (3). Don't.
- **The native scheduled auto-booker** (`AlarmManager` + `BroadcastReceiver` + headless WebView) — superseded by the localStorage version, and it is still the only design that could win a five-minute rush, so it is the one to come back to if the shipped version proves too slow. Two risks would need settling first, both unverified: whether the Salesforce session survives 24h fully idle (if not, nothing can recover it — Google SSO can't be scripted), and HyperOS battery optimisation killing scheduled alarms unless the app is whitelisted.
- **Kiwi Browser + Tampermonkey** as the Android path — still a working no-patch fallback (it runs desktop Chrome extensions, and Chromium's "Add to Home Screen" gives a chromeless standalone window). Ruled out alongside it: Brave for Android has no extension support at all, and Firefox does support Violentmonkey but its Add to Home Screen always opens a normal tab with browser UI.

### UI references the user wanted to look through

Named while discussing a broader overhaul; the user is deciding for themselves. What each is worth stealing:

- **Things 3** — the strongest case that deleting boxes makes a list *prettier*. Type and whitespace only, one accent.
- **Structured** — a time rail done well; colour-coded dots carry category without decoration.
- **Cron / Notion Calendar** — dark, hairline, restrained; proof a dark schedule UI can feel expensive with almost no colour.
- **Linear** — low-chroma dark, 1px borders instead of filled surfaces, obsessive consistency.
- **Amie** — the counter-argument: big type, playful colour, still simple. Look here if it should feel less severe.
- **Fantastical** — dense day lists that stay legible; strong time-column typography.
- **Citymapper** — a vertical time rail with connectors, if a day should read as a journey.

A comparison board (Rail / Timeline / Quiet-slab against the real schedule) was published as an artifact during that discussion. Its one durable finding: **a timeline whose block height maps to duration is wasted on Home**, because every FLAME class is 55 minutes — height would encode only the gaps. Worth stealing for Calendar, not Home.

## Environment-dependent values: the audit

The duplicate-slot bug (Book Slot, above) was one instance of a family: **an environment-dependent value reaching a comparison, a key, `localStorage`, or a request body.** Locale, timezone and engine-specific parsing are all fine for pixels and never for identity. The whole source was swept for the shape on 2026-09-04; this records what was found so it is not re-derived.

Still open, both judged not worth fixing yet:

- **Slot instants are built in the device's timezone.** `slotStartDate()` makes a local `Date` from the portal's clock time, and that instant then decides the 24h window, `windowSpent()`'s "already started", and — via `isoDateLocal(new Date())` — which day is "today". A phone off IST shifts all of it by the offset. Display is immune: parsing local and formatting local cancel out, so wall-clock times read correctly anywhere. Only booking logic is exposed, and only for someone travelling or with a hand-set clock. India has no DST, so that half is free.
- **`hoursFromResourceName` requires the literal word `to`** (`RESOURCE_HOURS_RE`). `Gym ( 6:00 am - 2:00 pm slot )` returns null, and with it go the derived slots: no skeleton grid, no local answer from `windowSpent`, nothing schedulable past the 24h window. Degrades silently to a spinner. Not a locale problem, the same brittleness — parsing the server's prose.

Checked and clean, so they need no second look:

- Every remaining `toLocale*` call is display-only, straight into `textContent`: `dates.js:34,49,70,74,79`, `calendar.js:181,203`, `shell.js:229`. None feeds a key or a comparison.
- `cleanResourceName().toLowerCase()` in `resourceClassKey` is safe. `toLowerCase` is locale-independent by spec; only `toLocaleLowerCase` carries the Turkish dotless-i behaviour.
- Events arrive as `"2025-03-18T10:00:00"`, a date-time with **no offset**, which the spec parses as *local* — so the wall-clock time displayed is stable in any timezone.
- `parseBookingDateTime` leans on `new Date("2026-08-31 9:00 AM")`, a format no spec requires. Tested on V8 with a plain space, U+00A0 and U+202F: all three parse. So an ICU upgrade on the portal's side won't break it in WebView or Chrome. **Firefox and Safari are untested** — only the desktop userscript ever runs there.
- The remaining `Map`/`Set` keys are intent ids or ISO dates, never formatted text: `paintAutoBookBanner`'s two sets (`autobook.js:480,492`) hold intent ids, and `runAutoBook`'s `claimed` (`autobook.js:523`) is keyed `resourceClassKey|isoDate`.

## Open items

1. **The login page is untouched.** The script bails via `isLoginPage()` — login is Google SSO and breaking it costs real access. If revisited: the same full-replace approach should be fine for the *portal's own* login chrome, since the OAuth handshake happens on Google's origin where this script never runs; the risk is blanking the button before it's clickable, not the auth flow. `isLoginPage()`'s path guess (`/login`, `/identity`, `/idp`) is **unconfirmed** — the HAR only covers post-login traffic. Verify the real path first.
2. **Long class titles on Home** — see the table above; two candidate fixes, neither chosen.
3. **Autobooking's 24h model, `startTime` passthrough and the "class" definition** are unverified against the real portal.
4. Gyan's bold rendering may not look bold on a device whose system font lacks a 700 weight.

## Releases

| Tag | Notes |
|---|---|
| `v1.0.3` | Current. A daily autobook can be started from an ordinary booking — the `Daily` toggle is in the manual confirm panel too, so "every day" no longer has to be said from a future day. Two settle paths that killed a running series silently now spawn the next occurrence. Carries the pinned resource collator held back from v1.0.2. |
| `v1.0.2` | Book Slot no longer draws every slot twice on phones whose locale formats times differently from the portal — slot times are compared on the minute now, not on the text. |
| `v1.0.1` | Gyan suggestion chips no longer fire a turn the composer is refusing, and a failed turn no longer leaves the tab dead. Book Slot answers a spent operating window locally instead of spinning into a bare empty state, and defaults to a window that is still live. |
| `v1.0.0` | Skeleton loaders, the repaint drop, past-slot prediction, the one-row empty state. First non-dated tag; numbering moves forward from here. |
| `v2026.09.04.1` | Confirm-button scroll + picker highlight. |
| `v2026.09.04` | Autobook confirm panel, daily repeat, the four rule bugs. |
| `v2026.09.03.2` | Predates the `src/` split, the cold-launch cover and the autobook UI work. |
| `v2026.09.03`, `v2026.09.03.1`, `v2026.09.01` | Earlier. |

Assets are named for their version (`MyFLAME-reskin-v1.0.1.apk`). They were `MyFLAME-reskin-working.apk` back when "working" meant *the build that finally worked*; every release is one, so the word said nothing and two downloads could not be told apart. `*.apk` is gitignored — this is an asset and local-copy convention only, and v1.0.0's asset keeps the name it was published under.

Repo: https://github.com/rk4500/myflame-portal-reskin (private).

## Files

| | |
|---|---|
| `src/` | the source — one module per tab, plus `aura.js`, `shell.js`, `state.js`, `persist.js`, `autobook.js`, `styles.css` |
| `build.mjs` | the build; also fuses `.patch-tools/hook.src.js` with the built userscript |
| `portal-reskin.user.js` | build output: the whole reskin in one file, no runtime dependencies |
| `preview.html` | local QA harness |
| `aura_map.json` / `aura_map.md` | every mapped RPC call, with real request and response shapes |
| `parse_aura_har.py` | HAR → RPC map: `python3 parse_aura_har.py portal.har aura_map`. **Merges** into the existing map by default; `--fresh` overwrites. (It used to overwrite silently, which lost the first 24-call capture once.) |
| `*.har` (untracked) | the captures everything above was derived from — the original archive, book-and-cancel, the "more than 1 not allowed" refusal, and two Gyan sessions. Gitignored: they carry live session tokens and are huge. |
| `PRODUCT.md` | product context |
| `.patch-tools/` | Android toolchain; only `hook.src.js`, `gadget-config.json`, `probe.js` are tracked |
